# Copy to keystore.properties (gitignored) and fill in. Never commit real secrets.
# storeFile is relative to apps/android/ (or use an absolute path).
storeFile=upload-keystore.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
