# PLAN.md — Find a past session (`.1008`)

**Freeze.** Implement only this file. Do not reopen refused items mid-build.
**Not** [docs/PLAN.md](docs/PLAN.md) (build phases A–I). The living roadmap
gets a matching frozen section so agents following the boot order find this
ship; this file is the find-past-session freeze.
**Lane:** Engineering-Web · History · **Horizon:** 0
**Label:** `2026.07-unified.1008` (master is `.1007` / `cd066471`
Name this finished session). Title stays **Find a past
session (.1008)**.
**Excellence-Override:** leftover search of the History list
after import + backfill (not a Feed, not Today)

---

## 0. What this is

Week strip + movement history + start-from
still leave a long scroll. Search the
History list. Empty query invents nothing.
Guest. First set ungated. Today stays one
Start.

`PRIVATE_MODE` stays on. Live www
stays `.696`. Do not promote. Do
not merge.

Full lock: [docs/PLAN.md](docs/PLAN.md)
frozen section `.1008`.

---

## 1. Investigate (done)

Name box matches `workoutName` only.
Untitled rows are dates. Tombs are
Restore, not search.

---

## 2. Lock

1. Empty query → same rows. Else match
   title, template, date, lift, note.
2. No matches → empty. Tombs stay out.
3. No Today chrome. No Feed.
4. `[skip vercel]` on every commit.
   Do not merge this PR yourself.
