# PLAN.md — This-movement title is the date or the name (`.1012`)

**Freeze.** Implement only this file. Do not reopen refused items mid-build.
**Not** [docs/PLAN.md](docs/PLAN.md) (build phases A–I). The living roadmap
gets a matching frozen section so agents following the boot order find this
ship; this file is the this-movement title freeze.
**Lane:** Engineering-Web · Train · **Horizon:** 0
**Label:** `2026.07-unified.1012` (master is `.1011` / `8df323949`
Export this diary). Title stays **This-movement title
is the date or the name (.1012)**.
**Excellence-Override:** leftover this-movement titles
after History name (`.1007`) (not a Feed)

---

## 0. What this is

`.1007` said empty is the date, a
name is the private title, the
template stays `workoutName`.
History list already does that.
Tap the open lift and the sheet
still bolds the template.

Empty invents nothing. Guest. First
set ungated. Today stays one Start.

`PRIVATE_MODE` stays on. Live www
stays `.696`. Do not promote. Do
not merge.

Full lock: [docs/PLAN.md](docs/PLAN.md)
frozen section `.1012`.

---

## 1. Investigate (done)

`MovementHistorySheet` bolds
`row.workoutName`. `listMovementHistory`
never copies `sessionTitle`.
`historySessionLabel` is the one
home.

---

## 2. Lock

1. This-movement row title is
   `historySessionLabel` (date or
   private title).
2. Template is subtitle only when
   it differs.
3. Empty / missing / tomb invents
   nothing. Do not smash History
   skip or export `.1011`.
4. No Today chrome. No Feed.
5. Do not thread duration Next/Last
   in this hop.
6. `[skip vercel]` on every commit.
   Do not merge this PR yourself.
