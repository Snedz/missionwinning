# CONTEXT.md — boot file (read first, every tool)

One screen of truth for any AI tool or human joining cold. Read this, then [AGENTS.md](AGENTS.md), then [INDEX.md](INDEX.md). Keep `## Now` current: update it on every ship, in the same commit as the [LOG.md](LOG.md) entry.

---

## What this is

**Mission Winning** · www.missionwinning.com · "Train Anywhere. Win Daily."

> Adaptive AI coaching for train-anywhere athletes — free offline logging (no account), weekly plans from logs alone (no wearable). Super Bundle adds Coach depth and the other pillars — it never gates the logger.

- Six pillars — Train · Fuel · Move · Mind · Track · Learn — unified by the Mission Score. Constitution: [vision.md](vision.md). Pitch the **Train + Mission Coach wedge**, never "everything app" ([docs/YC_THESIS.md](docs/YC_THESIS.md)). Evidence thesis (structured exercise vs vague advice — not a depression product): [docs/EXERCISE_AS_MEDICINE.md](docs/EXERCISE_AS_MEDICINE.md). Crypto is a **payment rail** (Lifetime USDC), not the product ([docs/CRYPTO_RAILS_THESIS.md](docs/CRYPTO_RAILS_THESIS.md)).
- Surfaces: Next.js 16 PWA (repo root) · native Android Compose ([apps/android](apps/android), v1.24.1) · iOS deferred ([docs/IOS_PLAYBOOK.md](docs/IOS_PLAYBOOK.md)) · "Beyond the Basics" guidebook (`/guide` + magazine PDF).
- Solo founder + AI agents. Founder owns users, money, legal, secrets, `PRIVATE_MODE`. Agents own code, tests, perf, docs — inside horizon gates.

---

## Now (2026-07-30 · web `2026.07-unified.209` · Android `1.24.1`)

> The ONLY "where we are" block in the repo — [ORCHESTRATION.md](ORCHESTRATION.md) points here.
>
> **Budget: ≤25 bullets.** Enforced by `src/lib/contextBudget.test.ts`. When over,
> rotate the oldest *shipped* entries to `docs/archive/` — `.203` moved `.123`–`.189`
> and `.207` moved `.190`–`.193`
> to [CONTEXT-now-2026-07-30.md](docs/archive/CONTEXT-now-2026-07-30.md) after this
> block reached **79 bullets / 103KB**. A status doc that only grows stops being read.

- **Horizon W — wedge excellence.** Web craft 1–6 + **Fuel estimate accuracy** (edit-before-log, better NL match, honest photo sources).
- **Free-first beta (~4 weeks):** LLC + EIN pending — **no Bundle UI** + **full depth unlocked** + **full More nav** (journey train-only deferred) ([docs/FREE_BETA.md](docs/FREE_BETA.md)).
- **Modernist rebrand (`.130`–`.131`):** founder-commissioned full rebrand (design handoff 2026-07-25 — paper/ink/**one red**, Archivo only, radius 0, 2px rules, light-only; override recorded in [docs/DESIGN_ORCHESTRATION.md](docs/DESIGN_ORCHESTRATION.md) wave D5). `.130` restyled `/private` (the whole public site while gated). `.131` is the **global token swap**: one `:root` flip in [src/index.css](src/index.css), Archivo only, whole Tailwind radius scale → 0, glows/gradients/shadows retired, ink-square mark + favicon/PWA icons + OG re-ink, form-guide SVGs re-inked (repo still had the navy set), DESIGN_SYSTEM/brand-guidelines rewritten. **Red is three tokens** (`--accent-poster` #ec3013 fills+chrome · `--primary-fill` #dd2b0f button fills · `--primary` #ae1800 small text) — paper inverts the `.127` contrast math; small red text is never poster red. **Android sync paused** (founder call) — Android rebrands after web; `check-token-sync` pins web values, motion still enforced. Beta testers see restyled-but-not-recut app screens until Phase 3. `.132` recut the shared public shell; `.133` the landing (flat hero, checkable stat row, numbered sections, red poster close). `.134` added the public /calculators/{1rm,tdee,strength-standards} SEO routes (new strengthStandards ladder lib, registered in sitemap+gate+smoke). `.135` recut the SEO furniture (square filter tags, 2px compare table w/ tinted MW column, guide TOC case). `.136` opened Phase 3 on the app: nav active = 2px red top rule, all blur+shadow gone (28→0), pills squared, coach week strip honest. `.137` re-inked the guidebook cover (PDF rebuilt, 27pp); `.138` shipped the three Modernist HTML emails + the first beta-invite sender. **Rebrand build complete and MERGED** (#89–97, 2026-07-26); founder owns `MAIL_POSTAL_ADDRESS`, photography, wordmark, and the `PRIVATE_MODE` flip.
- **Sheets + states (`.155`, partial):** `FormGuideSheet` folded onto `AdaptiveOverlay` — it was hand-rolled at `z-[60]`, **below** the shared shell's `z-[70]`, which is why a form guide could open *underneath* an open sheet and why the focus trap existed twice. Three overlay mechanisms → **two** (Radix `LibraryDetailSheet` is the last). **Session check-in scales were invisible** (`bg-muted` on a `bg-card` sheet = 1.01:1, four of five buttons absent until tapped) → one 2px-ruled strip; Save/Skip pinned at a full 52px each. **Offline is ink, never red**: [`OnlineStatusBanner`](src/components/layout/OnlineStatusBanner.tsx) was a centred 1px/60% strip wrapping to two lines at 390px; now one flush-left line under a 2px ink rule carrying the **outbox depth** — from the existing `outbox.subscribe()`, **not** the new storage key the handoff asks for (that would be a second source of truth). `app/error.tsx` matched to `not-found.tsx` (the `uppercase` was Barlow's), digest promoted to a labelled reference block. **Still open:** exercise picker → sheet, plate squares, Fuel meal strip + `estimateMealFromDescription` entry point, Adjust "Applied" panel, `LibraryDetailSheet` off Radix, offline "Waiting to sync" list, per-screen loading labels — each paired with a held pillar screen.
- **`e2e:visual` is the one dark gate.** Its three baselines were generated **2026-07-22, before the rebrand**, and depicted the old navy/emerald dark design — black grounds, emerald CTAs, rounded corners. Verified by opening one, not inferred from dates. **Deleted**, because a known-wrong baseline is worse than none: the first Linux run would have shown four huge diffs, and the reflex there is `--update-snapshots` without looking, which launders whatever renders that day into truth. `home-reduced.png` **never had a baseline at all**, so the homepage has been silently self-approving since the case was written. **First Linux CI run after billing clears must bootstrap all four** (`npx playwright test --grep @visual --update-snapshots`, then commit the artifact) — it cannot be done on macOS, the pixels will not match.
- **306 KB of translations on every route (`.209`):** measured, not estimated — the largest user-visible win in the wave. [`localeHttpLoader.ts`](src/lib/localeHttpLoader.ts) needed two metadata fields per namespace and got them by importing `LOCALE_EXPORTS`, which pulls 28 `*Locales.ts` modules **and** `localePacks`'s 14 JSON packs (1.1 MB on disk) — and it is reached from the **root layout**, so `app/layout.tsx → I18nPwaProvider → LocaleHttpSync → localeHttpLoader → exportLocales → localePacks` was entirely static. One `chunks/7660-…js` at **1,053,845 B raw / 313,839 B gzipped**, a plain `<script async>` on `/`, `/log` *and* `/active` — **44 % of the initial JS on the logger**. An English user in Nairobi on 3G downloaded Thai, Vietnamese and Hindi before first paint. `hydrateResources.ts` and `src/i18n.ts` both carry headers claiming the opposite: **true of the source, false of the build**, which is exactly why a byte budget beats a comment. Fix is [`localeExportManifest.ts`](src/i18n/localeExportManifest.ts) with **zero imports**; `exportLocales` derives `LocaleNamespace` from it so the list exists once. After: `/log` **264.3 KB**, `/active` **420.3 KB**, `/` **247.4 KB** gzipped, the megachunk gone from the build, and a byte scan of every chunk still on `/log` finds **no locale-pack strings** (checked for `Platos`, `ครั้ง`, `तकरार` rather than trusting a chunk name). **The missing gate step now exists**: [`bundle-budget.mjs`](scripts/bundle-budget.mjs) reads the *prerendered HTML* — which `<script>` tags the browser actually fetches, not what the graph contains — runs after the build, ratchets down only (`.202` shape), and **fails rather than passes when `.next/` is absent**. A second rule runs in the unit lane with no build: nothing on the root-layout path may value-import the locale bodies, and the manifest must import nothing. **The budget failed on its own first gate run**: I calibrated `/` against a default build (the six-chunk gated teaser, 164 KB) while `gate.mjs` builds `PRIVATE_MODE=false`, where `/` is the real landing page at 247.4 KB — the `.204` asymmetry again, *the gate measures a configuration production does not serve*. Caps now set against what the gate builds, with the note at the constant. Three mutants killed. Tests 1061→1066.
- **Numbers the product overstates (`.208`):** two celebrations the app could not support. **Four filters for one quantity** — [`percentLoad.ts`](src/lib/workout/percentLoad.ts) says the prescriptions, the /benchmarks cards, the live PR chip and the debrief "all quote the same number", and they did not: the PR chip **included sets taken to failure** while the other three excluded them. Prior best bench 100×6 (e1RM 116.1); log 100×8 marked failure → 124.1 → the brass chip fires and `isPr` goes into the log, while the chart, the debrief and next session's prescribed load all still say 116.1 — verbatim the failure `benchmarks.ts:4-9` claims was already fixed. A set to failure gives the highest and least repeatable reading, which is *why* the three estimate consumers refuse it, so the celebration fired on the one set kind the rest of the app declines to trust. **The `drop` disagreement was left standing on purpose** — it is taste, not arithmetic (a drop set is evidence but the burnout is not a moment to celebrate), so the two reasons are now two names: `countsTowardStrengthEstimate` for what the app *believes*, `countsTowardPr` for what it *celebrates*. Flattening a deliberate choice to make a table tidy would have been the wrong fix. **The PR that could never not happen**: `estimateOneRepMax` returns 0 above 12 reps, so `getBestPriorSet` returned **null forever** for a high-rep-only athlete while `isPersonalRecord` answered `if (!prior) return true` — goblet squats at 20kg × 20 got the chip and the haptic on set one and on set two hundred, with `isPr` written to the log each time. Checking the *new* set first fixes it; the genuine first-estimable-set case still reads as a PR. Two existing tests encoded the old behaviour with no rationale; one was the bug written down. Three mutants killed. Tests 1058→1061. **Streak decay, planned for `.208`, is deliberately not here** — separate wrong number, wider blast radius (`StreakChip`, `WorkoutVictorySheet`, `computeWinScore`, leaderboard), carried forward.
- **The coach that thinks it is always Monday (`.207`):** `useCoachPlan` called `adaptPlan(existing, ctx, weekStart)` — the start of the week as **today** — so `todayDayOffset` was always `0`. Two live consequences: `dayOffset < todayOffset` never matched, so **no session was ever marked missed** and the "life happened, days re-spread" adaptation could not fire at all; and the low-readiness recovery swap targets `dayOffset === todayOffset`, so on a Thursday with readiness 30 the coach swapped **Monday's** session — already gone — and left Thursday's heavy squat day standing. The real date was in scope the whole time, three lines up. **The revision was a claim nobody had to earn**: `adaptPlan` returned `revision + 1` unconditionally and the hook guards its save with `next.revision !== existing.revision`, always true — so every mount rewrote the plan, called `scheduleCoachPush()`, and `hasCoachAdaptationSignal` (`revision > 1`) told **every athlete on every visit** that their week had been adapted when nothing had changed (`.127`). An unchanged week now returns the same object. **The cycle**: `savePlan` dispatches, the hook listens and calls `refresh()` which called `savePlan` — synchronous `dispatchEvent`, two consumers mounted in `HomeTodayDashboard`. **I did not execute it** (needs a browser and a React tree), so rather than claim a reproduction, [`adaptTermination.test.ts`](src/lib/coach/adaptTermination.test.ts) pins the property that closes it: `adaptPlan` is idempotent on **every day of the week**. **The vacuous assertion**: `adapt.test.ts`'s "marks past sessions missed" passed `today = weekStart` then asserted `missed.length >= 0` — true of every array ever created, green for the whole life of the bug it was named after. **One mutant survived first run and it was my own new guard** — `recovery-swap-hits-the-wrong-day`, because the test read `if (kind !== 'strength') return` and the fixture has no session on day 3 at all. Fifth vacuous guard in this programme; preconditions are now asserted, never skipped past. Tests 1049→1058.
- **Two controls that write a number nobody asked for (`.206`):** both the same shape — a control touched for one reason quietly writes a second value the athlete never entered. **Editing reps zeroed the prescribed weight**: `updateSetInput` rebuilt the whole `{reps, weight}` pair from `getSetInput(exIdx, setIdx, 10, 0)`, and `resolveSetInput` returns those defaults **verbatim** for a prescribed exercise — so a coached bench 3×5 @ 100kg tapped once on reps logged **6 × 0kg**, zero volume on the lift the coach asked for, and then `suggestNextSetTarget` computed next week from a set that never happened. Every other `getSetInput` call site passes `set.reps, set.weight`; this one alone passed `10, 0`, which is why the console *displayed* the prescription correctly right up to the moment it was edited. Second half: it read `setInputs` from the render closure not the updater's `prev`, and "Apply targets" fires two synchronous calls per set — so a 3×5 prefilled as **10 reps**, verbatim the `.175` bug its own comment claims was fixed. Rule extracted to [`nextSetInput`](src/lib/workout/activeWorkoutHelpers.ts) because the defect is a *shape* (`.196`). `activeWorkoutHelpers.test.ts` covered `resolveSetInput` thoroughly and **it was never wrong** — what broke was the arguments a caller handed it, which no test of a correct function can see. **Fuel logged your goal as your lunch**: `applyTargets` called `saveMacroTargets` (the whole job) and *also* unshifted a `Calc target protein 180g` row into `mw_nutrition_log`, which `NutritionPage` sums into the day's **consumed** totals — so a 2400 kcal target read as 2400 kcal eaten before any meal, and fed the 150g high-protein counter, inflating the Mission Score's Fuel pillar off a number the athlete only wished for (`.127` at the source). It also wrote `logs.slice(0, 50)`, the only row cap in the repo where every other writer keeps **90 days** — under two weeks of real use, so one tap deleted months of meals. `.170`'s rule one layer down: deleting the write fixes both, and the guard is stated repo-wide (pruned by age, never by row count). Four mutants killed. Tests 1041→1049.
- **The restore the app forgets, then destroys (`.205`):** the worst defect the `.204`–`.213` audits found — it destroys the data an athlete deliberately asked the app to protect. [`backup.ts`](src/lib/backup.ts) and [`importCsvRestore.ts`](src/lib/workout/importCsvRestore.ts) write the zustand persist payload straight to `WORKOUT_STORE_KEY` and, by design, leave the refresh to the caller (pure merge testable, browser half thin — that seam is right). Both callers ran `setTimeout(() => router.refresh(), 1200)`, and **`router.refresh()` preserves client-side React state by design**: the store was already hydrated, `persist` re-reads storage only on rehydrate, and nothing asked it to. So — new phone, restore 300 workouts, toast says *"300 workouts merged… Reloading…"*, **nothing reloads**, History still empty, the athlete logs one set, `persist` serialises its in-memory state over the file, and **all 300 workouts are gone permanently**. Identical for Strong/Hevy CSV import. [`backup.test.ts`](src/lib/backup.test.ts) covers `mergeBackup` thoroughly and could not see it, because **the merge was always correct** — the defect lived entirely in the sentence after it, which is why the guard reads call sites rather than merge results. New [`reloadAfterRestore.ts`](src/lib/storage/reloadAfterRestore.ts) names the rule and owns it: *a function that writes storage behind a live store must say how the store learns.* A full document reload, not `persist.rehydrate()` — rehydrating repopulates the store but leaves every module that cached a read at mount holding pre-restore values, and the copy had been promising a reload the whole time. The guard also pins the copy: a toast saying "Reloading…" must be attached to something that reloads. Two mutants, three failures each. **Process note:** my mutant cleanup used `git checkout --` on files holding uncommitted work and silently reverted the fix — second occurrence, after `.202`. The new guard caught it on the next run. Rule now explicit: **commit before mutating.** Tests 1035→1041.
- **The invite that discarded its own code (`.204`):** the beta gate is red because there are fewer than 10 users, and **the mechanism for getting users threw the invite code away**. The shipped invite email linked to `/?invite=CODE`; [`proxy.ts`](proxy.ts) redirected with `new URL('/private', request.url)`, and **the `URL` constructor replaces the path *and the query*** — so every invited tester arrived with the code stripped, `isInvitee` false, and was shown the **public waitlist form** with the code field folded inside a `<details>`. The one screen written for an invited tester was unreachable from the one email that exists to send them there; `BETA_INVITE.md` and `print-beta-invite.ts` both say `/private?invite=…`, only the shipped HTML disagreed. The same line dropped `?next=`, which the gate also reads and **nothing had ever set** — dead code guarding the return path for every bookmark, push URL and share link. Cloning instead of constructing fixes both. **Why nine waves missed it:** `gate.mjs` builds `PRIVATE_MODE=false` (deliberately — that is what compiles the SW `offline.spec.ts` needs), so all 50 e2e and 33 a11y cases run against a config **no beta user will ever load**, and the first path every tester touches had never been executed. `.200`'s thesis applied to the deployed configuration. New [`privateGateRedirect.routetest.ts`](src/lib/privateGateRedirect.routetest.ts) calls `proxy()` under `PRIVATE_MODE=true` and asserts on the `Location` header — 18ms against ~3min for a second gated build. **Onboarding could also take a session away**: `startWorkout` replaces `activeWorkout` outright, right at its 17 other call sites (each an athlete tapping "start this workout") and wrong at `WelcomePage.finish()`, which a returning athlete reaches by accident because `/` renders marketing with one CTA back into onboarding — new `hasLoggedWork` draws the line at a **completed set**, guarded at the call site rather than in the store. **And two cards reached only athletes who no longer needed them**: `BetaWelcomeBanner` ("finish I-Day, log one workout, open Coach") and `TodayReentryCard` were dashboard-only while `HomePage` sends `i-day`/`basic` to the lean shell — and `basic` requires all five pillars, so the instructions appeared *after* the thing they instruct. Both now on shared [`todayGuidanceMount.ts`](src/lib/today/todayGuidanceMount.ts) (`.195` pattern; `i-day` still bare). One audit claim **refuted rather than fixed** — the lapsed-before-first-workout cohort needs no re-entry card, because `computeReentry` returns `NONE` for zero history on purpose. Four mutants killed; the redirect guard failed its own first run by matching the broken spelling inside the comment shipped with the fix — `ciTruth`'s workflow-header trap again. Tests 1022→1035, routes 7→12. **`MAIL_POSTAL_ADDRESS` is still unset, so the email cannot be sent** — this fixes the link, not the send.
- **State the repo can be trusted about (`.203`):** documentation as an executable contract. **Four migrations sat on disk in no founder checklist** — `20260731_llm_usage.sql` (`.188` LLM spend ledger, one parenthetical in ENV.md), `20260801_day_review_push.sql` (`.194`/`.196` evening push, named nowhere at all), and `20260719_wearable_connections.sql` + `20260720_perf_indexes.sql` — the last two found by [`migrationLedger.test.ts`](src/lib/migrationLedger.test.ts) on its **first run**, after the audit that wrote the guard had already declared the answer was two. A migration nobody wrote down is a migration nobody applies: the feature merges, ships, and the column it needs never exists. The guard is three rules — every file on disk is in [LAUNCH_RUNBOOK](docs/LAUNCH_RUNBOOK.md) §3 (base schema exempted as a glob, because eight files applied together should be named as a group), the runbook names nothing that is not on disk (a dead instruction wastes the one person who follows it), and **every entry says what breaks without it** — that third rule caught three older entries carrying a bare filename, which is exactly the shape `llm_usage` had in ENV.md while going unapplied. **`CONTEXT.md`'s own `## Now` was 79 bullets / 103KB** under a header calling it *"one screen of truth for any AI tool or human joining cold"* — seven of those bullets added by me in a single day. Same `+1`-per-feature growth `.197` fixed on the Today screen, so the same treatment: budget **≤25**, `.123`–`.189` rotated to [CONTEXT-now-2026-07-30.md](docs/archive/CONTEXT-now-2026-07-30.md), the block states its own budget, and [`contextBudget.test.ts`](src/lib/contextBudget.test.ts) fails the next PR that grows it silently. Two `.184`-class closures: `ProfileOwnerTools` read `mw_contributors`, which **nothing has ever written**, so "Members: 12,400" and "$44,640" were constants dressed as stored data — now a named `DEMO_MEMBERS` labelled *"(illustrative)"*, key deleted from the registry; and `restorePremiumCourseProgressForUser` had **zero callers** since it was written, so course progress mirrored device→device and never came back on a new one — wired into the `SIGNED_IN` branch of `useJourneySync`. Killed: `migration-not-in-the-ledger`, `ledger-entry-with-no-reason`, `context-now-unbounded`. Tests 1016→1022. **Closes the `.199`–`.203` plan.**
- **Measured, and it can only shrink (`.202`):** **`i18n-parity.ts` never opens a `.tsx`** — its only filesystem read is its own allowlist; everything else compares locale packs *to each other*. So it is structurally blind to a key a component uses that exists in **no pack at all**: all 15 languages agree they don't have it, and parity is trivially satisfied. First measurement: **665 of 1538 literal keys (43%) exist in no EN pack**. Since every call site is `t('key', { defaultValue: 'English' })`, nobody sees raw keys — they see **English**, silently, in 14 non-English languages, and the `@gate` no-raw-keys test passes and always will. Per the founder call (measure and ratchet, no translation work), [`i18n-coverage.ts`](scripts/i18n-coverage.ts) counts the gap, fails on any **new** uncovered key, and its cap can only be lowered — in the gate, because `i18n:parity` cannot see these keys by construction. **The cap shipped at 710, not 665:** five components (`TodayDayReviewCard`, `DayReviewOptIn`, `BehaviorStrip`, `DailyCheckIn`, `BreathingTimer`) had **no `useTranslation` at all**, and giving them one added 42 keys — measurement widening, not debt growing, since those strings previously had *no key* and were untranslatable by anyone. [`i18nCoverage.test.ts`](src/lib/i18nCoverage.test.ts) pins a checked-in high-water mark so the next raise must edit two files with the reason in the diff. **`BehaviorStrip` now uses the `labelKey`/`receiptKey` pairs `behaviors.ts` has shipped since `.190` and nothing had ever read** — the library was built i18n-ready and its only renderer ignored that for eleven builds; twelve behavior questions and their evidence receipts are translatable for the first time. Five mutants killed. **Two process notes:** `key-never-added-to-a-pack` survived first run because I wrote it as a *swap*, which leaves a total-count ratchet unmoved (a real regression *adds* a key); and my mutant cleanup used `git checkout --` on two files whose work was **uncommitted**, silently reverting them — the new coverage check caught it on the next run. Tests 1012→1016.
- **The screen that must not break (`.201`):** the app is fully client-rendered with **no nested `error.tsx` boundaries** (`app/error.tsx:8-10`), so any render-path throw replaces the whole route with the global error screen — which is what made each of these severe. **`MobileNav` threw inside `useMemo`**: a `MOBILE_TAB_HREFS` entry missing from `PRIMARY_NAV` blanked *every mobile screen*, since the bar is on all of them — a typo in five strings would have taken out the product on phones. Now it drops the unresolvable href and [`mobileNavTabs.test.ts`](src/lib/mobileNavTabs.test.ts) catches the mismatch where failing is free. **Six `activeWorkout!` assertions** on the logger narrowed — each was true in practice, but an assertion is a promise the compiler stops checking, on the one screen the product promises never breaks. **Then the new e2e guard found what neither audit had:** loading `/active` with corrupt persisted storage, `activeWorkout: 42` and `{id:'x'}` — valid JSON, wrong shape — **both blanked the screen**. `partialize` persists whatever the store held; what comes back is whatever is on the device. Fixed at rehydration with `isUsableActiveWorkout`: one check covering every consumer including future ones, and it **rejects the whole session when any exercise is unusable** rather than filtering — a half-restored session that silently loses sets is worse than a clean empty state the athlete can restart in one tap. Also `healthImport`'s unguarded `JSON.parse` (contained only by a property of its single caller) and `ProgramTemplatesPanel`'s `.find(…)!` dereferenced twenty lines later. Corrupt-storage resilience was unit-tested per parser and **never end to end** — which is precisely why both store failures survived: the parsers were fine; the crash was downstream. Tests 1003→1012; `@gate` e2e 46→50 plus 33 a11y.
- **The guards that never ran (`.200`):** the `.195`–`.199` defect class applied to the checks themselves. **`npm run a11y` executed nowhere** — 33 tests over 30 routes, excluded from the gate (whose own closing line said so) and absent from every workflow, while `ci.yml` claimed a11y "stays in CI extended" and `grep -rn a11y .github/workflows/` returned **zero hits**. The suite has existed since `.157`. Run for the first time it found **one real serious violation**: `/compare`'s table scrolls horizontally with no keyboard access, so every column past the fold existed only for people with a pointer — **32 of 33 passed**, so the suite was in far better shape than "never executed" implied. a11y now runs **inside the gate**, reusing the server the hero lane started (46 e2e + 33 a11y). **The visual job passed vacuously**: with no baselines it ran `--update-snapshots || true` then re-ran against the files it had just written — green every time over zero baselines. Now missing baselines fail loudly with bootstrap instructions; **baselines deliberately not generated here** (this container is Chromium 1194 against a repo pinning 1228; at 2% tolerance they would likely diff on a real runner). **One fact, one home:** `gate.mjs` asserted "Actions works again as of 2026-07-29" while CONTEXT recorded it re-blocked on the 30th — the repo contradicted itself about its own CI; and `ci.yml` inlined `npm audit` while `package.json` already defined `security-audit`. Guard [`ciTruth.test.ts`](src/lib/ciTruth.test.ts): *a guard nobody runs is a guard that does not exist* — every check runs somewhere or carries a reasoned `NOT_RUN` row, a11y specifically in the gate, and the visual step may not write its own baselines or swallow its exit code. **The suite caught three of my own mistakes on its first run**: `secrets:scan` looked CI-run because a *YAML comment* mentions it (the `.195` header-menu defect again → comments now stripped, and the exemption reason rewritten because it was wrong); the visual rule flagged **its own help text**; and switching ci.yml to the npm script broke compliance control **MW-MON-003**, which grepped the literal string `npm audit` — a third place encoding one fact, measuring a spelling. Tests 999→1003; gate 3.7→5.7 min.
- **One week, one answer (`.199`):** two live bugs, not hygiene. **The week started on different days depending on which file you asked** — Monday-offset arithmetic was re-derived **eight times across seven files** and **four were wrong**, each in a different band of the world: three called `toISOString()` on a local date (reproduced under `TZ=Asia/Tokyo` — week began **Sunday 07-26** when the correct Monday was **07-27**), and `coach/splitPlanner` anchored to noon first, which covers |offset| < 12 but **not UTC+13/+14** (reproduced under `TZ=Pacific/Kiritimati`). `historyAnalytics` held two more the audit missed, and they bucket the weekly volume chart. So weekly stats, challenges, pillar wins and the chart disagreed with the coach plan and week recap about which week it was — timezone- *and* time-of-day-dependent, which is why a suite running near UTC midday never saw it. The same mistake in miniature: `new Date().toISOString().split('T')[0]` writing a **UTC** date into **locally**-keyed records — **36 call sites across 24 files**, including the training streak, pillar wins, macro targets and assessments (a Tokyo athlete training at 08:00 wrote yesterday's date). New [`time/localDate.ts`](src/lib/time/localDate.ts) is the one implementation; five duplicate date formatters collapsed into it. **A comment I could not defend:** the first draft justified the `setHours`/`setDate` order as a DST guard; a surviving mutant sent me to sweep nine DST-heavy zones across 2026 and find **zero** disagreements — the claim was false and is deleted rather than tested. **The coach branch that could never run:** `savePreferredDays` had **zero callers**, so `mapToCalendar`'s athlete's-chosen-days branch was permanently dead and the even-spread fallback always ran (`.176`/`.196` a third time) — and `splitPlanner.test.ts` **already had** a passing test called *"honors preferredDays"* that passed them in directly, proving the branch worked while nothing could reach it. Now wired into the training-profile card. Guards: week-start and local-date as `SINGLE_DEFINITION` concepts, `schedulePrefs` in `WAVE_MODULES`, a rule that **no calendar date comes from `toISOString()`**, and a nine-timezone (UTC+14 → UTC−11, incl. Chatham +12:45) week test. **Two mutants survived first run** — one exposed the false comment, the other was the *identical* importing-is-not-using hole `.198` hit, third appearance in two PRs, now fixed once in `mentions()` rather than a third time at a call site. Tests 988→999.
- **The rule the test was not measuring (`.198`):** the fix is two words; the deliverable is what had to happen before they could be trusted. `.194` put a **second poster-red button on Today** (a default-variant shadcn `Button` renders `bg-primary-fill`) and **two tests should have caught it — neither did**: *"exactly one primary action"* counts elements carrying the **`.primary-action` class** and the button had the colour without the class, while *"every control is thumb-sized"* was scoped to **`/active`** and the 36px button was on `/log`. Two green tests, neither measuring what it is named for. Guard 1 ([`helpers/redActions.ts`](tests/e2e/helpers/redActions.ts)) reads **computed background colour**, stated as *the dock owns red, the cards do not* — a page-wide count of one was satisfied by the offending button alone, because the hero renders grey in some states. Guard 2 extracts the 44px sweep to [`helpers/thumbSweep.ts`](tests/e2e/helpers/thumbSweep.ts) and runs it on `/active`, `/log` (09:00 **and** 19:00) and `/mind` — **it found ~30 undersized controls on its first run** (breathing chips, every 1–5 rating button, every guided-session transport control), all fixed. **Systemic and deliberately not fixed:** the Button primitive's *default* size is `h-10` = 40px, under the 44px floor `.125` set — raising it changes every button in the app, so it is a founder call, named not made. Guard 3 counts rendered blocks from outside, importing `TODAY_MAX_TOP_LEVEL_BLOCKS`. Guard 4 extends `check-display-type` and **found a second red CTA on its first run** (`TodayJournalStrip`'s red `Log check-in →`, now outline). **The part worth recording:** Guard 1 passed with the bug in place **three times** while being written, each time because the surface was absent — the card returns null without data (by design), the opt-in needs a VAPID key that has been **unset in every e2e run this repo has ever done** (so every push surface has always rendered nothing under test), and it mounts only after a dynamic import resolves. The test now waits for both explicitly and fails on the precondition; `gate.mjs` injects a placeholder VAPID public key alongside the existing placeholder Supabase ones. Proof run: with the red button restored, the legacy assertion **passes** while Guard 1 **fails** naming it. Gate e2e 39→46.
- **One pass over the day (`.197`):** Today grew a card at a time and each card read the day for itself — by `.196` one render called `loadCheckIns()` **five times**, ran `computeBehaviorImpacts` **twice with byte-identical arguments**, and built the whole weekly debrief on a Tuesday to read `isFullDebrief` off it and learn it was not the weekend. **The fix is deliberately not per-card memoisation** — that is what produced this; four correct caches over four identical computations is still four computations, each hiding its cost inside a component. New [`todayDigest.ts`](src/lib/today/todayDigest.ts) owns the day, new [`useTodayDigest`](src/hooks/useTodayDigest.ts) reads storage once (in state, not a memo — device storage is not a React input). `buildWeeklyDebrief` is now dynamic and Sunday/Monday only, **injected** into the digest so that *"it ran zero times"* is a property a test asserts rather than a comment claiming it. New [`todayBlockBudget.ts`](src/lib/today/todayBlockBudget.ts) caps Today at **7 top-level blocks** (it reached eleven: every feature since `.170` was a permanent +1 and no PR is ever the one that made the screen long); overflow **spills into the existing disclosure rather than being deleted**, pinned blocks never spill, and priority decides *what* spills, never *what order things read in*. **Silent correctness bug found en route:** `buildMuscleHeatmap` used `EXERCISES.find()` against a **lazily loaded** catalog, so every session built from an extended-catalog exercise counted **zero** and the athlete saw those muscles reported as untrained — no error, no failing test, because the fixture always used `bench-press`. Also: `shareCard` moved inside its handler, `totalVolume`/`getTrainingStreak` memoised (both walk full history, both ran every render), `StaggerReveal` 70→50ms capped at 6 so the last block lands at 340ms not **740ms**, and `forceFull` deleted (zero callers, the `.195` rule). Guard [`todayPerf.test.ts`](src/lib/todayPerf.test.ts): no static heavy imports on the Today path, `loadCheckIns` only in the hook, one call site per correlation. Seven mutants — **`two-identical-impact-passes` survived first run**: "ran exactly once" is *not observable through the return value*, since a second pass over the same arrays produces an equal result, so the rule moved to the call-site count and says plainly that it is a shape guard. Tests 961→988.
- **The hour the athlete picked (`.196`):** `.176`, closed. `.194` shipped a `day_review_hour` column, a migration, a cron, an hourly workflow and a tone-tested push — and **one writer, which returned early whenever a push subscription already existed**. That is every athlete with the wind-down note on, which is exactly the population that would want an evening review: the column stayed NULL, `dayReviewDue` was always false, and the feature fired for nobody. Every test passed, because every test asked whether the decision was *correct* and none asked whether the input could ever arrive. The fix is **one row of a truth table** — new pure [`dayReviewPrefs.ts`](src/lib/dayReviewPrefs.ts) makes `dayReviewOfferState` total over its input and answers `hasPush: true, storedHour: null → 'offer'`, because having push and having an hour are different facts (a device with push was asked about the *wind-down* note). Written as a function, not a chain of early returns: a decision spelled `if (…) return;` can only be read by re-deriving it; one spelled as a function can be enumerated, and now is across all 32 combinations. `readDayReviewHour` never guesses — **anything unparseable is not-opted-in, never a default hour**, since an unrequested nightly notification is the one failure this cannot have. **A one-time offer is not a setting**, so new [`ProfileDayReviewRow`](src/components/profile/ProfileDayReviewRow.tsx) gives the hour a permanent home, gated on `pushSupported` not `pushOn` — gating on push-already-on would rebuild the dead end from the other side. **`null` vs `undefined` stays distinguishable and that distinction is the safety property**: null is "Off" and must reach the column, undefined is an unrelated sync that must leave it standing; collapsing them means either the athlete cannot turn it off or any passive page mount turns it off for them. Guard [`pushPrefsReachable.test.ts`](src/lib/pushPrefsReachable.test.ts) walks type → row builder → client → cadence sync → **a control wired into a screen**: *a server column the user cannot set is a column that stays NULL forever*. Seven mutants — **two survived first run, both holes in the guards not the code**: `cadence-clobbers-hour` (a grep cannot tell `{dayReviewHour: h}` from a conditional spread, and those differ by whether opening Profile on a laptop clears the hour set on a phone → rule extracted to `cadenceHourPatch`) and `hour-never-leaves-the-device` (the guard sliced to end-of-file and matched a function 200 lines below — a guard against an omission that could not see the omission). Tests 939→961. **Ships dark** — VAPID unset.
- **Reachable, or deleted (`.195`):** the `.190`–`.194` wave computed the right things and shipped them where nobody could get to them — **three recurrences of this repo's own defect class in one night**. `HomePage` routes `i-day`/`basic` to [`HomeTodayLean`](src/page-components/HomeTodayLean.tsx) and `phase ?? 'basic'` makes that the default, so the Day in Review card, its behavior impacts and the push opt-in nested inside it **reached zero new athletes**; `setJournalFeel` had no callers while History rendered `· Feel N/5` (`.184`, live again); and quarter-hour rounding plus `BEHAVIORS.find` each had **three** definitions while the shared ones sat unused (`.178`) — the inline rounding copy got the midnight wrap wrong, which is exactly the bug a second definition exists to produce. Deleted the dead nav subsystem (`EXTENDED_NAV_SECTIONS`, zero non-test importers), the `dayReviewLines` export, both duplicate roundings and lookups, and the comment claiming links "stay in the header menu" — there is no header menu, and that comment is why **`/benchmarks` was reachable from nothing but a typed URL**. New pure [`dayReviewMount.ts`](src/lib/today/dayReviewMount.ts) is one mount decision for both shells and hoists the hour gate out of the card's effect (09:00 was downloading seven modules to render `null`). `setJournalFeel` wired end to end through `ActiveEmptyState` — the live post-completion path. **The guards are the deliverable**, in the `surfaceReality.test.ts` source-reading idiom: [`reachability.test.ts`](src/lib/reachability.test.ts) (every export has a caller · a rendered field has a writer targeting the row completion just saved · a card is rendered in *both* shells · one concept, one definition) and [`navTruth.test.ts`](src/lib/navTruth.test.ts) (every enabled screen is in the bar, the rail, quiet links, or a reasoned `NAV_EXEMPT` — `/benchmarks` and `/programs` now founder decisions on the record, not rot). Twelve mutants killed — **`card-missing-from-lean-shell` survived first run**: the guard counted name occurrences and the deleted JSX left the name inside `dynamic(() => import('…/TodayDayReviewCard'))`, an importing-is-not-using hole in the test written to catch importing-is-not-using. Fifth surviving mutant of the wave; fifth to expose a hole in a *test*, not the code. **`NEXT_PUBLIC_SURFACES=wedge` parks `/mind`, taking the behavior journal and sleep consistency offline entirely** — a deployment consequence, not debt, recorded so nobody ships wedge-only and wonders where `.190`/`.191` went. LOG rotated (`.180`–`.188`, 35KB against a 20KB rule). Tests 923→939.
- **The doorbell, not the letter (`.194`) — behavior-journal wave complete:** the opt-in evening day-review push. **The push carries no numbers**, because the row it is sent from carries none: [`20260801_day_review_push.sql`](supabase/migrations/20260801_day_review_push.sql) adds only a chosen hour (a preference int of the same class as `days_per_week`) and its own marker column. The `20260730` migration already refused a three-value load zone as "reconstructable training history through the back door"; a caffeine count or sleep figure is categorically worse. The server knows only *when* to ring — the review is composed on the device on open. Pure [`dayReviewNudge.ts`](src/lib/dayReviewNudge.ts): fires only at the athlete's chosen hour (18–22, strictly opt-in), once per local day, **never on a night wind-down already spoke** — and `windDownDue` carries the mirror clause, so one-push-per-evening holds from both sides (push-fatigue research is blunt: ~32% uninstall past six notifications, one weekly push already costs ~10%). Own cron route + hourly workflow at :11 (offset from wind-down's :07), own `mw-day-review` tag so neither replaces the other's unopened notification. Seven mutants killed incl. both precedence directions and `behaviors-on-push-row` (a column allowlist test proving no behavior-shaped field can serialize). **Ships dark** — VAPID unset → nothing sends. Tests 906→923.
- **Media system:** Google Flow · Scout mascot — [docs/MEDIA_SYSTEM.md](docs/MEDIA_SYSTEM.md).
- **Ops:** prod ships via **Vercel Deploy Hook + GitHub webhook** (unmetered, no Actions) — [docs/VERCEL_DEPLOY_CHECKLIST.md](docs/VERCEL_DEPLOY_CHECKLIST.md) §1.1; `deploy-production` is now **manual-only** fallback. ~~**Actions is currently blocked**~~ — **cleared; Actions works again as of 2026-07-29**, verified by 8 green checks on PR #121 (CodeQL, gitleaks, aikido, build-and-test). The PR gate is live, so it no longer needs `npm run gate` as a substitute — though the local gate stays faster for pre-push. **Secrets program** + pre-public scrub shipped — [docs/SECRETS.md](docs/SECRETS.md); OSS public-ready (AGPL + CoC) — founder flips GitHub Public — [docs/OPEN_SOURCE.md](docs/OPEN_SOURCE.md). Promote **`.157`** — now on `master`; keep Supabase Site URL on www.
- Agents **must** ship wedge habit-loop + free acquisition. Refuse new pillars / America / locale farms / F5.
- **Founder:** Accept B on Android + phone excellence → invites → YC F26. **Wire the Deploy Hook webhook** ([checklist §1.1](docs/VERCEL_DEPLOY_CHECKLIST.md)) then promote `.157`; clear Actions billing to restore the PR gate. **Before any list email: set `MAIL_POSTAL_ADDRESS`** (CAN-SPAM footer — confirm the Bizee TX registered-agent address is publishable as a business address, else PO box/CMRA; same address closes the DMCA agent row) — [LEGAL_SAFETY.md](docs/LEGAL_SAFETY.md) §3. Before Public: `npm run secrets:scan`, enable GitHub secret scanning + push protection.

---

## Read next

[AGENTS.md](AGENTS.md) (conventions · glossary · commands) → [INDEX.md](INDEX.md) (task → doc routing · stale paths §4) → [ORCHESTRATION.md](ORCHESTRATION.md) (horizons · gates · departments) → the folder `INDEX.md` where you'll work.

---

## Trap terms (full glossary: AGENTS.md)

| Term | Means |
|------|-------|
| Mission Coach | AI plan engine — `src/lib/coach/`, `/coach` (≠ `/coaching` human-lead form) |
| Today | Route `/log` (`HomePage.tsx`), nav label "Today" |
| Train | Route `/active` — the logger |
| Fuel | Route `/nutrition` |
| Journey phase 0–3 | UX arc ([docs/JOURNEY.md](docs/JOURNEY.md)) ≠ build phases A–I ([docs/PLAN.md](docs/PLAN.md)) ≠ PFT G1–G8 |
| Horizon 0–3 | What may be built now — [ORCHESTRATION.md](ORCHESTRATION.md) |
| Wedge | Train + Mission Coach — the go-to-market story |
| Super Bundle | The one premium sub: $11.99/mo · $59/yr founders · $149 lifetime |
| PRIVATE_MODE | Site gate — only the founder flips it |
| mw-core | [packages/mw-core](packages/mw-core) — pure TS shared coach/workout logic |

---

## Commands

`npm run typecheck` · `npm test` · `npm run build` · `npm run lint` — full list in AGENTS.md §Commands.
Android: `cd apps/android && ./gradlew :app:assembleDebug`.

---

## Hard rules

1. **Horizon rule** — Horizon W: build wedge excellence (Train/Today/Victory/Coach). No new pillars/locales/America/F5 without explicit founder override. ≥10 beta only after excellence sign-off.
2. **The free logger is never gated. Ever.**
3. Agents never flip `PRIVATE_MODE`, never invent traction numbers, never mark founder tasks done.
4. Do not open stale/deleted paths — [INDEX.md](INDEX.md) §4.
5. Docs match reality: every ship updates [LOG.md](LOG.md) + this file's `## Now` (+ build label).
