# CONTEXT.md — boot file (read first, every tool)

One screen of truth for any AI tool or human joining cold. Read this, then [AGENTS.md](AGENTS.md), then [INDEX.md](INDEX.md). Keep `## Now` current: update it on every ship, in the same commit as the [LOG.md](LOG.md) entry.

---

## What this is

**Mission Winning** · www.missionwinning.com · "Train Anywhere. Win Daily."

> Adaptive AI coaching for train-anywhere athletes — free offline logging (no account), weekly plans from logs alone (no wearable). Super Bundle adds Coach depth and the other pillars — it never gates the logger.

- Six pillars — Train · Fuel · Move · Mind · Track · Learn — unified by the Mission Score. Constitution: [vision.md](vision.md). Pitch the **Train + Mission Coach wedge**, never "everything app" ([docs/YC_THESIS.md](docs/YC_THESIS.md)). Evidence thesis (structured exercise vs vague advice — not a depression product): [docs/EXERCISE_AS_MEDICINE.md](docs/EXERCISE_AS_MEDICINE.md). Crypto is a **payment rail** (Lifetime USDC), not the product ([docs/CRYPTO_RAILS_THESIS.md](docs/CRYPTO_RAILS_THESIS.md)).
- Surfaces: Next.js 16 PWA (repo root) · native Android Compose ([apps/android](apps/android), v1.24.1) · iOS deferred ([docs/IOS_PLAYBOOK.md](docs/IOS_PLAYBOOK.md)) · "Beyond the Basics" guidebook (`/guide` + magazine PDF).
- Solo founder + AI agents. Founder owns users, money, legal, secrets, `PRIVATE_MODE`. Agents own code, tests, perf, docs — inside horizon gates.

---

## Now (2026-07-30 · web `2026.07-unified.192` · Android `1.24.1`)

> The ONLY "where we are" block in the repo — [ORCHESTRATION.md](ORCHESTRATION.md) points here.

- **Horizon W — wedge excellence.** Web craft 1–6 + **Fuel estimate accuracy** (edit-before-log, better NL match, honest photo sources).
- **Logger reliability (`.123`):** fixed `/active` **Start Workout stuck disabled** (zustand ran `onRehydrateStorage` inside `create()` → swallowed TDZ error → `hasHydrated` never true). Web now on **sync v2** (`clientId`/`revision`/tombstones) behind a **durable outbox** — [src/lib/sync/INDEX.md](src/lib/sync/INDEX.md); all device writes go through [src/lib/storage/INDEX.md](src/lib/storage/INDEX.md). Hero + offline e2e now gate every PR.
- **Free-first beta (~4 weeks):** LLC + EIN pending — **no Bundle UI** + **full depth unlocked** + **full More nav** (journey train-only deferred) ([docs/FREE_BETA.md](docs/FREE_BETA.md)).
- **Surface parking (`.124`):** `NEXT_PUBLIC_SURFACES` ([src/lib/surface.ts](src/lib/surface.ts), [docs/ENV.md](docs/ENV.md)) parks non-wedge surfaces — **america · school · wearables · leaderboard · cryptoRails · paypal off by default**; six pillars stay on. `=wedge` parks the secondary pillars too (founder call). Parked = out of nav, 404 in `proxy.ts`, out of sitemap. Nothing deleted. Hero e2e passes in full wedge mode, so **the free logger is provably never gated**.
- **First 90 seconds (`.125`):** cold visitor → logged set is now a **budget test** (`tests/e2e/first-90.spec.ts`, 6 taps, no interstitial, one primary CTA, every logger control ≥44px — the ± steppers were 36px). Re-entry after a gap is calm and smaller, not a broken streak ([src/lib/reentry.ts](src/lib/reentry.ts)). `first_set_logged` carries `secondsFromStart`.
- **Homepage rebuild (`.126`):** `/` was the only page ignoring the briefing type system in `src/index.css`, so its H1 rendered in **Inter not Barlow Condensed**. Now on `.display-hero` / `.eyebrow`, restructured as the product loop (log → adapt → anywhere → free → start). Signature: [`LogToPlanHero`](src/components/landing/LogToPlanHero.tsx) runs the **real** `suggestNextSetTarget` engine — no hardcoded dashboard numbers. `CoachAdaptDemo` is visitor-driven, not an auto-carousel. Audience page at `/compare/test-prep`.
- **Quality pass (`.127`):** **`npm run a11y` green (10/10, was 8 failing + flaky)** — emerald split into `--primary` (accent on navy, 7.08:1) and `--primary-fill` (white text, 5.38:1) since no single value serves both; `--muted-foreground` 58→62%. Every declared `OutboxKind` now has a handler (`fuel.plan` removed — it writes to device storage, not a cloud). Cross-device **edits and deletes propagate** via the `updated_at` cursor. **`npm run gate`** runs the whole CI gate locally while Actions is blocked. Storage ratchet 59→53.
- **Storage ratchet empty (`.128`):** all 53 remaining files migrated to [safeStorage](src/lib/storage/INDEX.md) — `LEGACY_DIRECT_STORAGE` **deleted** from `eslint.config.js`, so a bare `localStorage` call is now a plain lint error with no allowlist to join (only `src/lib/backup.ts` stays exempt, since it must prefix-scan `mw_*` at runtime). ~200 call sites; ~40 hand-rolled `try/catch` and ~35 SSR guards **removed** rather than added, because `safeStorage` is SSR-safe and never throws. Every key resolves through `STORAGE_KEYS`.
- **Public site is one site (`.129`):** **94 of 219 advertised exercise URLs were 404ing** — `generateStaticParams` read the exercise catalog without awaiting the lazy extended modules, so only ~126 prerendered while `/exercises` linked to all of them. Sitemap now 273 URLs / 0 non-200 (was 95 non-200), with a `@gate` test that fetches every one. Then the UI: [`PublicPageShell`](src/components/public/PublicPageShell.tsx) replaces `PublicSeoHeader` across the ~250 SEO URLs whose `h1` rendered in **Inter** — the same defect `.126` fixed on `/` alone. Briefing type, one measure for header + body, one emerald CTA above the fold, full footer with legal + medical disclaimer (previously on 3 of ~270 public URLs). New [`PublicNavMenu`](src/components/public/PublicNavMenu.tsx) on Radix Dialog: at 390px the nav had **no links at all**. Global `:focus-visible` — there was none, and axe does not test it, which is why a11y was green while WCAG 2.4.7 failed. `scripts/check-display-type.mjs` stops `text-*` utilities nullifying `.display-*` clamps (10 sites did). Gates: `@gate` 16→25, `@a11y` 10→20. **`viewportFit: 'cover'` deliberately deferred** — no sticky header is inset-guarded, so it would create the bug it looks like it fixes.
- **Modernist rebrand (`.130`–`.131`):** founder-commissioned full rebrand (design handoff 2026-07-25 — paper/ink/**one red**, Archivo only, radius 0, 2px rules, light-only; override recorded in [docs/DESIGN_ORCHESTRATION.md](docs/DESIGN_ORCHESTRATION.md) wave D5). `.130` restyled `/private` (the whole public site while gated). `.131` is the **global token swap**: one `:root` flip in [src/index.css](src/index.css), Archivo only, whole Tailwind radius scale → 0, glows/gradients/shadows retired, ink-square mark + favicon/PWA icons + OG re-ink, form-guide SVGs re-inked (repo still had the navy set), DESIGN_SYSTEM/brand-guidelines rewritten. **Red is three tokens** (`--accent-poster` #ec3013 fills+chrome · `--primary-fill` #dd2b0f button fills · `--primary` #ae1800 small text) — paper inverts the `.127` contrast math; small red text is never poster red. **Android sync paused** (founder call) — Android rebrands after web; `check-token-sync` pins web values, motion still enforced. Beta testers see restyled-but-not-recut app screens until Phase 3. `.132` recut the shared public shell; `.133` the landing (flat hero, checkable stat row, numbered sections, red poster close). `.134` added the public /calculators/{1rm,tdee,strength-standards} SEO routes (new strengthStandards ladder lib, registered in sitemap+gate+smoke). `.135` recut the SEO furniture (square filter tags, 2px compare table w/ tinted MW column, guide TOC case). `.136` opened Phase 3 on the app: nav active = 2px red top rule, all blur+shadow gone (28→0), pills squared, coach week strip honest. `.137` re-inked the guidebook cover (PDF rebuilt, 27pp); `.138` shipped the three Modernist HTML emails + the first beta-invite sender. **Rebrand build complete and MERGED** (#89–97, 2026-07-26); founder owns `MAIL_POSTAL_ADDRESS`, photography, wordmark, and the `PRIVATE_MODE` flip.
- **Modernist primitives (`.139`):** Phase A of the **second** handoff (`design_handoff_missionwinning_modernist` — the 13 signed-in screens the first one left). Its tokens already matched `.131`, so this is the primitives layer: both ramps (`--neutral-*`, `--accent-*`, with 100/600/700 **aliasing** the semantic roles so they cannot drift), **`ProgressRing` deleted** in favour of [`ScoreNumeral`](src/components/ui/ScoreNumeral.tsx) + [`MeterBar`](src/components/ui/MeterBar.tsx), and the glow/texture classes gone for `card-boss` · `card-section` · `is-active-row` · `seg`. Found and fixed a **pre-existing** WCAG 1.4.1 failure — `npm run a11y` was really **18/20**, because `text-primary hover:underline` (56 sites) distinguishes links by hue alone at rest (1.27:1). Now 20/20. **Poster red is not a ramp step** — it sits between 500 and 600; don't "simplify" it to `accent-500`.
- **App shell grouped (`.140`):** rail + mobile tabs both from **`railGroupsForNav()`** ([src/lib/navConfig.ts](src/lib/navConfig.ts)) — the 13 handoff screens as **Mission · Pillars · Toolkit**, declared as hrefs so label/icon keep one definition and parked surfaces still filter out. Rail scrolls (icon-only at 72px, 2px rule instead of labels); tabs scroll horizontally because 13 at `flex-1` is 29px each on a 375px phone. OPEN BETA tag gated on `isFreeBeta()`. **Two handoff values overridden for contrast** — group labels `neutral-500` and inactive tabs `neutral-600` are 10px on paper at ~2.4:1 and 3.84:1, both now `muted-foreground`; a11y caught it at 13/20. Coach keeps its live label "AI weekly plan" — the mock's "Coach" is shorthand, and the handoff says copy is unchanged.
- **Today recut (`.141`):** first screen of the second handoff. The boss panel on Today is the **red field** — [`JourneyHero`](src/components/journey/JourneyHero.tsx) is now `.poster-field` with its `.primary-action` inverting to paper, so `first-90`'s one-CTA count holds. The field is **`--primary` #ae1800, not poster #ec3013, and that is forced**: it carries an 11px kicker and 14px sub-line needing 4.5:1, and *nothing* on #ec3013 reaches 4.5:1 — pure white is 4.19. `MetricsRow` absorbed the Mission Score into **one band of four** (red numeral leads; 2 cols on a phone). The streak was rendering **twice** — dashboard copy removed, meta row keeps it. Deltas are muted ink in both directions, which is the handoff's own answer.
- **Logger recut (`.142`):** Train. Found **the last pre-rebrand colour in the app** — [`src/lib/workout/setKind.ts`](src/lib/workout/setKind.ts) still held amber/rose/violet/emerald on dark-theme `*-950` grounds, so completed set rows rendered a green wash on paper; it survived `.131` because it lives in `lib/`, not `components/`. Set kind is a **tag** now, not a row tint. Live row = `is-active-row`; PR = **honor tier** (accent-800 + ★). Rest dock is the **ink panel** (neutral-900, 72px clock, accent-400 meter). Plates promoted from the overflow menu to the header. Superset edge is red, not blue. Plate dialog gained the handoff's quick-target chips. **`logger-depth` matches `/^log$/i`, so the mock's "Log set" label was NOT adopted.**
- **Coach recut (`.143`):** ADAPTED banner is accent-100 behind a red edge; sessions are a **2-col grid** with today marked by a red top rule + the screen's one elevation (`isToday` threaded from `CoachPage`, which already had `todayOffset`). Amber `--status-warn` borders and warning text collapse to the one red. **The mock's 'missed → Thu' annotation still does not ship** — the plan engine records no reshape target, so it would be invented.
- **History recut (`.144`):** found **three chart files that were never rebranded** — Recharts takes colour as *props*, not classes, so emerald/amber literals survived every class-name grep, and [`Benchmarks1RMChart`](src/components/benchmarks/Benchmarks1RMChart.tsx) was still **fully dark theme** (navy tooltip, light-grey axis text ~2:1 on paper). All tokenised. Muscle heatmap is now one hue deepening (accent-100→400) rather than jumping to amber at the top step. Two 1RM series distinguished by accent vs ink, not a second hue.
- **Photo slots (`.145`):** [`GrayscalePhoto`](src/components/marketing/GrayscalePhoto.tsx) owns ratio + desaturation so layout does not shift when real photos land — filling one is a file swap (`/public/photo/<name>.{avif,webp}` + `base`). Ships as **named placeholders**, not stand-in images: the captions are the shot brief. Placed in landing §03, **not the hero** — that slot is `LogToPlanHero` running the real engine, which beats a photo of someone else training. **Wedge-four redesign complete (A–G, `.139`–`.145`); the remaining 9 screens are the next tranche, gated on founder review.**
- **Fuel + runners (`.146`):** Fuel was the last big pocket (41 hits, 17 files) — radius/hairlines/tints/`status-warn` all systematised; week bars are poster·fill·neutral because amber for "over target" implied a severity the app does not assign. **Cherry-picked the spawned FAB-overlap fix `b5f53548`** so this restyles the fixed layout, and its spec still guards it. **[`GuidedStepPlayer`](src/components/session/GuidedStepPlayer.tsx) goes ink while running** (shared by Move + Mind — one change, both screens): 44px countdown, step dots, accent-400 meter, **red `.poster-field` completion banner**. Breathing anchor is now a **square**, ink — the last round object in the app. New `onInk`/`onInkSolid` Button variants + `MeterBar tone="ink"`.
- **Track · Learn · Library (`.147`):** 19 files, same patterns. Notable: `bg-black/20` scrims in the two locked previews were a dark-theme device (a hole punched in the page to sit a lock on) — now `bg-card`, so a locked preview reads as a quiet surface.
- **Assess · Builder (`.148`):** **the PAR-Q result was a traffic light** (danger/warn/ok borders) — three ranks now come from one hue (high = filled red poster, moderate = red edge, low = plain rule). A health screen should not render "low risk" in green when the instrument exists to send some people to a doctor. `MilitaryReadinessSection` was amber-on-amber on a gradient over `bg-black/20` rows; now flat, ink, one red. Last gradients gone.
- **You · Welcome + app-wide finish (`.149`):** recruiter badge was the last real **brass** (→ honor tier). Then a whole-app pass over 38 files. Key finding: **`rounded-sm`…`3xl` are no-ops** (the Tailwind scale maps to `--radius: 0`) — but **`rounded-full` is `9999px` and still drew 21 real circles**. Those are gone; the dead `rounded-*` classes are harmless and left. Cleared every remaining brass, `bg-black/*`/`bg-white/*` scrim, `status-info` panel and the last 4 gradients. `MacroCalculator`'s bar was sky/amber/rose — now one accent ramp. **Final audit: 0 off-palette, 0 gradients, 0 circles, 0 scrims, 0 live brass, 0 stray `hsl()`. The Modernist redesign is complete (A–K, `.139`–`.149`).**
- **Mobile app redesign — primitives (`.150`):** Phase 0 of the **third** handoff (`design_handoff_mobile_app`, the signed-in PWA at 390×844 — [docs/DESIGN_ORCHESTRATION.md](docs/DESIGN_ORCHESTRATION.md) wave D6). The handoff treats [`AdaptiveOverlay`](src/components/ui/AdaptiveOverlay.tsx) as the ready sheet shell; it was still pre-rebrand (1px header rule at 30%, `font-medium` eyebrow, 18px/600 title, alpha drag pill) and had **no footer slot**, which the sheet anatomy needs for its pinned action — so it gains one here rather than seven sheets hand-rolling it. `EmptyState`/`ErrorState` were the two dashed-rounded-centred surfaces on a flush-left system; both are two 2px rules + an ink square mark now. **`Skeleton` bars were invisible** (`bg-muted/50` over `bg-muted` = `#eae9e9` at half alpha on `#eae9e9`) — only the pulse revealed them, so the animation carried the information and `prefers-reduced-motion` deleted it; `neutral-300`, no pulse. The ~190 `font-medium` / ~107 alpha-border sites are **deliberately not swept here** — the logged ones sit on lines phases 2–3 rewrite outright. Correction: `ErrorState` is not "the last dashed surface", 8 remain.
- **Five tabs, not thirteen (`.151`):** the mobile bar flattened all 13 rail screens onto a horizontal scroller — **884px of track in a 390px window**, seven destinations off-screen with no affordance saying so, including the only route to sign-in and settings. Now **Today · Train · Coach · Fuel · More** at `flex-1`, 56px, no scroll, with a **More sheet** ([`MoreSheet`](src/components/layout/MoreSheet.tsx)) carrying the other nine grouped Mission/Pillars/Toolkit and a **live figure per row** where an honest one exists. `PRIMARY_NAV` is deliberately **not** rewritten — the rail resolves `/profile` through it, so the bar is a subset (`MOBILE_TAB_HREFS`) and More is a button, not a route. The header's inline nav panel is **gone**: one sheet answers "where is everything" at every width. Coach's tab reads `Coach` (the live label `AI weekly plan` renders as `AI WEEKLY …` at 78px; narrow `TAB_LABEL_OVERRIDES`, same device as `RAIL_LABEL_OVERRIDES`). New `tests/e2e/mobile-nav.spec.ts` measures the track **geometrically at 360px** and proves ≤2 taps to all 13. Gate **26→29**.
- **Today docks, nothing floats (`.152`):** `.today-shell` was double-padding the most important screen (`AppLayout` `px-4` **plus** `max(1rem,…)` = 32px inset, 326px usable). [`JourneyHero`](src/components/journey/JourneyHero.tsx) is now a **dock** — the button label *is* the title, description clamped to one line. New [`ScreenDock`](src/components/layout/ScreenDock.tsx): screens portal their docked field into a flex sibling of `main`, because **`position: fixed` does not work inside a screen** (`.stagger-enter` animates `transform` with `both`, and a transformed ancestor becomes the containing block) and because a flex sibling **reserves its own height**. `MobileNav` is **in flow, not fixed** — `main`'s `pb-[56px]` guess is gone, which is the real fix for **drift 10**: `RestTimerBar` covered the set row it counted down for. Muscle freshness: sideways chips in a `<details>` → ruled rows with an 8px meter (four-day boundary is `muscleFreshnessRows`' own). Today's "More" → **"Today details"** (three disclosures shared that word; `.151` added a tab called More). The mock's `~24 min` is **not** shipped — nothing estimates session duration, so it would be invented.
- **One console, two states (`.153`):** the logger's **Log button was off-screen mid-set** — every planned set rendered its own control band (~340px of steppers inside 326px), wrapped in `overflow-x-auto`, once per set. [`LogConsole`](src/components/workout/LogConsole.tsx) takes entry: ink panel, `Set n of m`, `Last time …`, 48×52px steppers, full-width poster-red **Log set** at 52px (poster is right here — every line on it is display-grade). `SetLogRow` is a **read-only record**; rest **takes the same console over** instead of floating on the rows it describes. Set kind moved into the console; `Apply`/`Use last` retired (the field is already seeded from `suggestNextSetTarget`). Rest presets were **36px** and `first-90`'s sweep scopes `main`, which the dock is not in — both fixed, and **the sweep now includes `#screen-dock`**. **Test contract changed on purpose:** `activeLogSet` is "Log set" in every locale, so `/^log$/i` widened to `/^log( set)?$/i` in **three** specs (the plan named two; the gate found `hero-flows`) — an `aria-label="Log"` over visible "Log set" would break WCAG 2.5.3.
- **First run (`.154`):** I-Day's two `<select>`s → [`RuledRadioGroup`](src/components/ui/RuledRadioGroup.tsx) (52px ruled rows, poster edge + `accent-100` when selected, native radios underneath so keyboard semantics come free). **Step 3's primary action is Skip** — sign-in had the filled primary and skip was a ghost button in muted text, i.e. the last onboarding screen spending its emphasis arguing with "no account required to start". And the label was **wrong in 13 locales** (`Skip — go to Today`, but `finish()` routes to `/active`) → `Skip — start training`. **Day 0 showed a Readiness of 42 computed from no history**: `ScoreNumeral` always rendered `value={null}` as an em-dash but `BodyScores` are plain numbers, so nothing ever passed one. `MetricsRow` takes `sessions` now — em-dashes carry the condition that fills them, and **Recovery stays dashed until 3 sessions** (`computeBodyScores` needs more than one). Omit the prop and behaviour is unchanged, so demos are untouched.
- **Sheets + states (`.155`, partial):** `FormGuideSheet` folded onto `AdaptiveOverlay` — it was hand-rolled at `z-[60]`, **below** the shared shell's `z-[70]`, which is why a form guide could open *underneath* an open sheet and why the focus trap existed twice. Three overlay mechanisms → **two** (Radix `LibraryDetailSheet` is the last). **Session check-in scales were invisible** (`bg-muted` on a `bg-card` sheet = 1.01:1, four of five buttons absent until tapped) → one 2px-ruled strip; Save/Skip pinned at a full 52px each. **Offline is ink, never red**: [`OnlineStatusBanner`](src/components/layout/OnlineStatusBanner.tsx) was a centred 1px/60% strip wrapping to two lines at 390px; now one flush-left line under a 2px ink rule carrying the **outbox depth** — from the existing `outbox.subscribe()`, **not** the new storage key the handoff asks for (that would be a second source of truth). `app/error.tsx` matched to `not-found.tsx` (the `uppercase` was Barlow's), digest promoted to a labelled reference block. **Still open:** exercise picker → sheet, plate squares, Fuel meal strip + `estimateMealFromDescription` entry point, Adjust "Applied" panel, `LibraryDetailSheet` off Radix, offline "Waiting to sync" list, per-screen loading labels — each paired with a held pillar screen.
- **Phase 5 closed (`.156`):** the seven items `.155` left. **`estimateMealFromDescription` had no entry point** — unit-tested since the NL fuel-log wave and referenced by nothing but its own test file; Fuel's log sheet gains a **Describe** mode that fills the existing Custom fields (and says so honestly when the estimator returns null rather than inventing macros). Offline shows **what is waiting to sync** via new `outbox.listPending()` — `OfflineContent`'s comment deferred this as needing service-worker context, but `app/offline/page.tsx` is an ordinary same-origin route. **Exercise picker is a sheet** (was an inline `max-h-48` list competing with the session for height); `LibraryDetailSheet` off Radix — **all seven app sheets now share one shell**; Adjust names the resulting change; plate stack is 52px squares + `Achieved · exact`; loading states print their static labels. **Test contract:** the picker needs one extra tap to open, so `logger-depth`, `first-90` **and `hero-flows`** each gained one — `hero-flows` was also the spec `.153` missed, so the habit now is `grep -rn "search exercises" tests/e2e/` before moving a driven control.
- **Nine screens had no axe coverage (`.157`):** `GATED_ROUTES` covered 4 of the 13 signed-in screens; the other nine — the tranche the rebrand recut screen-by-screen — **had never been scanned**. Adding them found **4 failing serious/critical, all shipping**: `/track` + `/profile` form fields with **no labels** (`<Label>` without `htmlFor`, and a goals `<textarea>` with none at all — a card title is not a label); `/library` cards were a `div role="button"` **wrapping a real Button** (two keyboard stops, a button inside a button) with forty identical "View details" names; `/builder`'s Radix `SelectTrigger` had **no accessible name** (a placeholder is not one); `/profile` also carried `link-in-text-block` on `SignInPanel`'s consent links — the defect `.139` fixed in 56 places, missed here. All fixed at source. **a11y 20→29.** Same lesson as `.129`'s SEO tail: a suite is only as honest as its route list.
- **`e2e:visual` is the one dark gate.** Its three baselines were generated **2026-07-22, before the rebrand**, and depicted the old navy/emerald dark design — black grounds, emerald CTAs, rounded corners. Verified by opening one, not inferred from dates. **Deleted**, because a known-wrong baseline is worse than none: the first Linux run would have shown four huge diffs, and the reflex there is `--update-snapshots` without looking, which launders whatever renders that day into truth. `home-reduced.png` **never had a baseline at all**, so the homepage has been silently self-approving since the case was written. **First Linux CI run after billing clears must bootstrap all four** (`npx playwright test --grep @visual --update-snapshots`, then commit the artifact) — it cannot be done on macOS, the pixels will not match.
- **First phone review found two bugs no suite could (`.158`):** verdict was "everything else is good", and both defects were on `/coach`. (1) **Commander's intent never loaded for a signed-out visitor** — `/api/coach/plan-voice` 401'd whenever `hasAppAccess` was false (no gate cookie, no session), i.e. the product's own default state. The cost gate sat **in front of** the branch deciding whether cost is incurred: `fetchPlanVoice(ctx, false)` is pure local rules. App access now governs the LLM path only; rate limit and premium check untouched. (2) **`coachWhyCompound` printed raw under a real set** — `PlanSessionCard` used `defaultValue: ex.whyKey`, so an unresolved key became its own copy. That key exists nowhere in source or locales: it is baked into plans **persisted by an older build**, and plans regenerate weekly. Fixed with `i18n.exists()` — **i18next treats `defaultValue: ''` as absent and returns the key**, so the obvious fix silently does nothing. New `tests/e2e/coach-voice.spec.ts` guards both plus "no raw translation key renders" on `/coach`, `/log`, `/active`. Gate **31→34**. `plan-voice` previously had **no test at all**.
- **Three handoffs are three surfaces (`.159`):** the repo never wrote down that `design_handoff_missionwinning_modernist` is **the desktop app** and `design_handoff_mobile_app` is **the mobile app** — so D6's structural decisions were applied at every width and overwrote a desktop design that was already correct. The bundled screenshots are **examples, not targets**; each design is responsive in its own band and the only fixed number is the boundary. New [`useIsCompact()`](src/hooks/useIsCompact.ts) reads `md` (768px) — not a new line, the one `Sidebar`/`MobileNav` already used. Restored at `md+`: `ScreenDock` renders **in the flow** instead of portaling; `JourneyHero` gets its `.149` form back (kicker, full title, full description, inline-width action); `RestTimerBar` is a centred panel not a full-bleed bar; `.today-shell` keeps its horizontal padding; `MuscleFreshnessStrip` is chips again. **Branch in JS — never render both behind `md:hidden`**, because `first-90` counts hidden `.primary-action` nodes. Tokens, primitives, a11y and defect fixes from `.150`–`.158` stay everywhere: those are the system, not the mobile app. Verified at **1440 · 1024 · 834 · 768 · 767 · 390** — the pair either side of 768 proves the handover. Mobile is signed off, so **any mobile diff here would be a new bug**; there is none. **Deferred, founder's call:** the logger's per-set inline inputs at `md+` (forks `SetLogRow` 138→329 lines) and the header dropdown (the sidebar + More sheet already reach all 13 screens). **Not in scope:** Today's `max-w-lg` column cap — that predates this work and shipped in `.149`.
- **Desktop Today was half its width (`.160`):** founder compared the live screen against the handoff's own `screenshots/app-01-today.png` and it did not match. The handoff ships as **HTML, not just PNGs**, so the target is literal: `<div data-screen-label="Today" style="max-width:960px;padding:36px 44px 64px">`, and its `.btn` is `display:inline-flex`. Ours: Today added `max-w-lg mx-auto` **on top of** `AppLayout`'s responsive container, capping the screen at **512px → 480px of content at every desktop width**, with `.primary-action`'s `flex w-full` stretching the CTA across it. Both Today variants now defer to `AppLayout` at `md+` (`md:max-w-none`) and the desktop hero button takes `w-auto` — utilities layer after components, so it beats `w-full` without touching the shared class. Today at 1440 goes **480 → 832px** and the CTA **832 → 149px**. **The cap was not rebrand damage — `git log -L` puts it in `.106`**, ~40 builds before handoff 2, so `.141` restyled Today inside an already-wrong box and `.149`'s audit passed it because **width is not a token**. Measured at 1440 · 1024 · 768 · 767 · 390; compact is untouched (480/358). **Known remaining difference: the handoff left-aligns each screen in `main`; `AppLayout` centres it** — shell-wide, all 13 screens, held for founder call. Verification gotcha: a fresh profile redirects `/log` → `/welcome`, so any Today measurement must seed `mw_journey_state` **and real `workout-tracker-storage` history** (`detectBasicMilestones` re-derives the flag and demotes to `basic` without it) — see `tests/e2e/helpers/journey.ts`.
- **The logger is two designs now (`.161`):** desktop still *was* the mobile app — only 3 components consulted `useIsCompact`, so Train, the header and the rail all rendered mobile structure at 1440. **Read the mock's markup, not its screenshots** — the bundles ship HTML, so the target is literal. New [`SetLogTable`](src/components/workout/SetLogTable.tsx) is the handoff's `<table class="table" style="max-width:640px">`: `Set · Prev · kg · Reps`, active row `background:accent-100` + `box-shadow:inset 2px 0 0 accent` (that pair is already `.is-active-row`), inline 72/60px inputs and an inline `Log set`. `LogConsole` is **compact-only** — two places to type the same number is worse than either. **`RestTimerBar` corrected**: the mock is `position:sticky;bottom:0` full-bleed, one row, 30px clock; `.159` had restored `.149`'s centred `max-w-lg` panel, i.e. matched the old **code** rather than the **reference**, and `.149` had already drifted. Uses `md:contents` so one markup tree yields both a stacked phone dock and a desktop row. Header: the brand is a **button only on compact** (it opens the More sheet); the mock's header has no chevron and no menu, because the rail lists all 13. Rail gained the mock's `v… · Guide` footer (`lg:` only — it is 72px icon-only below that). **Regression caught in review, not by a suite:** killing the console on desktop silently removed *all* set-kind control, since the chips lived in it — restored as chips on the exercise card at `md+`. Verified 1440 · 768 · 767 · 390: table+2 inline inputs+empty dock above the line, no table + docked console below it. **Still mobile on desktop:** the add-exercise sheet (mock: an inline search input), first-run, and the nine unswept screens.
- **Desktop had zero test coverage (`.162`):** `playwright.config.ts` declared **one project, `mobile-chrome` at 390×844** — so the whole suite only ever rendered a phone, and `.159`–`.161` each shipped desktop structure under a green 34/34 that had never loaded a desktop viewport. That is the same pattern as `.129`'s SEO tail and `.157`'s a11y route list: **a green suite only proves something about what it was pointed at.** New `desktop-chrome` project at 1440×900, scoped by `testMatch` to [`surface-split.spec.ts`](tests/e2e/surface-split.spec.ts) — the other specs assert tab bar / docked console / sheets and would fail at 1440 *by design*, so they stay `testIgnore`d there. Four assertions: rail-not-tab-bar (and **no brand button** — it is the More handle, compact-only), Today past the 512px phone measure, the logger's **table + 2 inline inputs + empty dock**, and set kind reachable. Gate **34→38**. **Each test was falsified before being trusted** — forcing `useIsCompact` to `true` must fail them; the first run failed only 2 of 4, because the set-kind check passed for the wrong reason (the console renders its own chips, so the mobile surface satisfied it). Anchored outside `#screen-dock`, it fails properly: 3 of 4 now break under the probe, and the fourth guards a CSS regression the probe cannot reach.
- **Separation finished on the wedge (`.163`):** swept all eleven remaining rail screens at 1440 first — **every one already used the full 832px, no tab bar, no phone cap**, so the mobile bleed was never app-wide; it was concentrated in the shell, Today and Train, which `.159`–`.162` fixed. Two real items left, both now done. **Add exercise** is the handoff's inline `Add exercise — search 300+ movements` at the foot of the list; the compact trigger is `md:hidden`, so desktop has one entry point rather than a modal for a second exercise. `.156` moved it into a sheet because an inline `max-h-48` list competed for height — **a 390×844 problem, not a 1440 one**. Gotcha: it must sit **outside** the `exercises.length === 0` ternary, or it never mounts on the empty session where it is most needed. **First run** is chips at `md+` (`RuledRadioGroup`), stacked 52px ruled rows on compact — done in **CSS, not `useIsCompact`**, since the markup is identical and there is nothing to branch. Selected chip is `--primary-fill`, **not poster**: white on #ec3013 is 4.19:1 and the label is 14px, the same split `.141` made for Today's field. Measured: labels 132/148/147px on one row at 1440, 358px stacked at 390. Gate **38→39**.
- **The return channel had no seat for our own user (`.164`):** boss metric is week-4 retained weekly loggers, promise is **no account**, and every path back required one — at **five layers**: the reminders card sat inside `{email && …}`, `pushClient` bailed on `getUser()`, the schema had `user_id **not null**`, all four RLS policies were `auth.uid() = user_id`, and candidates came `from('profiles')`. Someone who logged six sessions signed-out then went quiet was unreachable forever. Now: `user_id` nullable + `device_id` + owner check constraint; new **`POST|DELETE /api/push/subscribe` on the service role** (anonymous rows are RLS-invisible by design — a public policy would let anyone enumerate devices); **signed in and signed out take the same path**, because two writers to one table is how a device gets subscribed twice or opted in once and reachable never. `getOrCreateDeviceId()` already existed — it is the identity, not a new one. **`Notification.requestPermission()` may never be called down a path that cannot use the result**; the old guard was unreachable only because the UI hid the toggle. **`streak-at-risk` deleted, not resoftened** — it fired on the consecutive-day premise `reentry.ts` rejects, so a 3x/week lifter was told a rest day cost them something; thresholds are now the athlete's own (`quietThresholdDays`). New [`reentryTone.ts`](src/lib/reentryTone.ts) makes the rule **executable** — the two lines that shipped (`Your N-day streak ends tonight`, `it's been N days`) are in the test verbatim as the regression, and reintroducing one fails the suite naming both rules. **`server-only` is why this drifted**: it made every word in `nudgeServer.ts` untestable, so copy + decision moved to [`nudgeCopy.ts`](src/lib/nudgeCopy.ts) (same split as `pushPayload.ts` out of `pushServer.ts`). Anonymous devices get **`comeback` only** — the server holds a last-session date and a cadence, never history, so the other kinds have nothing to compute from. Cron now runs anonymous push **before** the `RESEND_API_KEY` check: a missing email key was silencing exactly the athletes with no other channel. Nothing past **28 days** quiet. Tests **599→613**. Plan: [docs/RETURN_LOOP_PLAN.md](docs/RETURN_LOOP_PLAN.md). **Founder-owned and NOT done: apply the migration to Supabase**; push stays dark until the public flip by design.
- **The boss metric counted deleted workouts (`.165`):** `mw_week4_retention()` shipped in `20260720_referrals.sql`; `workout_logs.deleted_at` arrived **the next day** in sync v2 and nothing reconciled them. Deletes propagate cross-device (`.127`) so it is a live path, and **the error has no sign** — a deleted first session inflates the denominator, a deleted week-4 session inflates the numerator. Fixed in `20260728_week4_exclude_tombstones.sql`. The window had **three definitions, no two alike** ([POST_LAUNCH_CADENCE.md](docs/POST_LAUNCH_CADENCE.md) said "week 4" and "days 22–28"; SQL said 21–27) — SQL is right (first workout is day 0, blocks are 0-6/7-13/14-20/**21-27**), doc corrected to it. New [`supabase/checks/week4_retention_proof.sql`](supabase/checks/week4_retention_proof.sql) seeds all seven boundary cases, asserts 5 eligible / 2 retained, and **rolls back** — `week4-smoke` proves the RPC *answers*, this proves it answers *correctly*. **`npm run gate` was testing whatever sat on port 3000:** `next start` cannot bind an occupied port but `waitForServer()` only asks whether *something* replies, so a stale dev server ran all 39 `@gate` tests against a **pre-`.159` build** (34 failed; the same suite passed on a clean port minutes earlier). Red was the lucky direction — a compatible-but-wrong build goes green. **Fourth** suite-pointed-at-the-wrong-thing (`.129` · `.157` · `.162` · this); `gate.mjs` now refuses an occupied port **before** the build. Gate **39/39** clean (2.0 min) incl. the two offline/SW cases that skip unless built `PRIVATE_MODE=false`. **Founder-owned: both migrations unapplied.**
- **Parking parked the routes, not the work (`.166`):** two leaks past `NEXT_PUBLIC_SURFACES`. **`scheduleLeaderboardPush` ran on every completed workout** while `/leaderboard` 404s — and **queued the entire workout history**, which its own comment said it computed a snapshot to avoid ("queuing an entire workout history would bloat device storage", then `enqueue(…, { workoutHistory, savedCount })`); [sync/INDEX.md](src/lib/sync/INDEX.md) repeated the false claim. `userId` was only a passthrough, so the documented design was achievable all along. **`MilitaryReadinessSection` rendered on `/benchmarks`** — a secondary pillar, **on by default** — with no america guard, while its sibling `PresidentialFitnessSection` on the same page always had one; america is parked for **legal/channel** reasons, not tidiness. **Why nothing caught it:** `surface.test.ts` has 14 sound cases and every one checks `SURFACE_PATHS` **against itself** ("every surface declares at least one path" validates the table using the table). New [`surfaceReality.test.ts`](src/lib/surfaceReality.test.ts) checks the declaration against the app; **all three assertions falsified** before being trusted. **Fifth** suite pointed at its own assumptions (`.129` · `.157` · `.162` · `.165` · this). Tests **613→616**. **Founder call untouched:** whether the 73 parked files / ~6,200 lines / 20 of 63 API routes stay or go.
- **Four live pages had never been checked; two were broken (`.167`):** the a11y suite excluded `/benchmarks` as *"parked… 404s by design"* — but it is a **secondary** pillar (on unless `NEXT_PUBLIC_SURFACES=wedge`), a nav screen, serving **200**. Cross-checking the registry found **four of eight enabled surfaces had no axe coverage**: benchmarks, guidebook, programs, **calculators** — the last public even behind the private gate. Two were genuinely broken: **`/calculators` critical `label`** — every `<Label>` in the three calculator panels lacked `htmlFor` and every `<Input>` lacked `id`; Radix `Label` renders a bare `<label>` and does **not** self-associate, so seven inputs read as unlabelled boxes while *looking* labelled (which is why a rebrand editing these files never noticed). Fixed with `useId()`, not literals — `PlateCalculatorPanel` also renders inside `PlateCalculatorSheet`, so static ids would trade one violation for a duplicate-id one. **`/programs` serious `link-in-text-block`** — a `/learn` link in a muted paragraph was `text-primary hover:underline`, i.e. colour-only at rest; now underlined at rest. [`surfaceReality.test.ts`](src/lib/surfaceReality.test.ts) gains two assertions **derived from `SURFACE_PATHS`** so enabling a surface drags its a11y coverage along; both falsified. a11y **29→33**. **Sixth/seventh sighting of the pattern** (`.129` · `.157` · `.162` · `.165` · `.166` · this — plus one of my own build monitors grepping past a `tail -6`): a check aimed at a target nobody wrote down.
- **Hard rule 5 is now executable (`.168`):** *"every ship updates LOG.md + `## Now` + build label"* was prose, so it drifted — `chore/github-security-hardening` has a commit subject reading `(.164)` that touches **none** of `buildInfo.ts`/`CONTEXT.md`/`LOG.md`; its label is still `.163`, so it announces a version it never minted while this session minted `.164` independently. Beyond tidiness: `APP_BUILD_LABEL` is what `/profile` renders and what [`gate-smoke.ts`](scripts/gate-smoke.ts) asserts **against production**, so merging a stale label walks the deployed label backwards and the deploy smoke then confirms the wrong build shipped. New [`scripts/check-build-label.mjs`](scripts/check-build-label.mjs) — label must exceed **`origin/master`'s** (the remote; a stale local `master` would wave through a taken number) and be cited in both docs, **each in the form that doc actually uses** (CONTEXT full label, LOG heading `` (`.N`) ``; my first cut demanded the full label in both and failed on this branch — a check aimed at an unwritten convention, this session's own joke at my expense). Skips when HEAD is the base or the branch touches no app code. **Gate step 2**, before the 3-min build. Falsified three ways.
- **The gate that keeps us unlaunched was bypassable (`.169`):** pushing `.164`–`.168` surfaced **45 open Dependabot alerts** (24 high). Investigated, not bulk-bumped. **Next 16.2.7 → 16.2.12** closes all **9** `next` alerts — including **Middleware / Proxy bypass in App Router** (`>= 16.0.0, < 16.2.11`). The private gate **is** middleware ([proxy.ts](proxy.ts)), so that advisory defeats `PRIVATE_MODE` outright rather than degrading it; two SSRF advisories close with it. `^16.2.7` already permitted the fix — lockfile move, not a migration. **19 of 45 are one parked surface:** 18 `axios` + `bigint-buffer` all trace through `@phantom/react-sdk`, i.e. **cryptoRails** (`PARKED_BY_DEFAULT`, API 404s) — but [BundlePage.tsx:27](src/page-components/BundlePage.tsx) imports `PhantomLifetimeCheckout` **statically**, no surface guard, no `dynamic()`, so **508K** is built into the client. **Latent, not live** (verified, not assumed): `/bundle` **307s to `/log`** because `isFreeBeta()` defaults **true** — so it arms itself exactly when FREE_BETA goes off at the payments milestone. **Left unfixed: payments path, founder call.** Same shape as `.166` — parking parked the route, not the bundle. Remaining 17 are dev-scope (`brace-expansion`, `js-yaml`) or build-time (`postcss`, `@babel/core`). **Why nothing landed:** 7 `dependabot/*` branches open, none of them the Next bump; Actions is blocked so nothing auto-merges.
- **Told the athlete it saved when it hadn't (`.170`):** pre-launch readiness pass. The runbook's own header says *"the app has been 'almost ready' for months — **the code is not the bottleneck**"*, and [REDTEAM](docs/REDTEAM.md) **A5**'s falsifier (*"14 days… still no 10 beta users"*, dated 2026-07-02, now 27 days) has fired — so **this ships no features**. **The waitlist told people it saved when it hadn't:** `submitLead` never throws (reports via `{ ok }`), `UnlockButton` wrapped it in a dead `try/catch` and **discarded the return**, always rendering *"You're on the founders list."* — and `track('waitlist_joined')` fired anyway, so the lead was lost, the athlete misled, **and the metric counted a row that never existed**. Same bug found again in `PrivateTeaserClient` — the private gate, the *only* public capture point while `PRIVATE_MODE` is on. Both fixed. (`FeedbackPage` is a third instance but persists locally first; needs an outbox retry, flagged not bodged.) **The Fuel diary listed GPS runs as food** — `pillarLog` writes `"{pillar} win:"` rows at 0g/0kcal into `nutrition_entries` and `NutritionPage` mapped every cloud row in; predicate now lives **beside the writer** (`isNonFoodEntryName`), falsified. Three **ungated** wedge `window.alert()`s → `toast` (the other six are correctly `NODE_ENV === 'development'` — checked). Deleted the **fake 350ms delay** in the photo-meal heuristic. `/coaching`'s *"reply within 48 hours"* had no queue behind it. **Three guards ran locally and in no workflow** (`check-display-type` · `check-token-sync` · `check-build-label`) → now in `ci.yml` — **and `actions/checkout` shallow-clones, so the label check would have silently skipped**; proved it with a real `--depth 1` clone, added `fetch-depth: 0`, i.e. nearly shipped a decorative gate for the seventh time. **Six launch-doc contradictions reconciled to `LAUNCH_RUNBOOK` as single source** — worst: §4 said *"open Stripe as Individual"* citing `LLC_AND_PAYMENTS §1d`, **which is the override forbidding it**; and **five migrations were in no founder checklist**, two of them gating the return loop and the boss metric. Tests **616→622**.
- **The coach can do arithmetic now (`.171`):** Week 1 of the EIN-window sprint — two **pure engines**, no UI yet, powering web + Android + iOS alike. Brief was a WHOOP screenshot (*"15.3 strain against your 11.8 upper bound"*): that sentence is not AI, it is a **personal baseline plus the nerve to say a number** — WHOOP needs a strap, we need a logbook we already have, free and account-free. **[`coach/load.ts`](src/lib/coach/load.ts)** — Foster **session-RPE** (RPE × minutes), *not* an invented tonnage/density composite that would look scientific and mean nothing; **ACWR in EWMA form** (rolling averages suffer mathematical coupling and degrade badly on a 3×/week lifter's gappy history). **Three tested refusals:** ratio is **null** under 14 days (a plausible baseline from one week is the `.127` day-zero-Readiness-of-42 defect again); warmups are not load; bodyweight sessions have zero tonnage so tonnage-weighted RPE would have returned **`NaN`** — falls back to a plain mean. **Descriptive, not predictive** — ACWR is validated in team sports, contested, and never validated for recreational lifting; recorded in the module header so nobody phrases it as medicine ([LEGAL_SAFETY.md](docs/LEGAL_SAFETY.md) §3a). **[`coach/progress.ts`](src/lib/coach/progress.ts)** — e1RM series (Brzycki ≤10 reps, Epley above, capped at 12 because an e1RM from a set of 20 measures endurance), PRs, plateaus. One session = **one** exposure. A PR must clear `PR_EPSILON` or the app congratulates you for rounding. A plateau is **"the best is old"**, not "the last few were equal" — backing off after a PR is not stalling. **One test was wrong and the code was right:** 100 kg×5 estimates 113, 105 kg×3 estimates 111, so a session's *heaviest* set is not its strongest; the assertion encoded intuition, not e1RM. All new assertions falsified. Tests **622→650**.
- **Why the number moved, and a baseline that would have lied (`.172`):** rest of Week 1. **The check-in explains itself now** — `checkInReadinessDelta` has always moved readiness from sleep/soreness/energy/stress and **no component ever surfaced why**. (The plan said "add sleep + soreness"; reading first showed both already exist — the gap was the *sentence*, not the schema.) New `checkInReasons()` returns what fired **with the rating entered**, so the debrief says *"you rated sleep 2/5"* instead of silently shrinking a session. **Both derive from one rule table**, so number and explanation cannot disagree — same lesson as `isNonFoodEntryName` beside its writer. Middling day returns **no reasons** rather than inventing one. Behaviour provably unchanged: the pre-refactor implementation is inlined as an oracle and checked across **all 900 input combinations**. **The visual baseline that would have lied:** `ci-extended.yml` starts the server `PRIVATE_MODE=false` but sets **nothing** for `NEXT_PUBLIC_FREE_BETA` (defaults **true**), so `/bundle` **307s to `/log`** — bootstrapping would have written `bundle-reduced.png` **containing the Today page** and then passed against it forever, which is exactly the laundering that file's header warns about, arriving on the first run. Now checks the **landing URL** (not the flag, which drifts) and skips with a reason; proved `1 skipped` against a real server in the CI config. Other three routes confirmed 200. **Still needs a Linux runner — and the baselines must be looked at before committing.** Tests **650→656**.
- **The coach speaks (`.173`):** Week 2 — the `.171` engines become sentences in [`coach/debrief.ts`](src/lib/coach/debrief.ts) + [`SessionDebriefCard`](src/components/workout/SessionDebriefCard.tsx), wired into the victory sheet. **Verified in a browser against a seeded 10-week history**, which is how the one real bug was found. Live output after 140 kg×5 on a 100 kg base: *"700 kg moved, 1 working set in 76 min — about 63% above your recent average. **Best bench press weight: 140 kg, past 100.** … Your last week is running heavier than your month … You rated sleep 2/5 today, so the next session is scaled to match."* That is the WHOOP anatomy — number, own baseline, mechanism, one action, a question — **from a logbook**: no strap, no account, no API key. **Rules over computed values, not prose generation**, for two load-bearing reasons: it works offline and free, and it can be **tested for tone** — `reentryTone` now asserts across *every* debrief line, which you cannot do to a sentence a model generates tomorrow. LLM voice stays a Bundle-tier **rewrite**, never the source. **Nothing is said that is not known** (no baseline under 3 priors, no band under 14 days, no PR without a previous best; a good day gets **no invented concern**). **The band never predicts** — a test greps every band line for injury/risk/danger wording. **Reply chips are the point of the question**: tapping one writes today's check-in, so "Harder than expected" lowers the next dose — verified live (`energy: 2` written, `sleep: 2` preserved). **Falsification found dead code in my own work:** a filter keeping the session out of its own baseline changed **no test outcome** — `compareToBaseline` already guards it by date — and the replacement test *also* failed to discriminate until rebuilt at 3 priors and +15%, where the guard actually decides the sentence. **The browser found what tests could not:** first render read **"1 working sets"**; every unit test passed because none asserted grammar. Tests **656→668**. **The gate caught what the browser did not:** the debrief pushed the victory dialog past the viewport and `DialogContent` sets **no max height**, so "Back to Today" was *visible but unreachable* — a click timeout in hero e2e, not a render error. Fixed with `max-h-[90dvh] overflow-y-auto`; the dialog was already near that edge before this change.
- **One estimator, and Android sessions finally count (`.174`):** opening PR of the coach-loop sprint; both defects found by reading, not by anything failing. **Android sessions were invisible to every engine** — the mobile sync route stored Android's **flat** set array in the same `workout_logs.exercises` column web fills with **nested**, so `sessionLoad` read `ex.sets ?? []`, found nothing, and the session counted as **zero working sets** (gone from load/debrief/PRs, silently) while `toSyncPayload` **threw** on re-enqueue, taking the sync loop down. Broken both ways — Android's GET handed nested rows to an app expecting flat. New pure [`normalizeExercises.ts`](src/lib/sync/normalizeExercises.ts) converts at **both** boundaries: grouping on write, normalizing on read — which **heals every row already in the table**, no backfill. Android GET flattens out, so installed builds need no release. Numeric RPE maps **6→easy, 7–8→med, 9–10→hard** (conventional RPE↔RIR; brackets web's `{5,7,9}` anchors) and the test asserts an Android session and its web twin produce **identical** `sessionLoad`. Engines stay strict — tolerating two shapes in six call sites would hide the defect. **Four estimators became one:** `percentLoad` prescribed weights with **uncapped Epley and no tombstone check** (a deleted heavy session, or one 20-rep set, inflated working max for 28 days); `benchmarks` re-implemented Epley inline, **counted warmups and failure sets**, charted deleted sessions, and backs the **live PR chip** — so a 15-rep set could fire a "record" the debrief then refused to confirm. Displayed e1RMs move **0–3.4% down**; **prescribed weights barely move** (holds/deloads anchor to last weight, so the max cancels out) though the shown `%` may rise because the max got honest. `5×110` is no longer a PR over `8×100` — both estimate **124**, the same strength twice; Epley separated them by 1 kg and celebrated rounding. Three drifted copies of `roundToStep`/`workingSets` → [`setMath.ts`](src/lib/workout/setMath.ts). **Falsification found two untested fixes:** removing the benchmarks tombstone skip failed *nothing* (no `benchmarks.test.ts` existed) and neither did un-normalizing the sync read (`workoutMerge.test.ts` had 16 green cases, none covering it) — both now tested and falsified. Tests **671→690**.
- **The coach was being overruled in the gym (`.175`):** everything in `.171`–`.174` reported honestly *after* a session and **never steered one**. `getSetInput` used the plan's prescription as a **fallback**, behind `suggestNextSetTarget` — which assumes **8–12 reps for every athlete**, reads no RPE, and has no concept of a deload. With any history: a strength plan of **3×5 prefilled as 6**; an endurance plan said *13×40* while the same card hinted *8×42.5*, inches apart and unlabelled; on a stall the coach **deloaded ×0.9** while the logger said *add a rep*. Order is now **typed → prescription → suggestion (in the athlete's goal range) → last time → default**; the split is *plan prescribes, logger suggests where no plan exists*. A `prescribed` flag rides `PlanExercise` → store → `ActiveExerciseLog` via one shared [`planSessionToTemplates`](src/lib/coach/planSessionTemplates.ts) (extracted because two cards had byte-identical copies and a forgetful third would restore the bug). **`repRangeForGoal` is now exported and actually passed** — `suggestNextSetTarget` has always accepted `opts.repMin/repMax` and **no call site ever passed one**, so the whole app ran on the 8–12 default. The **"Next: N × W"** line on a prescribed exercise echoes the coach instead of competing. **Today shows the work, not just the name** — 3 exercises with sets×reps, load and why via shared [`PlanExerciseLine`](src/components/coach/PlanExerciseLine.tsx) (carries the `i18n.exists` guard), plus a descriptive band sentence and **silence when ratio is null**. **Falsification found the headline fix untestable** — removing the precedence line broke *nothing* because the logic sat inside a React component; extracted as pure `resolveSetInput` (5 cases; reversing the order now fails two), and new `engineConsistency.test.ts` is the first test comparing the two target engines at all. **Browser-verified on identical history:** prescribed prefills **5** / hints `5×100`; freestyle prefills **6** / hints `6×100`. Tests **690→699**.
- **One stall truth (`.178`):** the app held **two definitions of "stalled" that could disagree** — a private `stalled()` in `progression.ts` (three sessions matching on the *first working set*) and `exerciseProgress().plateaued` in `progress.ts` ("the best is old") with **no consumers at all**. The engine could deload on one while the module owning strength truth considered the athlete fine, and the richer signal was computed and discarded every time — the `.174`/`.175` self-disagreement, one layer down. [`stallSignal`](src/lib/coach/progress.ts) is now the only answer, returning **`stalled`** (three flat exposures → the existing deload, unchanged) and **`plateaued`** (best is four exposures old **even when no two sessions matched** — the case the equality rule could not see, where a lifter wobbling under an old record was told to add weight for a month). One deliberate widening: stall is judged on **e1RM within `PR_EPSILON`**, since per `.174` `5×110` and `8×100` both estimate **124**. The **bodyweight fallback is load-bearing, not a nicety** — `estimate1rm` is null at zero weight and above 12 reps, so `e1rmSeries` is *empty* for calisthenics and high-rep athletes, and a pure-e1RM rule would report `none` forever: not "no stall" but **invisible**. The debrief now names it **from the same function** (one line, worst exercise, `plateaued` only), so `engineConsistency` can assert sentence and prescription never disagree; variation is **offered, never applied**. Falsified: collapsing `plateaued` fails 3, deleting the bodyweight fallback fails 1, exact-equality instead of epsilon fails 1 — and **every pre-existing stall/deload test passes unmodified**, the migration contract. **Two of my own tests were wrong first:** the "same strength two ways" case proved nothing about epsilon (both estimate *exactly* 124, so the exact-equality mutant survived) and needed a real near-miss pair; and the debrief reported a plateau one exposure stale because `input.history` excludes the current session. Tests **733→745** (this branch adds 12; the 707 baseline in the commit message predates `.176`/`.177` merging).
- **The thank-you screen was the third lie (`.179`):** pre-launch honesty pass II. **FeedbackPage silently dropped notes on a dead network** — `.170` fixed two pages and flagged this one ("needs an outbox retry, not a polish fix"); it called `submitLead`, discarded `{ ok }`, and thanked the user over the beta's "why I almost quit" capture point. Now `feedback.submit` rides the durable outbox (per-entity keys — two notes both arrive, a double-tap collapses), with the verdict extracted pure as [`feedbackDeliveryDone`](src/lib/sync/feedbackSync.ts) because the first fix was unfalsifiable (the test registered its own handler); both failure directions now fail a test — swallowing `ok:false` re-creates the bug, retrying `localOnly` strands demo-env devices stuck. **The 508K Phantom import on `/bundle` is defused** — `dynamic({ssr:false})` behind `isSurfaceEnabled('cryptoRails')`, so the 19-alert SDK costs zero bytes while parked instead of arming itself at the payments flip. **The hardcoded `347` beta-spots counter is deleted**, key and all — a number nobody measured has no place in a product positioned on not inventing numbers. Runbook migration list now includes `20260730_wind_down_nudge` (#15) + the #129 CI apply path. Tests **745→750**.
- **The switching moment (`.180`):** CSV import on web — a **Strong or Hevy export becomes native history** via pure [`importCsv.ts`](src/lib/workout/importCsv.ts) + one Profile card. Hevy caps free history at 3 months (the export is how you leave); one file in and PRs/e1RM/load band light up against years of the athlete's own training. Real quote-aware CSV scanner (Hevy notes contain newlines — naive split fails 7 tests); Hevy RPE through the **same** `rpeNumberToCategory` Android sync uses, asserted the `.174` way (imported session ≡ native twin in `sessionLoad`); weights convert to display unit; unmatched names become slug ids so **matching is never why a set is lost**; `mergeImportedLogs` keys (minute, name, set count) so **re-import is a no-op and native history always wins**. The `.128` ratchet caught bare `localStorage` in the first version. Free forever, never gated — Android's importer contract, now on both surfaces. Tests **750→761**.
- **The percentages were prose (`.181`):** famous free programs with executable wave maths. 5/3/1 BBB + GZCLP promoted out of the premium file (new `free-*` ids — `strip-pro-programs.ts` deletes `pro-*` blocks; verified 52 kept/0 removed) + net-new r/Fitness Basic Beginner; deep catalog (nSuns/Smolov/Sheiko) stays paid — the Boostcamp split. Every catalog percentage previously lived as prose in `notes` because nothing materialized a percent into a weight. Now `WorkoutSetTemplate.loadPct` (per-set — 65/75/85 inside one session, which the exercise-level field cannot express; TM 90% baked into the authored numbers) resolves via pure [`materializeProgram.ts`](src/lib/workout/materializeProgram.ts) against `workingMaxFromHistory` **at `startWorkout`** — the one seam all callers share — so a saved cycle re-anchors to the athlete's current max each run, and materialized sets ride the `.175` `prescribed` path. Falsified: no-op materializer fails 2, fabricated max on empty history fails the `.127` identity assertion, missing `prescribed` stamp fails 2; plain templates byte-identical. **`.180`+`.181` is the launch demo: import Hevy history → start 5/3/1 → week 1 prescribed in your own weights.** Tests **761→766**.
- **The growth loop a privacy-first app is allowed to have (`.182`):** Hevy's moat is an in-app social feed; we cannot build that without breaking "workout content never leaves the device", so this is **share OUT** — a PNG rendered on-device, sent only by choice, carrying the `?ref=` code. Two cards from data that already existed: session (`WorkoutVictorySummary` + debrief `records`) and week (`WeeklyDebrief.train.prs` has counted PRs since `.173` with **nothing reading the number**). **The honesty rules follow the product onto the image** — no records → no line (a first-ever is `previous: null`, not a record broken; falsified), **zero PRs is silence** not "0 PRs!" (falsified), a one-day streak is not a streak. Contrast carries too: poster red is 3.78:1 on paper so it is the large kicker band only, PR line uses `--primary` — the `index.css` rule is not suspended because the surface is a canvas. Pure data builders + thin DOM painter following `progressPhotos.ts`'s `canvas.toBlob`; `navigator.canShare({files})` degrades to download+clipboard. **Then I rendered one and it was broken** — 4 stats + a PR line overprinted the footer (y=1280 vs 1270), in exactly the best case (streak *and* record); every test passed because all asserted *data*, not layout — the **eighth** sighting of "a green suite only proves what it was pointed at". Fixed with pure `computeCardLayout` + two-column stats; the invariant is now a test across 1–6 stats × with/without PR, and the fix was verified by rendering the real bundled source in a browser and **looking at the PNG**. Tests **766→774**.
- **The coach reads it out (`.183`):** Phase 0 of voice — `speechSynthesis` speaks the debrief lines the rules engine already composed and `reentryTone` already tested. **No key, no vendor, no network, no LLM in the path**; the `.173` doctrine holds exactly (rules are the source, speech is presentation). Came out of the Grok Voice research: xAI's voice stack is real (Voice Agent $0.05/min, TTS $4.20/M chars) and the repo already targets `grok-4.5` with a ZDR header check matching xAI's actual contract — but realtime voice is ~**$1.20/user/mo** against an $11.99 tier, needs per-user spend metering that **does not exist** (per-IP limits only, and FREE_BETA passes everyone through the premium gate), and streams gym audio to a third party from an app whose position is that workout content never leaves the device. On-device TTS is $0 and ships today. **Two refusals, both tested:** the closing question is never spoken (its answer is a *tap* that writes a check-in) and neither is the action line (a next step is to re-read, not half-remember mid-rack). Order is the debrief's own — reordering for the ear escapes the tone tests, so that variant fails; nothing sayable is **silence**, so invented filler fails; speaking everything fails 4. Every caller is a **button press** because iOS Safari drops `speak()` outside a user gesture. Verified in a real browser by intercepting the utterance. Tests **774→782**.
- **Notes stop vanishing (`.184`):** first PR of the Field Notes plan (Granola-of-training-journals — fragments steer, machine fills, entry stays the athlete's). Three write-only surfaces became real: **exercise notes now display in the History detail dialog** (written and synced since day one, `grep note` on HistoryPage previously returned zero hits — a note nobody can re-read is a note nobody writes); **both Android-boundary note drops fixed** (`flattenExercises` omitted `note` so web notes never reached the phone; `mobileSyncSetSchema` stripped it so Android set notes never reached the cloud — per-set notes fold into the exercise note newline-joined, exercise notes ride the first set); and **the debrief is persisted** — new device-only [`journalStore.ts`](src/lib/journal/journalStore.ts) (`mw_session_journal`, cap 200, backup-swept via the `mw_` prefix) saves what the coach said at finish, and History's detail shows it, so re-opening a session finally shows the entry it was given. **Device-only deliberately** — journal content is the data class users 1-star apps over (Day One); sync would be its own decision with its own encryption. Falsified: reverting either boundary fix fails the round-trip test; removing persistence fails 4. Tests **782→787**.
- **A digest that cannot lie (`.192`):** the evening **Day in Review** — the direct answer to the founder's WHOOP screenshot, built the opposite way. WHOOP's AI narrative is currently their most-attacked surface (their coach was caught fabricating a stat and discussing it as fact); every sentence in [`dayReview.ts`](src/lib/dayReview.ts) is a template slot filled from local state, so it **cannot hallucinate a number**, renders offline, and is readable code rather than a model's output. Shape: **one fact, one reason, one option** — and `null` when the day holds nothing true, because a card that appears nightly regardless is how no-input commentary becomes a joke. Refusals: shares the push **tone contract via `findToneViolations`** (one contract, not a second that can drift — no absence length, no streak loss; streak-protection in lifting means training when you shouldn't), no prescriptions/medical framing, no invented precision, and **no "strain floor"** (the screenshot's phrase is derived from a target we don't compute over a ratio the literature contests — `.177` already made our ACWR low end inert). Names both "strain" concepts in its header to prevent the `.178` drift, and deliberately never reads `getTrainingStreak` (localStorage-overridable → unfit for a factual sentence; proven by a purity test). Seven mutants killed — **`strain-floor-claim` survived first run** because no fixture produced a below-average session: load is RPE×duration and every fixture shared a duration, so the branch never rendered. Fixtures now vary minutes, with an explicit branch-coverage guard. Tests 877→891.
- **The number we will not print (`.191`):** the founder's screenshot said *"sleep debt is now 2h 7m 48s"* — we ship the honest version. **Sleep debt is not computable here** (no HealthKit web API on iOS, no PWA background sync; self-report disagrees with measured sleep by an hour+, so seconds are theatre) and a quantified physiological deficit is the shape of output FDA's 2026 guidance reads as a device claim. [`sleepConsistency.ts`](src/lib/sleepConsistency.ts) reports **regularity** instead — better evidenced than duration (UK Biobank n=60k), derivable from two taps, owned by no wearable company. Banded `steady|drifting|scattered` (median absolute deviation) + "N of your last 7 were later than that"; silent under 5 nights in a fortnight; **noon-anchored** so 23:45 and 00:15 read 30 min apart, not 23½ h. Killed: `under-min-nights-speaks`, `midnight-wrap`, `seconds-precision`, `sleep-debt-number`, `sri-score-leaks`, `missing-night-counts-as-zero`, `prescriptive-line` — **that last one survived first run** because the tone test read only the short form of the sentence; it now asserts over both shapes (a copy constitution that reads half the copy is not one). Tests 862→877.
- **Twelve questions, each with its receipts (`.190`):** first PR of the behavior-journal wave (WHOOP-Journal shape, our doctrine). Pure [`behaviors.ts`](src/lib/behaviors.ts) — **12 behaviors, not 300** (WHOOP's own advice is ≤10; catalog paralysis + a 5-yes/5-no bar is what produces their "everything shows a negative impact" complaint), each with an evidence **tier A/B/C and an openable receipt**, incl. a C on late meals that says the research is unsettled. Registry carries **one pre-registered outcome per behavior** (the multiple-comparisons kill switch — you cannot fish for the metric that moved) and **a declared lag** (alcohol tonight is a claim about tomorrow's session). **The storage was the dangerous half**: `normalizeCheckIn` + `upsertTodayPartial` are whitelist reconstructions — a field added to one and not the other dies with no error (the real scenario: log in the morning, tap a victory reply chip that evening, lose the morning). Both edited together; `MAX_ENTRIES` 30→90 because a 30-day window could never fill a paired-observation threshold. Counts are not ratings (0 servings is an answer; 8 stays 8; footer says "Caffeine 2" never "2/5"). Eight named mutants killed. Optional, free, device-only; pre-session sheet untouched; no medication/reproductive/substance categories (special-category data we have no reason to hold). Tests 835→862. Next: `.191` sleep consistency, `.192` Day in Review, `.193` behavior impacts, `.194` evening push.
- **The untestable half, tested (`.189`):** `.188`'s own PR body admitted the spend routes could not be tested as *wiring* — they import `server-only`, which throws under `tsx`. Node's exports map resolves it to a no-op under the **`react-server` condition**, so there is now a second lane: **`npm run test:routes`** (`*.routetest.ts`, deliberately outside the `*.test.ts` glob), wired into both [gate.mjs](scripts/gate.mjs) and ci.yml. Seven contracts pinned, all on the *degrade* path — exhausted quota still answers 200-with-rules, dark env never consults the quota nor says "quota", signed-out plan-voice always gets its briefing, chat's 429 is the deliberate exception and unconfigured still reads `coach_offline`. **The mutant `.188` could not run now dies**: `quota-blocks-rules-path` (quota as a route-wide 429) passes every pure test in the repo and fails here; also killed `plan-voice-gate-moved-up` and `dark-env-reads-as-quota`. Caps driven to `0` for determinism (kill switch refuses before the limiter), unique IP per case. Tests 835→842.
- **The meter exists now (`.188`):** per-user LLM metering + spend hardening — the prerequisite every paid LLM/voice feature was gated on. Both clients now return token `usage` (provider meter or char estimate, *marked* estimated; stream via `include_usage`); new [`llm_usage`](supabase/migrations/20260731_llm_usage.sql) ledger (dual identity per anonymous-push, **no insert policy** — clients must not forge their own spend; counts, never content); pure [`quota.ts`](src/lib/llm/quota.ts) daily per-identity caps (user > device > **ip floor**; `LLM_DAILY_CAP_*` env, `'0'` = kill switch; fail-open — ZDR stays the one fail-closed check); [`identity.ts`](src/lib/llm/identity.ts) resolves the user **before** the premium bypass (the paid path finally knows who it spends on). **Two previously unguarded money routes fixed**: `daily-insight` and `estimate-meal` ran paid inference for any gate-cookie holder — their LLM/vision branches now gate on premium + quota, degrading to rules/heuristic, never an error; plan-voice's signed-out rules contract untouched; chat 429s `coach_quota` honestly. **Provably dark today** (env unset → unchanged; free beta → unchanged); arms when founder applies the migration + sets `COACH_LLM_*`/Upstash/cap numbers. Six named mutants killed. **Actions billing re-blocked 2026-07-30** (all workflows die in 2s, no runner) — founder must clear billing; merges ride the local gate per the `.130`–`.157` precedent meanwhile.
- **Impacts (`.187`) — Field Notes complete:** the WHOOP journal payoff without the strap — on-device check-in ↔ session-load correlations in the weekly Mission Debrief (*"Sessions on sleep ≤ 2 days: load −33% vs your other days (4 vs 6 sessions)"*). Pure [`impacts.ts`](src/lib/journal/impacts.ts); refusals as killed mutants: **≥8 pairs + ≥3 per side or silence** (`.127`), <5% is noise not insight, an unfilled field never speaks (absence is not a rating), stress flags HIGH per the readiness direction, descriptive-only wording (LEGAL_SAFETY §3a grep), tombstones silent. Flag thresholds are byte-identical to `READINESS_RULES` — one definition of "bad day" (`.178` lesson). Session counts print in the line so the athlete weighs the evidence. **Field Notes shipped end-to-end (`.184`–`.187`)**: notes persist+sync → session entry composes (fragments first, verbatim) → coach remembers cues → journal pays back.
- **Cue & tweak memory (`.186`):** the logger shows an exercise's last note when it comes up again — *Last note (Jul 28): "tuck elbows"* — the page-flip a paper logbook gets used for, and the loop that makes every note written raise the next session's value. Pure [`cueMemory.ts`](src/lib/journal/cueMemory.ts) `lastNotesFor(exerciseId, history, n)`: newest-first with a defensive sort (post-merge array order is not trustworthy), tombstones silent, text verbatim. Named mutants all killed: any-exercise-note (a bench cue on squat day is worse than silence), stale-note-first, placeholder-on-empty, deleted-log-speaks. Notes sync as workout content, so Android-written cues surface on web. Next: `.187` Impacts.
- **The session entry (`.185`):** the Granola moment of Field Notes — the logger gains a collapsed **Field note** jot ([`SessionJotField`](src/components/workout/SessionJotField.tsx); rest window is dead air, five words while they're true), stored on `ActiveWorkout.sessionNote` and deliberately **never** copied onto the synced `CompletedWorkoutLog` — exercise notes are workout content and sync; the journal never leaves the device. Pure [`composeEntry.ts`](src/lib/journal/composeEntry.ts) fuses at finish: `collectFragments` (session jot first, then per-exercise notes attributed by name, one fragment per line) + `composeSessionEntry` (athlete's words **first and verbatim**, debrief after, check-in strip as footer). The binding rule is the Strava lesson, kept as a named mutant: **empty fragments → the entry is exactly the debrief** — invented-reflection, reworded-fragment, lost-attribution and order-flip variants all fail tests. `/history` splits into **Sessions | Journal** ([`JournalTimeline`](src/components/history/JournalTimeline.tsx)): entries + mind check-in notes interleaved, text search, privacy stated on the surface, and fragments editable post-hoc (`updateJournalFragments` — the machine's lines are not editable, an edited debrief would claim the rules said something they did not; editing a missing entry mints nothing). Victory sheet shows fragments above the debrief + "Saved to your journal." Next: `.186` cue memory, `.187` Impacts.
- **Load steers the plan (`.177`):** `.171` built [`coach/load.ts`](src/lib/coach/load.ts) and `.172`–`.173` taught the debrief to speak it — and **nothing ever fed it back**. Its only two importers were `debrief.ts` and `CoachTodayCard`; not one line of `adapt.ts`/`planEngine.ts`/`progression.ts` read ACWR, so the app measured that a week ran hot, said so honestly *after* the session, then planned the next week as though it had never looked. New pure [`loadGuard.ts`](src/lib/coach/loadGuard.ts) closes it at the one seam every prescription already flows through — `nextTargets`, reached from exactly one call site (`selector.ts`), which serves weekly generate, premium regen, adjust rebuilds and recovery alike. **Cap-only, and the asymmetry is the whole design:** a high ratio **holds** a rise (`coachWhySteadyWeek`) and never deloads, never cuts sets, never says anything readable as medical advice — ACWR is validated in team sports, contested in the literature, and **never validated for recreational lifting**. Declining to add weight on that evidence is a coaching call; taking weight away on it is not. `light` is likewise identity — auto-inflating volume off a contested ratio is the same error wearing a smile, and the debrief already *offers* the extra set. **`unknown` is provably identity**, asserted by `deepEqual` against the unguarded call rather than by reading the branch — the `.127` principle, and it is what every athlete under 14 days of history gets. Zone is computed **once at context build**, so `generateWeek` stays deterministic and the plan cannot shift under an athlete mid-week. Deliberately **not** in `chooseSplit`/`adaptPlan`: strain ≥70 already trims exercise count and readiness <40 already swaps to recovery — one mechanism per axis, or the same hard week gets punished three times by three unexplainable dampers. **Every clause falsified individually** (deleting the cap fails 3 tests; clamping decreases fails 2; letting `light` or `unknown` act fails 1 each), and the golden personas stay **byte-identical** — the proof the free path did not move. My own first integration test was wrong and the engine was right: 16 byte-identical sessions trip `stalled()` and *deload*, so "steady" never rose; the history had to become a genuinely progressing athlete before the cap was even reachable. Tests **725→733** (this branch adds 8; the 699 baseline in the commit message predates `.176` merging).
- **A notification nobody could have received (`.176`):** the wind-down nudge — after a session heavier than the athlete's own recent usual, one evening note while they can still act on it (*"That one ran hot… Water, food, and an early night buy tomorrow back."*). **The reach problem came first and nearly shipped unnoticed:** push has been user-triggerable through exactly **one** control since it shipped — a toggle buried in Profile → Reminders — so the cron would have fired for approximately nobody, with **no error anywhere to say so**. The debrief now offers it after a **high-zone** session, where the value is concrete; the button calls `subscribePush` **directly** (the `pushClient.ts` ordering contract — a granted-then-discarded permission poisons the origin permanently), and on iOS before install, where there is no `PushManager` at all, the same slot offers **Add to Home Screen** rather than a button that cannot work. Offered at most once a fortnight. **Time-of-day is the whole design**, so it is hourly (a UTC cron cannot hit 19:00 local for everyone) and lives in pure [`windDown.ts`](src/lib/windDown.ts) rather than a `decideNudge` branch — every other nudge gates on *absence*, this one requires the athlete was **present today**. **No stored zone → no send** (a guess risks 03:00); the day is judged **locally**, because a 22:00Z session is *this evening* in New York and *yesterday* in UTC; `last_wind_down_at` vs `last_session_at` **is** the idempotency, re-armed by the next hard session with no expiry job. **Its own cooldown** — sharing the comeback's 44 h marker would suppress exactly the success case (morning comeback → hard evening session). **One bit, not a zone stream:** `last_session_high boolean` self-heals on sync; an enum stores states nothing reads and a zone series is reconstructable training content. The same `handleComplete` call fixes a **live** defect — `last_session_at` only refreshed if the athlete visited Profile, so **comeback nudges were already firing on stale data**. Per-kind **tags** so an evening note never replaces a pending comeback; per-kind **preferences** deliberately not (they widen the `20260728` privacy contract). **Falsification found the fix twice more, not the bug:** the only-present-fields rule sat inline in a route handler and the 14-day cap inside a React component — extracted to `buildSubscriptionRow` / `mayOfferWindDown`, where restoring `?? null`, swapping `!== undefined` for truthiness (which would strand `lastSessionHigh: false` on `true` forever), or shortening the interval each now fail. **Founder: `20260730_wind_down_nudge.sql` is unapplied, and without VAPID keys nothing sends.** Tests **699→725**.
- **Media system:** Google Flow · Scout mascot — [docs/MEDIA_SYSTEM.md](docs/MEDIA_SYSTEM.md).
- **Ops:** prod ships via **Vercel Deploy Hook + GitHub webhook** (unmetered, no Actions) — [docs/VERCEL_DEPLOY_CHECKLIST.md](docs/VERCEL_DEPLOY_CHECKLIST.md) §1.1; `deploy-production` is now **manual-only** fallback. ~~**Actions is currently blocked**~~ — **cleared; Actions works again as of 2026-07-29**, verified by 8 green checks on PR #121 (CodeQL, gitleaks, aikido, build-and-test). The PR gate is live, so it no longer needs `npm run gate` as a substitute — though the local gate stays faster for pre-push. **Secrets program** + pre-public scrub shipped — [docs/SECRETS.md](docs/SECRETS.md); OSS public-ready (AGPL + CoC) — founder flips GitHub Public — [docs/OPEN_SOURCE.md](docs/OPEN_SOURCE.md). Promote **`.157`** — now on `master`; keep Supabase Site URL on www.
- Agents **must** ship wedge habit-loop + free acquisition. Refuse new pillars / America / locale farms / F5.
- **The whole stack is MERGED (2026-07-26).** All 13 PRs **#89–#101** landed on `master` in order — the Modernist rebrand (`.130`–`.138`), the 13 app screens (`.139`–`.149`), the mobile program (`.150`–`.156`) and the a11y coverage pass (`.157`). 39 commits, no conflicts anywhere: it was one linear chain. Verified **on merged master**: gate **29/29**, a11y **29/29**, 599 unit tests, `secrets:scan` clean. Actions is still billing-blocked, so `npm run gate` locally remains the gate. **Nothing is deployed by merging** — promotion is the Deploy Hook, below.
- **Founder:** Accept B on Android + phone excellence → invites → YC F26. **Wire the Deploy Hook webhook** ([checklist §1.1](docs/VERCEL_DEPLOY_CHECKLIST.md)) then promote `.157`; clear Actions billing to restore the PR gate. **Before any list email: set `MAIL_POSTAL_ADDRESS`** (CAN-SPAM footer — confirm the Bizee TX registered-agent address is publishable as a business address, else PO box/CMRA; same address closes the DMCA agent row) — [LEGAL_SAFETY.md](docs/LEGAL_SAFETY.md) §3. Before Public: `npm run secrets:scan`, enable GitHub secret scanning + push protection.

---

## Read next

[AGENTS.md](AGENTS.md) (conventions · glossary · commands) → [INDEX.md](INDEX.md) (task → doc routing · stale paths §4) → [ORCHESTRATION.md](ORCHESTRATION.md) (horizons · gates · departments) → the folder `INDEX.md` where you'll work.

---

## Trap terms (full glossary: AGENTS.md)

| Term | Means |
|------|-------|
| Mission Coach | AI plan engine — `src/lib/coach/`, `/coach` (≠ `/coaching` human-lead form) |
| Today | Route `/log` (`HomePage.tsx`), nav label "Today" |
| Train | Route `/active` — the logger |
| Fuel | Route `/nutrition` |
| Journey phase 0–3 | UX arc ([docs/JOURNEY.md](docs/JOURNEY.md)) ≠ build phases A–I ([docs/PLAN.md](docs/PLAN.md)) ≠ PFT G1–G8 |
| Horizon 0–3 | What may be built now — [ORCHESTRATION.md](ORCHESTRATION.md) |
| Wedge | Train + Mission Coach — the go-to-market story |
| Super Bundle | The one premium sub: $11.99/mo · $59/yr founders · $149 lifetime |
| PRIVATE_MODE | Site gate — only the founder flips it |
| mw-core | [packages/mw-core](packages/mw-core) — pure TS shared coach/workout logic |

---

## Commands

`npm run typecheck` · `npm test` · `npm run build` · `npm run lint` — full list in AGENTS.md §Commands.
Android: `cd apps/android && ./gradlew :app:assembleDebug`.

---

## Hard rules

1. **Horizon rule** — Horizon W: build wedge excellence (Train/Today/Victory/Coach). No new pillars/locales/America/F5 without explicit founder override. ≥10 beta only after excellence sign-off.
2. **The free logger is never gated. Ever.**
3. Agents never flip `PRIVATE_MODE`, never invent traction numbers, never mark founder tasks done.
4. Do not open stale/deleted paths — [INDEX.md](INDEX.md) §4.
5. Docs match reality: every ship updates [LOG.md](LOG.md) + this file's `## Now` (+ build label).
