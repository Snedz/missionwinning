#!/usr/bin/env node
/**
 * Stage INTERNAL + founder ops content into ./ops/ (gitignored).
 *
 * Founder: run `npm run ops:sync`, then init/push ops/ as private mission-ops.
 * Never commit ./ops to the product repo.
 *
 * docs/DUAL_REPO.md · docs/CLASSIFICATION.md
 */

import { mkdirSync, copyFileSync, writeFileSync, existsSync, readFileSync } from 'node:fs';
import { dirname, join, relative } from 'node:path';
import { fileURLToPath } from 'node:url';

const root = join(dirname(fileURLToPath(import.meta.url)), '..');
const ops = join(root, 'ops');

/**
 * Product-repo paths to stage into ops/strategy/ when present.
 * After public-OSS scrub, war-room paths are stubs only — never overwrite a full
 * ops copy with a stub (see isRelocatedStub).
 */
const INTERNAL_COPIES = [
  'docs/STRATEGY.md',
  'docs/REDTEAM.md',
  'docs/PRELAUNCH_CAPITAL.md',
  'docs/OUTREACH_VA_BRIEF.md',
  'docs/YC_THESIS.md',
  'docs/ACCELERATOR_SPRINT.md',
  'docs/PRICING_REVIEW_2026-08.md',
  'docs/CLUB_PLAN.md',
  'docs/IDENTITY_SOCIAL_PLAN.md',
  'docs/CLASSIFICATION.md',
  'docs/DUAL_REPO.md',
];

function ensureDir(p) {
  mkdirSync(p, { recursive: true });
}

function write(rel, body) {
  const dest = join(ops, rel);
  ensureDir(dirname(dest));
  writeFileSync(dest, body, 'utf8');
  console.log('wrote', relative(root, dest));
}

function isRelocatedStub(content) {
  return (
    content.includes('RELOCATED_TO_MISSION_OPS') ||
    content.includes('Full text was removed from the public product tree')
  );
}

function copyIfExists(rel, destRel) {
  const src = join(root, rel);
  if (!existsSync(src)) {
    console.log('skip (missing)', rel);
    return;
  }
  const body = readFileSync(src, 'utf8');
  const dest = join(ops, destRel);
  if (isRelocatedStub(body)) {
    if (existsSync(dest) && !isRelocatedStub(readFileSync(dest, 'utf8'))) {
      console.log('skip stub (keeping full ops copy)', rel);
      return;
    }
    console.log('skip stub (no full ops copy yet)', rel);
    return;
  }
  ensureDir(dirname(dest));
  writeFileSync(dest, body, 'utf8');
  console.log('copied', rel, '→', relative(root, dest));
}

ensureDir(ops);
ensureDir(join(ops, 'strategy'));

write(
  'README.md',
  `# mission-ops (PRIVATE)

This directory is generated by \`npm run ops:sync\` in the product repo and is
**gitignored** there. Push it to a **private** GitHub repo (e.g. \`Snedz/mission-ops\`).

Never merge this tree into public \`missionwinning\`.

| File | Purpose |
|------|---------|
| VISION_LONG_EVERYTHING.md | Health → WeChat-scale long-term thesis |
| FOUNDER_CRITICAL_PATH.md | Users / money / legal this week |
| STATUS_PRIVATE.md | Founder blockers not for public CONTEXT |
| INTERNAL_MANIFEST.md | What must not ship public |
| strategy/ | Copies of INTERNAL planning docs |

See product \`docs/DUAL_REPO.md\` and \`docs/CLASSIFICATION.md\`.
`
);

write(
  'INTERNAL_MANIFEST.md',
  `# INTERNAL manifest

Full text for these lives **only** in this private repo under \`strategy/\`.
The product repo keeps short stubs (marker \`RELOCATED_TO_MISSION_OPS\`).

| Product stub path | Private file |
|-------------------|--------------|
| docs/STRATEGY.md | strategy/STRATEGY.md |
| docs/REDTEAM.md | strategy/REDTEAM.md |
| docs/PRELAUNCH_CAPITAL.md | strategy/PRELAUNCH_CAPITAL.md |
| docs/OUTREACH_VA_BRIEF.md | strategy/OUTREACH_VA_BRIEF.md |
| docs/YC_THESIS.md | strategy/YC_THESIS.md |
| docs/ACCELERATOR_SPRINT.md | strategy/ACCELERATOR_SPRINT.md |
| docs/PRICING_REVIEW_2026-08.md | strategy/PRICING_REVIEW_2026-08.md |
| docs/applications/* answers | LOCAL gitignored on product |
| .hermes/plans/ | LOCAL — never git |
| ops/ | LOCAL staging on product machine |

Also mirrored for convenience (product may still hold public copies): CLUB_PLAN, IDENTITY_SOCIAL_PLAN, CLASSIFICATION, DUAL_REPO.

Public-safe product: docs/contracts/*, AGENTS, INDEX, help, architecture, CLASSIFICATION, DUAL_REPO, product code.
`
);

write(
  'FOUNDER_CRITICAL_PATH.md',
  `# Founder critical path — users before everything-app

**Owner:** Founder only. Agents support product excellence; they never mark these done.

The long-term everything platform needs a living population. Until week-4 retention
exists, games / metaverse / mini-hosts are contracts only.

## This week (ordered)

| # | Task | Doc / surface | Done when |
|---|------|---------------|-----------|
| 1 | Set \`MAIL_POSTAL_ADDRESS\` (CAN-SPAM) | LAUNCH_RUNBOOK §2 · CONTEXT Status | Invite emails can send |
| 2 | Phone excellence pass | Train → Today → Victory → Coach | Founder says pass (Horizon W) |
| 3 | Dogfood notes written | LAUNCH_RUNBOOK §3a | Friction list pasted to agents |
| 4 | Recruit ≥10 real humans | STRATEGY first-10 · BETA_INVITE | Profiles exist; not friends-only theater |
| 5 | Pending migrations applied | LAUNCH_RUNBOOK §3 · migration ledger | Ledger green |
| 6 | Ops Wave A | Upstash, Sentry, SMOKE_BASE_URL | Rate limits + errors real |
| 7 | Public site flip | PRIVATE_MODE=false when gates green | Offline/SW validated |
| 8 | EIN → Stripe take-a-dollar | LLC + PRE_REVENUE | One paid path verified |

## Standing bans (until retention + cash)

- New games / metaverse product surfaces  
- Social feed / DMs / Top-8  
- America marketing / wearables-as-score  
- Locale body farms  
- Agents inventing traction or flipping PRIVATE_MODE  

## Decision filter

1. Week-4 retained weekly loggers or honest revenue this quarter?  
2. Else: contract/spec only?  
3. Else: written founder override?  
4. Else: refuse.

## Dual-repo

\`npm run ops:sync\` then push this folder to private mission-ops.
`
);

write(
  'STATUS_PRIVATE.md',
  `# Private status (not for public CONTEXT.md)

Update freely. Product CONTEXT.md keeps only agent-needed gate facts.

| Fact | State | Updated |
|------|-------|---------|
| Beta humans (real) | | |
| Week-4 retention (last cohort) | n/a until public | |
| EIN / Stripe | | |
| Capital runway | | |
| Dual-repo pushed | | |
| Public flip target date | | |
| Notes | | |
`
);

write(
  'VISION_LONG_EVERYTHING.md',
  `# VISION — Long-term everything (INTERNAL)

**Class:** INTERNAL · **Not for public GitHub product tree**

## Nested products

| Layer | Name | Public language |
|-------|------|-----------------|
| L0 | Mission OS (identity, economy, module host) | Invisible |
| L1 | Mission Winning Health | Train Anywhere. Win Daily. |
| L2 | Everything ecosystem | Only after health PMF + cash |

## Sequence (WeChat lesson)

1. Daily habit — Train + Today  
2. Identity — Athlete Page + Card (MySpace soul, no feed)  
3. Money — Super Bundle rails  
4. Platform — mini-programs on shared Mission ID  
5. Games / metaverse — same inventory and points contracts  

## One fix

Unblock humans (postal, excellence, 10 beta, public offline proof) before building continents.

## Contracts (product repo, PUBLIC-safe)

- docs/contracts/IDENTITY.md  
- docs/contracts/ECONOMY.md  
- docs/contracts/MODULE.md  
- docs/contracts/AI_INTEROP.md  

## Privilege design

User owns body data · free core covenant · earned not bought rank · AI is coach not clinician · no dark retention · agents cannot flip production gates.

Full session plan may live under ~/.grok/sessions; this file is the durable ops home.
`
);

for (const rel of INTERNAL_COPIES) {
  copyIfExists(rel, join('strategy', rel.replace(/^docs\//, '')));
}

// Pointer so product agents know ops is local
const pointer = join(root, 'docs', 'OPS_LOCAL.md');
if (!existsSync(pointer)) {
  writeFileSync(
    pointer,
    `# ops/ local pack

After \`npm run ops:sync\`, private founder content lives in **\`ops/\`** (gitignored).

See [DUAL_REPO.md](DUAL_REPO.md) and [CLASSIFICATION.md](CLASSIFICATION.md).
`,
    'utf8'
  );
  console.log('wrote docs/OPS_LOCAL.md');
}

console.log('\nDone. Next (founder): cd ops && git init && push to private mission-ops');
