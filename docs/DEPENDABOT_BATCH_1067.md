# Frozen plan — Dependabot leftovers batch (`.1067`)

**Status: FROZEN.** Implement this document. Do not edit this file after the freeze commit. Do not merge the listed Dependabot PRs (that burns GitHub Actions minutes and Vercel Hobby). This branch supersedes the leftovers that are still unmerged.

**Label:** `2026.07-unified.1067`  
**Branch:** `cursor/dependabot-leftovers-batch-8c8c` from current `origin/master` (`7aade1b3`)  
**Excellence-Override:** dependabot leftovers batch (non-major)  
**Hard rules:** do not flip `PRIVATE_MODE`; no secrets; no EIN; no traction numbers; `[skip vercel]` on every commit; local `npm test` + lint + typecheck only (no GitHub Actions). Actions red is OK. Do not merge. Do not promote. Live www stays `.697`.

Does **not** replace [PLAN.md](PLAN.md) (build phases A–I).

---

## Goal / one concern

Apply remaining **safe non-major** Dependabot leftovers on one Cursor chore PR, stamp paper `.1067`, and keep Merge Steward from squash-merging raw Dependabot PRs. One coherent lockfile. No product features.

Master moved while this batch was assigned. Tip at assign was `.1066` (`97d78155`). Current tip is `7aade1b3` with three unstamped Dependabot squashes already on master: `#919` react-i18next, `#922` next, `#924` radix-dialog. This PR branches from that tip and restamps paper. It does not revert those squashes.

---

## Take (apply on this branch)

| PR | Bump | State at freeze | Action |
|----|------|-----------------|--------|
| #921 | `stripe` 22.5.0 → 22.6.2 | **Open.** SDK pins API `2026-08-26.dahlia`. Hardenings (webhook secret non-empty, URL requestor, multipart boundaries). | Apply via `package.json` + one lockfile refresh. Typecheck existing Stripe usage. If `LatestApiVersion` no longer accepts `2026-07-29.dahlia`, change only the `apiVersion` string in `src/lib/stripeServer.ts` to `2026-08-26.dahlia`. No new checkout, portal, webhook, or product code. |
| #923 | `@playwright/test` 1.62.1 → 1.63.0 | **Closed unmerged** (dirty / rebase). | Apply locally. Also bump sibling `playwright` 1.62.1 → 1.63.0 so runner and browser package stay one version (same posture as `.744`). Dev only. Revert both if the local suite breaks. |
| #919 | `react-i18next` 17.0.11 → 17.0.13 | **Already on master** (`1c949ded`). | Do not re-apply. Named in supersede list so Steward does not re-open / re-merge. |
| #922 | `next` 16.3.3 → 16.3.4 | **Already on master** (`5566967c`). | Do not re-apply. Named in supersede list. |

If a taken bump breaks `npm test`, lint, or typecheck: revert **that** bump only, keep the others, document the skip.

---

## Skip

| PR | Bump | Why |
|----|------|-----|
| #920 | `eslint` 9.39.4 → 10.10.0 | **MAJOR.** Leave that PR open. Note in this PR body that eslint 10 is skipped. |
| (none open) | Tailwind 4 | Permanent skip. Do not take if one appears. |
| #925 | `@next/bundle-analyzer` 16.2.10 → 16.3.4 | Arrived after this batch was assigned. Out of scope. |
| #926 | `posthog-js` 1.396.5 → 1.429.5 | Arrived after this batch. Out of scope (large minor). |
| #927 | `@supabase/ssr` 0.12.3 → 0.12.7 | Arrived after this batch. Out of scope. |
| #928 | `i18next` 26.3.1 → 26.4.2 | Arrived after this batch. Out of scope. |
| #924 | `@radix-ui/react-dialog` 1.1.15 → 1.1.23 | Already squash-merged on master. Do not revert. Not in this take list. |

---

## Files

- `package.json` + `package-lock.json` — `stripe` ^22.6.2, `@playwright/test` ^1.63.0, `playwright` ^1.63.0
- `src/lib/stripeServer.ts` — `apiVersion` string only, and only if types require (or to match the SDK pin). No other Stripe edits.
- `src/lib/buildInfo.ts` — `APP_BUILD_LABEL` → `2026.07-unified.1067`
- `LOG.md` — new `.1067` heading; rotate oldest live (`.1048`) to `docs/archive/log/` + row in `docs/archive/INDEX.md` (keep ≤15)
- `CONTEXT.md` `## Now` — add `.1067`, drop oldest shipped (`.1047`) so budget stays ≤25; mention full label
- this file (frozen)
- `docs/INDEX.md` — one row pointing at this file

Do **not** edit product UI, checkout params, webhooks, eslint config, Tailwind, or `PRIVATE_MODE`.

---

## Refuse

- Do not merge `#919` `#921` `#922` `#923` themselves (already-merged `#919`/`#922` stay as history; do not re-merge).
- Do not take eslint 10 (`#920`) or Tailwind 4.
- Do not flip `PRIVATE_MODE`.
- Do not invent traction. No EIN. No promote. Live www stays `.697`.
- Do not force-push `master` / `main`.
- Do not burn Vercel Hobby (`[skip vercel]` on every commit; zero Preview).
- Do not smash product code to land a bump.
- Do not expand into `#925`–`#928`.
- Do not merge this PR — Merge Steward squash when ready.

---

## Ship protocol (same commit as the bumps)

1. `APP_BUILD_LABEL` → `2026.07-unified.1067` in `src/lib/buildInfo.ts`
2. `LOG.md` — new `.1067` heading; rotate `.1048`
3. `CONTEXT.md` `## Now` — add `.1067`, drop `.1047`
4. Commit trailer: `Excellence-Override: dependabot leftovers batch (non-major)`
5. Commit trailer: `[skip vercel]`
6. Open one undrafted batch PR on `master` (do not merge). Title: `chore dependabot leftovers batch (.1067)`.
7. PR body must say: Do not merge the listed Dependabot PRs; this supersedes #919 #921 #922 #923; skip #920 eslint major; no PRIVATE_MODE flip; no promote; live stays .697.

---

## Verify (Cursor VM, not Actions)

```
npm test
npm run lint
npm run typecheck
```

No `npm run gate`, no e2e, no Vercel, no merge. Report results in the batch PR body. Do not wait on GitHub Actions green.

---

## Done when

- One open batch PR on `master` with `.1067` stamp, coherent lockfile, taken leftovers applied.
- eslint 10 and Tailwind 4 absent from the take list.
- Clear supersede list for Merge Steward: close leftover Dependabot PRs after squash (`#921` open; `#923` already closed unmerged; `#919`/`#922` already merged — do not re-merge).
- `#920` left open as skipped major.
- Local suite result reported in the PR body.
