# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-set-table-next-cite
done_means: Set-table after-set next-cite line under .mw-house is house leftover (--house-muted / house-lede). vs-last leftover stays. Skip stays outline. load-% stays parked this leftover. Finish / Skip / Swap / Form guide stay outline. Log set stays the sole filled press. Picker rewrite stays parked.
accept: npx tsx --test src/lib/setTableNextCite.test.ts
test_written: yes

## progress

Honesty first (3 fail / 4 pass). Chrome after red. Accept green (30/30 including houseChrome). Walk 1440 localhost:3000 /active hydrated: next-cite “5 × BW · Coach plan” rgb(113, 113, 122) 11px house-lede house-next-cite. Skip outline house-btn bg rgb(244, 244, 245). Log set rgb(24, 24, 27). vs-last leftover stays +10 kg 11px muted. e1RM leftover stays. load-% cite not painted this leftover (SetRowPercentCite still text-muted). Picker rewrite stays parked.

Keep going after this leftover ships: one leftover per pass. Prefer remaining live-row muted (load-%). Not You chrome. ExercisePicker rewrite stays parked.

## decisions

- One leftover: Set-table next-cite line is house leftover. Class-only.
- vs-last leftover stays. Do not restyle it.
- Next-cite Skip stays house-btn outline — never house-btn-primary.
- Do not restyle load-% this leftover.
- Close / Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- Log set stays the sole filled press.
- ExercisePicker rewrite stays parked.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
- Founder said keep going without Continue.
