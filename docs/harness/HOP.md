# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-victory-receipt-heads
done_means: Victory receipt table heads under .mw-house.house-victory-receipt are house leftover (--house-muted / house-lede). Receipt lead leftover stays. Prev / vs-last cells stay muted this leftover. Description stays muted. Next stays the filled press on Victory. Do not put mw-house on the Victory dialog. Log set stays the sole filled press on compose. Finish / Skip / Swap / Form guide stay outline. Picker rewrite stays parked.
accept: npx tsx --test src/lib/victoryReceiptHeadsCite.test.ts
test_written: yes

## progress

Honesty first (3 fail / 5 pass). Chrome after red. Accept green (34/34 including houseChrome). Walk 1440 localhost:3000 /active Finish: receipt heads Set / Prev / Load / vs last rgb(113, 113, 122) 11px, text-transform none. Lead leftover stays 12px. Prev cell stays muted. Description stays muted. Next stays filled on poster-field rgb(173, 23, 0). Dialog is not mw-house. Picker rewrite stays parked.

Keep going after this leftover ships: one leftover per pass. Prefer remaining Victory first-paint muted (receipt prev / vs-last cells, description). Not picker rewrite. Not You chrome.

## decisions

- One leftover: Victory receipt table heads is house leftover. Class-only.
- Receipt lead leftover stays. Do not restyle it.
- Prev / set index / vs-last cells stay text-muted this leftover.
- Description / feel / share stay muted this leftover.
- Do not put mw-house on the Victory dialog. Receipt already carries house-victory-receipt.
- Next stays the filled press on Victory (`primary-action`).
- Log set stays the sole filled press on compose.
- Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- ExercisePicker rewrite stays parked.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
- Founder said Continue.
