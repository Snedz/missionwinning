# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: Quiet Track (.975)
done_means: /track first paint logs a scale/tape number they already have; empty invents nothing; not on Today; measurements stay free; Today still one Start
accept: node --import tsx --test src/lib/quietTrack.test.ts src/lib/quietTrackSurface.test.ts && npx tsc --noEmit
test_written: no

## progress

Frozen plan `.975` landed. Product code next.

## decisions

Track is the body-metrics pillar (Fuel-class). Activity/GPS fold under Show more. Progress photos stay off this surface. Skip `.974` (Quiet Move).
