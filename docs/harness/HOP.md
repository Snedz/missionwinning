# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-inset-cue-line
done_means: Train first-paint in-set cue line is house leftover, not text-muted. Finish / Skip / Swap / Form guide / Repeat* never house-btn-primary.
accept: npx tsx --test src/lib/workout/inSetCueLine.test.ts
test_written: yes

## progress

Honesty was red (missing house-cue-line). Chrome is house leftover. Walk next.

## decisions

- One leftover: In-set cue line is house leftover.
- Do not restyle Finish / Skip / Swap / Form guide / Repeat*.
- Do not invent rooms. Calendar / charts / posters stay parked.
- Do not restyle shared EmptyState.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- Next-target / e1RM / vs-last / load-% cites wait until after a set.
- Week pane may stay a JS button.
- I-Day Begin / Continue parked unless nothing else is honest.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
