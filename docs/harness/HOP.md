# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-move
done_means: Move first paint is the quiet log. No flow grid, collections, premium preview, recent wins, or Browse-flows empty on /move. QuietMoveLogCard stays the writer. Log set stays filled. generateWeek stays on /coach. Fuel stays empty + Log meal, or today's meals.
accept: npx tsx --test src/lib/move/leftoverMove.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Park leftover PR #889. Continue #890.

## decisions

- Tracker first, then leftover house rooms. Fuel was the log. Move is the quiet log, not a flow tour.
- Flow catalog stays off first paint (unmounted, not folded).
- Stamp `.1064` from this branch `.1063`.
- `[skip vercel]`. Do not merge. Do not promote. PRIVATE_MODE on. Live www stays `.696`.
