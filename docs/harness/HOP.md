# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-movement-history-list-row-cites
done_means: MovementHistorySheet list-row cites (workoutName subtitle + sets line) under .mw-house are house leftover (--house-muted / house-lede), not text-muted-foreground. Empty cite leftover stays. Sheet chrome / Close / copy stay. Finish / Skip / Swap / Form guide stay outline. Log set stays the sole filled press. After-set cites stay parked.
accept: npx tsx --test src/lib/movementHistoryListRowCite.test.ts
test_written: yes

## progress

Honesty was red (2 fail / 5 pass). Chrome. Accept pass. Walk next.

## decisions

- One leftover: Movement-history list-row cites are house leftover.
- Empty cite leftover stays. Do not restyle it.
- Do not restyle the sheet chrome, Close, or copy.
- Close / Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- Log set stays the sole filled press.
- After-set cites (next-target / e1RM / vs-last / load-%) stay parked.
- Plate-line cite / Check-in scale / Movement-history empty cite stay done. Do not reopen.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
