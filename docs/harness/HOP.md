# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-skipped-this-session-cite
done_means: session-skipped-exercise cite under .mw-house is house leftover (--house-muted / house-lede), not text-muted-foreground. vs-last / e1RM / next-target / load-% stay parked muted. Skip hold stays outline. Copy stays. Finish / Swap / Form guide stay outline. Log set stays the sole filled press.
accept: npx tsx --test src/lib/skippedThisSessionCite.test.ts
test_written: yes

## progress

Honesty was red (3 fail / 4 pass). Chrome. Accept pass. Walk 1440: already on /active from prior Home Start. Keyboard-confirm Skip on Bear Crawl / Superman. Skipped-this-session cite "Skipped this session" house-lede --house-muted rgb(113,113,122) at 14px. Log set rgb(24,24,27). Skip hold stays outline house-btn. Copy untouched.

Next leftover: pick the loudest non-parked first-paint fail on /active after this leftover ships. Same leftover pattern. Prefer Swap / Form guide / Check-in / live row. After-set cites (next-target / e1RM / vs-last / load-%) stay parked. Do not restyle Skip hold, next-target, or Log set. Not You chrome. ExercisePicker rewrite stays parked.

## decisions

- One leftover: Skipped-this-session cite is house leftover.
- vs-last / e1RM / next-target / load-% stay parked. Do not restyle them.
- Skip this exercise stays house-btn hold, not filled.
- Do not restyle copy ("Skipped this session").
- Close / Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- Log set stays the sole filled press.
- Set-table RPE / in-set PR leftovers stay. Do not reopen.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
