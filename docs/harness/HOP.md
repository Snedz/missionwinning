# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-explore
done_means: Explore first paint is the board, pin list, and add a place. No GPS nearby, session tag, or maps tour on /explore. Add a place stays the writer. Log set stays filled. generateWeek stays on /coach. Fuel stays the log. Feedback stays the form. Garage stays quiet title.
accept: npx tsx --test src/lib/places/leftoverExplore.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Park leftover PR #889. Continue #890.

## decisions

- Tracker first, then leftover house rooms. Explore is the board, not a GPS tour.
- GPS / tag last / maps stay off first paint (unmounted, not folded).
- Stamp `.1072` from this branch `.1071`.
- `[skip vercel]`. Do not merge. Do not promote. PRIVATE_MODE on. Live www stays `.696`.
