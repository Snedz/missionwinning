# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-garage
done_means: Garage first paint is quiet title. No rooms, buddy list, chat, or presence tour on /server. Log set stays filled. generateWeek stays on /coach. Fuel stays the log. Feedback stays the form. Coach never reads Garage.
accept: npx tsx --test src/lib/leftoverGarage.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Park leftover PR #889. Continue #890.

## decisions

- Tracker first, then leftover house rooms. Garage is a quiet foot, not a messenger tour.
- Rooms / buddy list / chat stay off first paint (unmounted, not folded). Engine stays on disk.
- Stamp `.1071` from this branch `.1070`.
- `[skip vercel]`. Do not merge. Do not promote. PRIVATE_MODE on. Live www stays `.696`.
