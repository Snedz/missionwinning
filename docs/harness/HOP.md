# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-form-guide-figure-cap
done_means: Form guide figure caption under .mw-house is house leftover (--house-muted / house-lede). Breath leftover stays. Ready leftover stays. Section items leftover stays. Section headings / marks / Got it / copy stay. Finish / Skip / Swap / Form guide stay outline. Log set stays the sole filled press. After-set cites stay parked. ExercisePicker rewrite stays parked.
accept: npx tsx --test src/lib/formGuideFigureCapCite.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Honesty red 3 fail / 7 pass, then class-only chrome.

Walk 1440 localhost:3000 /active (hydrated). Bench Form guide. Figure caption "Side view · full range of motion" is house-lede rgb(113, 113, 122) 12px. Breath leftover stays. Ready leftover stays. Section items leftover stays. Got it / Form guide / Finish outline rgb(244, 244, 245). Log set rgb(24, 24, 27). Copy untouched.

Next leftover: pick the loudest non-parked first-paint fail on /active after this leftover ships. Same leftover pattern. Prefer Swap / Form guide / Check-in / live row. After-set cites (next-target / e1RM / vs-last / load-%) stay parked. Do not restyle ready-position, breath, section items, Got it, or Log set. Not You chrome. ExercisePicker rewrite stays parked.

## decisions

- One leftover: Form guide figure caption is house leftover.
- Breath leftover stays. Ready leftover stays. Section items leftover stays. Do not restyle them.
- Do not restyle section headings, marks, Got it, or copy.
- Close / Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- Log set stays the sole filled press.
- After-set cites stay parked.
- ExercisePicker rewrite stays parked.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
