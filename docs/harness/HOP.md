# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-form-guide-breath-cite
done_means: Form guide breath body cite under .mw-house is house leftover (--house-muted / house-lede). Ready-position cite leftover stays. Setup / execute / Avoid / Got it / copy stay. Finish / Skip / Swap / Form guide stay outline. Log set stays the sole filled press. After-set cites stay parked. ExercisePicker rewrite stays parked.
accept: npx tsx --test src/lib/formGuideBreathCite.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Honesty red 3 fail / 5 pass, then class-only chrome.

Walk 1440 localhost:3000 /active (hydrated). Bench Form guide. Breath cite "In down · Out up" is house-lede rgb(113, 113, 122) 17px. Ready-position leftover stays house-lede rgb(113, 113, 122). Got it / Form guide / Finish outline rgb(244, 244, 245). Log set rgb(24, 24, 27). Copy untouched.

Next leftover: pick the loudest non-parked first-paint fail on /active after this leftover ships. Same leftover pattern. Prefer Swap / Form guide / Check-in / live row. After-set cites (next-target / e1RM / vs-last / load-%) stay parked. Do not restyle ready-position, Got it, or Log set. Not You chrome. ExercisePicker rewrite stays parked.

## decisions

- One leftover: Form guide breath cite is house leftover.
- Ready-position cite leftover stays. Do not restyle it.
- Do not restyle Setup / Execute / Avoid items, figure caption, Got it, or copy.
- Close / Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- Log set stays the sole filled press.
- After-set cites stay parked.
- ExercisePicker rewrite stays parked.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
