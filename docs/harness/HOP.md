# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-account
done_means: Account first paint is sign-in, return, and prefs. No Explore, help, gym kit, referral, feedback, or Super Bundle tour on /account. Import stays reachable via #import. Log set stays filled. generateWeek stays on /coach. Fuel stays the log. Move stays the quiet log. You stays title + Account.
accept: npx tsx --test src/lib/leftoverAccount.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Park leftover PR #889. Continue #890.

## decisions

- Tracker first, then leftover house rooms. Account is sign-in / return / prefs, not a settings tour.
- `#import` stays a deep link. Owner tools stay if the founder is on.
- Stamp `.1066` from this branch `.1065`.
- `[skip vercel]`. Do not merge. Do not promote. PRIVATE_MODE on. Live www stays `.696`.
