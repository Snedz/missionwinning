# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-hard-session-lead-cite
done_means: Hard-session lead cite under .mw-house is house leftover (--house-muted / house-lede), not text-muted-foreground. Stop / not-care / emergency / clinician stay. Confirm stays outline. Portals stay mw-house. Copy unchanged.
accept: npx tsx --test src/lib/hardSessionLeadCite.test.ts
test_written: yes

## progress

Honesty was red (3 fail / 3 pass). Chrome. Accept pass. Walk next.

## decisions

- One leftover: Hard-session lead cite is house leftover.
- Cite is house-lede. Scoped `.house-hard-session .house-lede` keeps inherit and `--house-muted`.
- Do not change hard-session / PT / field-test copy. Counsel owns it.
- Do not restyle confirm. Confirm / Finish / Skip / Swap / Form guide stay outline.
- Log set stays the sole filled press.
- Check-in lead / Swap garage / another-movement / Form guide ready-cite stay done. Do not reopen.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- After-set cites parked.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
