# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-track
done_means: Track first paint is the scale/tape log. No walks, GPS, import, wearables, or trends tour on /track. BodyMetricsCard stays the writer. Log set stays filled. generateWeek stays on /coach. Fuel stays the log. Move stays the quiet log. Mind stays the check-in.
accept: npx tsx --test src/lib/track/leftoverTrack.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Park leftover PR #889. Continue #890.

## decisions

- Tracker first, then leftover house rooms. Track is the scale/tape log, not a walks tour.
- Walks / GPS / import / trends stay off first paint (unmounted, not folded).
- Stamp `.1068` from this branch `.1067`.
- `[skip vercel]`. Do not merge. Do not promote. PRIVATE_MODE on. Live www stays `.696`.
