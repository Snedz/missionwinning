# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-fuel
done_means: Fuel first paint is empty + Log meal, or today's meals. No remaining, notepad, Show more, recipes, restock, week, wizard, adapt, weight, or past-days tour on /nutrition. FuelLogSheet stays the meal writer. Log set stays filled. generateWeek stays on /coach.
accept: npx tsx --test src/lib/nutrition/leftoverFuel.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Park leftover PR #889. Continue #890.

## decisions

- Tracker first, then leftover house rooms. Fuel is the log, not a tour.
- Search / barcode / recipes stay off first paint (unmounted, not folded).
- Stamp `.1063` from this branch `.1062`.
- `[skip vercel]`. Do not merge. Do not promote. PRIVATE_MODE on. Live www stays `.696`.
