# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-victory-stats-labels
done_means: Victory Duration / Volume / Sets labels under .mw-house.house-victory are house leftover (--house-muted / house-lede). vs-last leftover stays 11px. Volume unit stays muted this leftover. Receipt / description stay muted. Next stays the filled press on Victory. Do not put mw-house on the Victory dialog. Log set stays the sole filled press on compose. Finish / Skip / Swap / Form guide stay outline. Picker rewrite stays parked.
accept: npx tsx --test src/lib/victoryStatsLabelsCite.test.ts
test_written: yes

## progress

Honesty first (3 fail / 5 pass). Chrome after red. Accept green (33/33 including houseChrome). Walk 1440 localhost:3000 /active Finish: Duration / Volume / Sets labels rgb(113, 113, 122) 12px house-lede house-victory-stat-label. vs-last leftover “+1:07 vs last” stays 11px. Volume unit “reps” stays text-muted. Receipt stays muted. Next stays filled on poster-field rgb(173, 23, 0). Dialog is not mw-house. Picker rewrite stays parked.

Keep going after this leftover ships: one leftover per pass. Prefer remaining Victory first-paint muted (volume unit, receipt, description). Not picker rewrite. Not You chrome.

## decisions

- One leftover: Victory stats labels (Duration / Volume / Sets) are house leftover. Class-only.
- vs-last leftover stays. Do not restyle it.
- Volume unit (`volume.unit`) stays text-muted this leftover.
- Receipt / description / feel / share stay muted this leftover.
- Do not put mw-house on the Victory dialog. Stats strip already carries house-victory.
- Next stays the filled press on Victory (`primary-action`).
- Log set stays the sole filled press on compose.
- Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- ExercisePicker rewrite stays parked.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
- Founder said Continue.
