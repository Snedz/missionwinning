# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-active-log-set-without-persist
done_means: /active first paint mounts the set table + Log set (`house-btn-primary` / `--house-press`) from the painted compose. Persist hydrate must not own nextSet. Finish / Skip / Swap / Form guide / Repeat* never house-btn-primary.
accept: npx tsx --test src/lib/workout/activeLogSetWithoutPersist.test.ts
test_written: yes

## progress

Honesty was red (page gated nextSet on
activeWorkout; composeNextSet missing).
Chrome in. Accept next.

## decisions

- One leftover: nextSet from painted compose when the store is empty.
- Do not restyle Finish / Skip / Swap / Form guide / Repeat*.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
