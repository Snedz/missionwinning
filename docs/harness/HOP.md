# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-victory-volume-unit
done_means: Victory volume unit cite under .mw-house.house-victory is house leftover (--house-muted / house-lede). Stats labels leftover stays 12px. vs-last leftover stays 11px. Receipt / description stay muted. Next stays the filled press on Victory. Do not put mw-house on the Victory dialog. Log set stays the sole filled press on compose. Finish / Skip / Swap / Form guide stay outline. Picker rewrite stays parked.
accept: npx tsx --test src/lib/victoryVolumeUnitCite.test.ts
test_written: yes

## progress

Honesty first (3 fail / 6 pass). Chrome after red. Accept green (34/34 including houseChrome). Walk 1440 localhost:3000 /active Finish: volume unit “reps” rgb(113, 113, 122) 12px house-lede house-victory-volume-unit. Stats labels leftover stays 12px. vs-last leftover “-1:18 vs last” stays 11px. Receipt stays muted. Next stays filled on poster-field rgb(173, 23, 0). Dialog is not mw-house. Picker rewrite stays parked.

Keep going after this leftover ships: one leftover per pass. Prefer remaining Victory first-paint muted (receipt, description). Not picker rewrite. Not You chrome.

## decisions

- One leftover: Victory volume unit cite is house leftover. Class-only.
- Stats labels leftover stays. Do not restyle it.
- vs-last leftover stays. Do not restyle it.
- Receipt / description / feel / share stay muted this leftover.
- Do not put mw-house on the Victory dialog. Stats strip already carries house-victory.
- Next stays the filled press on Victory (`primary-action`).
- Log set stays the sole filled press on compose.
- Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- ExercisePicker rewrite stays parked.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
- Founder said Continue.
