# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-form-guide-section-items
done_means: Form guide Setup / Execute / Avoid item lines under .mw-house are house leftover (--house-muted / house-lede). Breath leftover stays. Ready-position leftover stays. Section headings / marks / figure caption / Got it / copy stay. Finish / Skip / Swap / Form guide stay outline. Log set stays the sole filled press. After-set cites stay parked. ExercisePicker rewrite stays parked.
accept: npx tsx --test src/lib/formGuideSectionItemsCite.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Honesty red 3 fail / 6 pass, then class-only chrome.

Walk 1440 localhost:3000 /active (hydrated). Bench Form guide. Setup / Execute / Avoid items are house-lede rgb(113, 113, 122) 16px. Headings stay ink rgb(24, 24, 27). Breath leftover stays house-lede rgb(113, 113, 122). Ready leftover stays house-lede rgb(113, 113, 122). Got it / Form guide / Finish outline rgb(244, 244, 245). Log set rgb(24, 24, 27). Copy untouched.

Next leftover: pick the loudest non-parked first-paint fail on /active after this leftover ships. Same leftover pattern. Prefer Swap / Form guide / Check-in / live row. After-set cites (next-target / e1RM / vs-last / load-%) stay parked. Do not restyle ready-position, breath, Got it, or Log set. Not You chrome. ExercisePicker rewrite stays parked.

## decisions

- One leftover: Form guide section items is house leftover.
- Breath leftover stays. Ready-position leftover stays. Do not restyle them.
- Do not restyle section headings, marks, figure caption, Got it, or copy.
- Close / Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- Log set stays the sole filled press.
- After-set cites stay parked.
- ExercisePicker rewrite stays parked.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
