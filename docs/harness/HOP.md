# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-logconsole-log-set-press
done_means: LogConsole Log set is house leftover press (`house-btn-primary` / `--house-press` #18181b), not poster-red. Table Log set stays the same. Finish / Skip / Swap / Form guide / Repeat* never house-btn-primary.
accept: npx tsx --test src/lib/workout/logConsoleLogSetHousePress.test.ts
test_written: yes

## progress

Honesty test written first. Chrome on LogConsole Log set. Accept next. Walk Home left bar → Start → /active.

## decisions

- One leftover: LogConsole Log set press. Do not restyle kind chips / steppers.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
