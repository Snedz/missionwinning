# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-you
done_means: You first paint is quiet title + Account door. No identity kit, career, table, rewards, share, kits, or private note tour on /profile. Log set stays filled. generateWeek stays on /coach. Fuel stays the log. Move stays the quiet log.
accept: npx tsx --test src/lib/identity/leftoverYou.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Park leftover PR #889. Continue #890.

## decisions

- Tracker first, then leftover house rooms. You is quiet title + Account door, not an athlete-page tour.
- Identity widgets stay on disk; unmounted from first paint.
- Stamp `.1065` from this branch `.1064`.
- `[skip vercel]`. Do not merge. Do not promote. PRIVATE_MODE on. Live www stays `.696`.
