# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-victory-vs-last-cite
done_means: Victory vs-last cite under .mw-house.house-victory is house leftover (--house-muted / house-lede). Stats strip carries mw-house house-victory so the cite can paint. Do not put mw-house on the Victory dialog (Next contrast). Stats labels / receipt / description stay muted this leftover. Next stays the filled press on Victory. Log set stays the sole filled press on compose. Finish / Skip / Swap / Form guide stay outline. Picker rewrite stays parked.
accept: npx tsx --test src/lib/victoryVsLastCite.test.ts
test_written: yes

## progress

Honesty first (4 fail / 4 pass). Chrome after red. First put mw-house on the Victory dialog — Next went black-on-black; reverted. Stats strip carries mw-house house-victory instead. Accept green (25/25 including houseChrome). Walk 1440 localhost:3000 /active Finish: vs-last cite class paints rgb(113, 113, 122) 11px. Duration / Volume / Sets labels stay muted 12px. Receipt stays muted. Next stays field-manual filled on poster-field rgb(173, 23, 0). Dialog is not mw-house. Picker rewrite stays parked.

Keep going after this leftover ships: one leftover per pass. Prefer remaining Victory first-paint muted (stats labels, receipt, description). Not picker rewrite. Not You chrome.

## decisions

- One leftover: Victory vs-last cite is house leftover. Class-only.
- Stats strip carries `mw-house house-victory` so leftover CSS can paint. Do not put mw-house on the Victory dialog. Do not rewrite stats cards, receipt table, or Next this leftover.
- Stats Duration / Volume / Sets labels stay text-muted this leftover.
- Receipt / description / feel / share stay muted this leftover.
- Next stays the filled press on Victory (`primary-action`).
- Log set stays the sole filled press on compose.
- Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- ExercisePicker rewrite stays parked.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
- Founder said Continue after compose-table muted queue emptied.
