# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: today-date-start
done_means: /log first paint is date + session + one Start. No HouseFirstRoomsCard. No #today-week. No Generate on Today. Start still writeTodayComposeSession then /active. Log set stays filled on Train. generateWeek stays on /coach.
accept: npx tsx --test src/lib/today/todayDateStart.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Park leftover PR #889. Continue #890.

## decisions

- Lane A hop 1: Today is one Start, not a tour.
- Do not leftover-cite captions. Un-ratchet density requires in houseChrome.
- Not I-Day wall. Not Victory. Not a new design system. Not a letter.
- Stamp `.1059` from this branch `.1058`.
- `[skip vercel]`. Do not merge. Do not promote. PRIVATE_MODE on. Live www stays `.696`.
