# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-victory-receipt-prev
done_means: Victory receipt Prev cells under .mw-house.house-victory-receipt are house leftover (--house-muted / house-lede). Receipt heads leftover stays. Set index / vs-last cells stay muted this leftover. Description stays muted. Next stays the filled press on Victory. Do not put mw-house on the Victory dialog. Log set stays the sole filled press on compose. Finish / Skip / Swap / Form guide stay outline. Picker rewrite stays parked.
accept: npx tsx --test src/lib/victoryReceiptPrevCite.test.ts
test_written: yes

## progress

Honesty first (3 fail / 5 pass). Chrome after red. Accept green (42/42 including houseChrome). Walk 1440 localhost:3000 /active Finish: receipt Prev “8 reps” rgb(113, 113, 122) 14px house-lede house-victory-receipt-prev. Heads leftover stays 11px. Lead leftover stays 12px. vs-last cell “—” stays muted. Description stays muted. Next stays filled on poster-field rgb(173, 23, 0). Dialog is not mw-house. Picker rewrite stays parked.

Keep going after this leftover ships: one leftover per pass. Prefer remaining Victory first-paint muted (receipt vs-last cells, description). Not picker rewrite. Not You chrome.

## decisions

- One leftover: Victory receipt Prev cells is house leftover. Class-only.
- Receipt heads leftover stays. Do not restyle it.
- Set index / vs-last cells stay text-muted this leftover.
- Description / feel / share stay muted this leftover.
- Do not put mw-house on the Victory dialog. Receipt already carries house-victory-receipt.
- Next stays the filled press on Victory (`primary-action`).
- Log set stays the sole filled press on compose.
- Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- ExercisePicker rewrite stays parked.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
- Founder said Continue.
