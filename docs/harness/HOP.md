# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-plate-line-cite
done_means: Live barbell row plate-line cite (SetLogPlateLine / set-table-plates-line) under .mw-house is house leftover (--house-muted / house-lede), not text-muted-foreground. Plate math / row layout / Skip stay. Log set stays the sole filled press. After-set cites stay parked. Copy unchanged.
accept: npx tsx --test src/lib/setLogPlateLineCite.test.ts
test_written: yes

## progress

Honesty was red (3 fail / 4 pass). Chrome. Accept pass. Walk 1440: Home left second bar → Start → /active. Live barbell row plate-line cite (`20 + 2×20`) house-lede --house-muted rgb(113,113,122). Log set rgb(24,24,27). Finish / Skip / Swap stay outline. Copy untouched. Plate math / row layout untouched.

Next leftover: movement-history empty cite (tap the lift name from /active first paint; MovementHistorySheet empty still text-muted-foreground). Same leftover pattern. After-set cites (next-target / e1RM / vs-last / load-%) stay parked. Do not restyle the plate-line cite or Log set. Not You chrome.

## decisions

- One leftover: Plate-line cite on the live barbell row is house leftover.
- Cite is house-lede. Scoped `.house-compose-live .house-plate-line .house-lede` keeps inherit and `--house-muted`.
- Do not rewrite the plate math or the row layout.
- Do not restyle Log set. It stays the sole filled press.
- Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- After-set cites (next-target / e1RM / vs-last / load-%) stay parked.
- Check-in scale hints / value / lead stay done. Do not reopen.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
