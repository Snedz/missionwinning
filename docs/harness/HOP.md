# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-account-goals-card
done_means: Account first-paint goals card is house leftover, not Card. Goals textarea and Save stay. Finish / Skip / Swap / Form guide / Repeat* never house-btn-primary.
accept: npx tsx --test src/lib/accountGoalsCard.test.ts
test_written: yes

## progress

Honesty was red (3 fail / 1 pass). Chrome: goals card is house-card, not Card. Textarea and Save stay.

## decisions

- One leftover: Account first-paint goals card frame is house leftover.
- Do not restyle the goals textarea (border-2 stays this hop).
- Do not restyle Save goals button.
- Do not restyle muted hint this hop.
- Do not rewrite shared ExercisePicker.
- After-set cites stay parked.
- Calendar / charts / posters stay parked.
- Do not restyle shared EmptyState.
- Do not restyle Finish / Skip / Swap / Form guide / Repeat*.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
