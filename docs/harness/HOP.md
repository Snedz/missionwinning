# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-victory-receipt-lead
done_means: Victory receipt lead (“This session”) under .mw-house.house-victory-receipt is house leftover (--house-muted / house-lede). Receipt table / prev / vs-last cells stay muted this leftover. Description stays muted. Stats leftovers stay. Next stays the filled press on Victory. Do not put mw-house on the Victory dialog. Log set stays the sole filled press on compose. Finish / Skip / Swap / Form guide stay outline. Picker rewrite stays parked.
accept: npx tsx --test src/lib/victoryReceiptLeadCite.test.ts
test_written: yes

## progress

Honesty first (4 fail / 5 pass). Chrome after red. Accept green (35/35 including houseChrome). Walk 1440 localhost:3000 /active Finish: receipt lead “This session” rgb(113, 113, 122) 12px house-lede house-victory-receipt-lead, text-transform none. Receipt table heads stay uppercase muted. Prev stays muted. Description stays muted. Volume unit leftover stays 12px. Next stays filled on poster-field rgb(173, 23, 0). Dialog is not mw-house. Picker rewrite stays parked.

Keep going after this leftover ships: one leftover per pass. Prefer remaining Victory first-paint muted (receipt table, description). Not picker rewrite. Not You chrome.

## decisions

- One leftover: Victory receipt lead is house leftover. Class-only.
- Receipt section carries `mw-house house-victory-receipt` so leftover CSS can paint. Do not put mw-house on the Victory dialog.
- Receipt table heads / prev / set index / vs-last cells stay text-muted this leftover.
- Description / feel / share stay muted this leftover.
- Stats labels / volume unit / vs-last leftovers stay. Do not restyle them.
- Next stays the filled press on Victory (`primary-action`).
- Log set stays the sole filled press on compose.
- Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- ExercisePicker rewrite stays parked.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
- Founder said Continue.
