# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-set-table-in-set-pr
done_means: Completed-set in-set PR cite under .mw-house is house leftover (--house-muted / house-lede), not text-muted-foreground. vs-last / e1RM / next-target / load-% stay parked muted. RPE cite leftover stays. Chips / RPE10 / copy stay. Finish / Skip / Swap / Form guide stay outline. Log set stays the sole filled press.
accept: npx tsx --test src/lib/setTableInSetPrCite.test.ts
test_written: yes

## progress

Honesty was red (2 fail / 5 pass). Chrome. Accept pass. Walk 1440: Finish prior Bench 60×8 into History. Home left second bar Start → /active → Not now check-in → Swap Diamond Push-up to Bench Press → Log 70×8. In-set PR cite "✓Heaviest" house-lede --house-muted rgb(113,113,122) at 11px. vs-last "+10 kg" stays text-muted-foreground. Log set rgb(24,24,27). Finish / Skip / Swap / Form guide stay outline. Copy / ✓ mark untouched. RPE cite leftover stays.

Next leftover: pick the loudest non-parked first-paint fail on /active after this leftover ships. Same leftover pattern. Prefer Swap / Form guide / Check-in / live row. After-set cites (next-target / e1RM / vs-last / load-%) stay parked. Do not restyle vs-last, RPE cite, chips, or Log set. Not You chrome. ExercisePicker rewrite stays parked.

## decisions

- One leftover: Set-table in-set PR cite is house leftover.
- vs-last / e1RM / next-target / load-% stay parked. Do not restyle them.
- RPE cite leftover stays. Do not restyle it.
- Easy / Med / Hard chips stay house-state. RPE10 select stays house-num.
- Do not restyle copy. Keep the ✓ mark.
- Close / Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- Log set stays the sole filled press.
- Set-table RPE cite leftover stays. Do not reopen.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
