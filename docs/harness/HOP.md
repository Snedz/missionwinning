# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-movement-history-empty-cite
done_means: MovementHistorySheet empty cite (tap the lift name from /active first paint; movement-history-empty) under .mw-house is house leftover (--house-muted / house-lede), not text-muted-foreground. Sheet chrome / list rows / copy stay. Close stays house-btn. Log set stays the sole filled press. After-set cites stay parked.
accept: npx tsx --test src/lib/movementHistoryEmptyCite.test.ts
test_written: yes

## progress

Honesty was red (3 fail / 4 pass). Chrome. Accept pass. Walk pending.

## decisions

- One leftover: Movement-history empty cite is house leftover.
- Empty cite is house-lede. Scoped `.house-movement-sheet .house-lede` keeps inherit and `--house-muted`.
- Do not restyle the sheet chrome, list rows, or copy.
- Close / Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- Log set stays the sole filled press.
- After-set cites (next-target / e1RM / vs-last / load-%) stay parked.
- Plate-line cite / Check-in scale hints / value / lead stay done. Do not reopen.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
