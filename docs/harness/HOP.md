# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-set-upcoming-row
done_means: Train first-paint upcoming set row is house leftover, not text-muted. Finish / Skip / Swap / Form guide / Repeat* never house-btn-primary.
accept: npx tsx --test src/lib/workout/setTableUpcomingRow.test.ts
test_written: yes

## progress

Walked 1280: left Start href=/active (72+264) → /active Log set rgb(24, 24, 27). Finish/Swap/Form guide never house-btn-primary. Two upcoming rows house-set-wait, color rgb(113, 113, 122).

## decisions

- One leftover: Upcoming set row is house leftover.
- Do not restyle Finish / Skip / Swap / Form guide / Repeat*.
- Do not invent rooms. Calendar / charts / posters stay parked.
- Do not restyle shared EmptyState.
- Fuel / Mind / Learn / Track / Move chrome not restarted.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
