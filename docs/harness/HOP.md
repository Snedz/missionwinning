# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-set-table-load-pct-cite
done_means: Set-table load-% cite under .mw-house is house leftover (--house-muted / house-lede). next-cite leftover stays. Load-% cell stays house-num. Finish / Skip / Swap / Form guide stay outline. Log set stays the sole filled press. Picker rewrite stays parked.
accept: npx tsx --test src/lib/setTableLoadPctCite.test.ts
test_written: yes

## progress

Honesty first (3 fail / 4 pass). Chrome after red. Accept green (58/58 including houseChrome). Walk 1440 localhost:3000 /active hydrated: filled 70 kg + 80%, logged set. load-% cite “80%” rgb(113, 113, 122) 11px house-lede house-load-pct. Load-% cell stays house-num. Skip / Finish outline. Log set rgb(24, 24, 27). next-cite leftover stays 11px muted. Picker rewrite stays parked.

Keep going after this leftover ships: preferred first-paint muted queue is exhausted. ExercisePicker rewrite stays parked.

## decisions

- One leftover: Set-table load-% cite is house leftover. Class-only.
- next-cite leftover stays. Do not restyle it.
- Load-% cell (`set-table-load-pct`) stays house-num. Do not rewrite the input.
- Close / Finish / Skip / Swap / Form guide stay outline — never house-btn-primary.
- Log set stays the sole filled press.
- ExercisePicker rewrite stays parked.
- Stamp stays `.1058`. Not a letter. Do not run harness:done.
- Founder said keep going without Continue.
