# Live hop

The open hop only. Not CONTEXT, not GRAPH_LOOP, not LOG.
Write this **before** the first product edit. Clear it back to this
template after the row is marked `done`. `npm run harness:done` is the
closer — the agent's last message is not.

ticket: leftover-learn
done_means: Learn first paint is the intro. No other paths, guidebook, sample workout, or premium tour on /learn. QuietLearnIntroCard stays the intro. Log set stays filled. generateWeek stays on /coach. Fuel stays the log. Move stays the quiet log. Mind stays the check-in. Track stays the scale/tape log.
accept: npx tsx --test src/lib/learn/leftoverLearn.test.ts
test_written: yes

## progress

Honesty first. Chrome after red. Park leftover PR #889. Continue #890.

## decisions

- Tracker first, then leftover house rooms. Learn is the intro, not a paths tour.
- Other paths / guidebook / sample / premium stay off first paint (unmounted, not folded).
- Stamp `.1069` from this branch `.1068`.
- `[skip vercel]`. Do not merge. Do not promote. PRIVATE_MODE on. Live www stays `.696`.
