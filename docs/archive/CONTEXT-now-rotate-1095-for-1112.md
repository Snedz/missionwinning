# CONTEXT `## Now` bullet rotated 2026-09-24 for `.1112`

Superseded live bullet. The full record is [LOG.md](../archive/log/LOG-rotate-1095-for-1110.md).

- **`.1095`:** (`2026.07-unified.1095`) **Dual-mount photos isolation** — Two live minis with photos scope each see only their own stub snapshot via `photos.read`. Injecting / mutating A does not change B.read. Unscoped still `scope_denied` on read / write (`.1080`). Storage (`.1092`), identity (`.1093`), and billing (`.1094`) isolation stay. No UI / Stripe / camera. Paper only. `[skip vercel]`. No tip-promote. Live www stays `.697`. PRIVATE_MODE stays.
