# Rotated from LOG.md for `.905`

## 2026-08-17 — Today is Summary: Start, not First Steps (`.890`)

Basic Today opened with a tour card above the one action that
counts. Start already said what to do. The card was the six-room
house on the first screen.

**Ship:** `firstStepsMayMount` is off. Neither Today shell mounts
`FirstStepsCard`. Checklist stays in More. Re-entry quiet line stays.
One poster-red Start.

Label `.890` (stacked on `.889`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-872-for-890.md](docs/archive/log/LOG-rotate-872-for-890.md).
