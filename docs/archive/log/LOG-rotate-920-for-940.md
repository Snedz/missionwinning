# Rotated from LOG.md for `.940`

Oldest live entry moved when Strong CSV import (`.940`) shipped.

## 2026-08-18 — Adjust session is Adjust today’s session (`.920`)

The sheet defaulted a straight apostrophe. Packs use a
curly one. First paint listed it after Unlock waitlist.

**Ship:** defaultValue matches the pack. Same sheet.
Same four adjustments.

Label `.920` (stacked on `.919`).

Rotated LOG oldest → [docs/archive/log/LOG-rotate-905-for-920.md](docs/archive/log/LOG-rotate-905-for-920.md).
