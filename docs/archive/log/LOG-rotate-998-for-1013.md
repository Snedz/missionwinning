## 2026-08-25 — Live session reorder (`.998`)

Swap/skip `.959` replaces a movement.
Session-Start `.991` replays last
order. The live list could not be
dragged.

**Ship:** in the live Train session
they can drag the lifts (or move
them up / down) to a new order.
Sets travel with the card. Tap the
name still opens this-movement
history. Later lifts stay off the
board until first set. Guest. First
set ungated. Honesty `.971` still
applies. Today still one Start.
Resume `.963` kept. History Edit
`.997` stays on History. Note+pin
`.996` / rest `.995` / set-row type
`.994` / history `.993` / custom
`.992` / Start this again `.991`
stay. Swap/skip stay the replace
path. `/private` stays the tight
lock. No `PRIVATE_MODE` flip.

Label `2026.07-unified.998` (past
master `.997` / `bb13642b`). Live
www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-978-for-998.md](docs/archive/log/LOG-rotate-978-for-998.md).
