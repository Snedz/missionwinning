# Rotated from LOG.md for `.921`

## 2026-08-17 — Start session is Chest, not Just Go (`.906`)

Start opened a session branded Just Go — Chest. The work
is Chest. Notes names the session what it is.

**Ship:** freestyle Start names the focus group. Coach
keeps the plan name. Same tap, same sets.

Label `.906` (stacked on `.905`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-891-for-906.md](LOG-rotate-891-for-906.md).
