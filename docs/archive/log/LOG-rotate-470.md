## 2026-08-04 — Hand-tuned form stick figures (`.455`)

Instructional form SVGs rebuilt from explicit poses (`scripts/form-kit/poses.mjs`) instead of parametric lean math that drew limbs through the head. All 59 guides regenerated; pattern art diverges from exercise twins (push = plank press, pull = hinged row, isolation = raise) so uniqueness dice stays under threshold. Thruster / push-up / deadlift read as distinct three-phase diagrams again.

Mutants: re-clone bicep-curl to pattern-isolation geometry → uniqueness red; zero-length shoulder default in stick kit → garbage limbs (covered by kit rules).
