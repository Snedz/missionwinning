# Rotated from LOG.md when `.870` landed

## 2026-08-16 — Cohort numbers declare their frame (`.855`)

H-09: a week-4 figure without saying who could have appeared is unsigned.
`frameCohort` is the declaration; the discover guard is the enforcer; `unknown=yes`
must stay in the formatted line. X-09 exists because that enforcer exists.

**Ship:** `selectionFrame.ts` · wired through `computeWeek4Retention` and the
founder digest. RPC frame is workout_logs (not working-set weeks) — named,
not implied.

Label `.855` (onto master `.854`).

Excellence-Override: metrics instrument (no athlete surface; RESULT unscored)

Rotated LOG oldest → [docs/archive/log/LOG-rotate-840-for-855.md](LOG-rotate-840-for-855.md).
