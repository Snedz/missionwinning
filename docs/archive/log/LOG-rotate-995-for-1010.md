## 2026-08-25 — Per-exercise rest (`.995`)

Global rest, last-rest recall, drop
rest-zero `.986`, and EMOM already
existed. Last-rest was one number per
lift. Warmup and work shared it. A 90s
global swallowed laterals.

**Ship:** rest duration lives on the
exercise. Two lanes — warmup and work —
settable on the open Train lift.
Bench can rest 3:00 while laterals
rest 1:00. Warmup never inherits the
work 3:00. Named hint wins. Drop still
zeros. EMOM still skips auto rest.
Note+pin holds. Set-row type `.994`
still honors weight · bodyweight ·
duration · assisted. Global Default
stays the dock fallback — not a
second home. Empty invents nothing.
Honesty `.971` still applies. Optional.
Guest. First set ungated. Today still
one Start. Resume `.963` kept. History
`.993` / custom `.992` / Start this
again `.991` / Track trend `.989` /
EMOM `.988` / drop-set `.986` /
warmup `.985` / notes `.983` / 1RM
`.981` / supersets `.980` / Learn
`.978` / week strip `.977` stay.
`/private` stays the tight lock. No
`PRIVATE_MODE` flip.

Label `2026.07-unified.995` (past
master `.994` / `bc03b98e`). Live www
stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-974-for-995.md](LOG-rotate-974-for-995.md).
