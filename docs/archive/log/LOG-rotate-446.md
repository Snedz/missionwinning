## 2026-08-04 — ActiveExerciseHeader peel (`.431`)

Title / load chip / More menu / next line / swap / notes peel into `ActiveExerciseHeader`. Card is mount + sets + footer (**343→224**). Wiring guards follow header/footer pattern.

Mutants: leave MoreMenu import on card → wiring red; drop Header mount → wiring red.
