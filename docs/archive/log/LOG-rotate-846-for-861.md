# Rotated from LOG.md when `.861` landed

## 2026-08-15 — The Idea Loop, the third organ (`.846`)

This repo had two of the three organs of an autonomous development system.
`GRAPH_LOOP` decides what gets built next; `GAUNTLET_LOOP` decides whether it is
good enough. Nothing decided what deserves to exist, and the queue ran roughly
sixteen consecutive first-paint copy-drift rows behind the words "Do not invent X2."

**Ship:** [docs/IDEA_LOOP.md](docs/IDEA_LOOP.md) + [docs/mechanics/](docs/mechanics/INDEX.md)
+ `src/lib/ideaGraph/` + `npm run idea:validate|next|pack` + gate step 4. Prose for
the roles, code for the selector. A mechanic is never recorded as a feature.
`idea:next` emits `H-01` (power the week-4 measurement chain). Does not paste a
GRAPH_LOOP row — that remains a human/spawn action. Rebased past `.845`.

Label `.846` (onto master `.845`).

Excellence-Override: idea graph is development tooling (`src/lib/ideaGraph`; RESULT unscored)

Rotated LOG oldest → [docs/archive/log/LOG-rotate-831-for-846.md](LOG-rotate-831-for-846.md).
