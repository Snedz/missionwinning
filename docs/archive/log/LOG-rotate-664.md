## 2026-08-10 — Assessments stage labels honesty (`.649`)

CX walk: Stage of Change chips used i18n keys `stagePre` / `stageCont` / … that **do not exist** in locale packs (real keys are `assessStagePreName` etc.), so athletes always saw raw machine tokens. Focus defaulted to `''` (blank); questions used `defaultValue: qKey`. Floors + correct name keys.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-649.md](docs/archive/log/LOG-rotate-649.md).

