# Rotated from LOG.md for `.970`

## 2026-08-24 — Plate math on the free set row (`.950`)

The greedy plate helper already existed. The live
row printed per-side `45 + 45 / side` and always
used the unit default bar.

**Ship:** after a barbell weight, a skippable line
shows both-sides counts (`45 + 2×45`). Bar defaults
to 45 lb / 20 kg and is editable. Empty or 0 invents
no plates. Log a set never waits. Today untouched.

Label `.950` (past master `.949`; planned as `.948`). No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-929-for-950.md](docs/archive/log/LOG-rotate-929-for-950.md).
