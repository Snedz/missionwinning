# Rotated from LOG.md for `.960`

## 2026-08-24 — E-Victory receipt: vs-last same shape (`.944`)

Victory after Finish already existed. Session
totals compared last time by workout name, so a
renamed week stayed quiet and a same-named cardio
day invented a vs-last.

**Ship:** session vs-last matches sorted unique
lift ids. Empty history stays empty — no fake PR.
One prior same-shape session fills the receipt.
Sheet reused. Today untouched. No feed.

Label `.944` (stacked on `.943`).

Rotated LOG oldest → [docs/archive/log/LOG-rotate-924-for-944.md](./LOG-rotate-924-for-944.md).
