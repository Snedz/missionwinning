## 2026-08-13 — Repeat last session from the log (`.747`)

Strong/Hevy migrants live on copying the last completed session. History already
had “Train this again” (`templateFromCompletedLog`); Today and Train empty Start
seeded Just Go instead. One control, not a template marketplace.

**Ship:** `repeatLastSessionTemplate` wraps the existing mapper (newest-first,
skip tombstones). Train empty Start copies that log or opens a blank logger.
Today primary: resume → live Coach (honesty unchanged) → Repeat last → Just Go.
Prefills compose with `getLastPerformanceForSet` / `resolveSetInput` (F-013/#489
untouched). No rest auto-start, no Coach on this control, no account/network,
no guilt copy when history is empty. Set-log table is first paint when exercises
are copied.

Label `.747` (onto master `.746`). Originally reserved `.717`; landed as `.747` past master `.746`.
Excellence-Override below.

Excellence-Override: repeat last session

Rotated LOG oldest → [docs/archive/log/LOG-rotate-685-for-747.md](docs/archive/log/LOG-rotate-685-for-747.md). · [`.746` for `.761`](docs/archive/log/LOG-rotate-746-for-761.md).
