## 2026-08-04 — Active table set controls pure extract (`.408`)

`resolveActiveTableSetControls` projects desktop table dial + kind for the open
set; `volumeTrimToastCopy` owns volume-trim toast i18n keys (T2.1).
