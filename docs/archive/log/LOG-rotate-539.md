# Rotated for .539

## 2026-08-05 — Kaizen: badge desc EN + HistoryDay formatLocalDateKey (`.524`)

EN `rewardBadge*Desc` strings for Profile shelf. HistoryDayPage drops page-local formatDay — shared `formatLocalDateKey` with long-date opts.

Mutants: HistoryDayPage function formatDay → red; strip rewardBadgeFirstBloodDesc → red.

