# Rotated from LOG.md for `.929`

## 2026-08-17 — Today Show all holds Coach week (`.914`)

After Summary, Coach week lived only on the orphaned
dashboard. Lean had no house door.

**Ship:** Show all on Lean mounts the existing week strip
(dose + compact adapt). First paint stays date, pins, one
sentence, one Start. Label is Show all, not the Fuel pack.

Label `.914` (stacked on `.913`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-899-for-914.md](LOG-rotate-899-for-914.md).
