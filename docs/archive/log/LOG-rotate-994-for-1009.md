## 2026-08-25 — Set-row type (`.994`)

Custom `.992` names a movement.
Plus-load already prints BW / vest.
The open set row still spoke kg ×
reps. Pull-ups got a fake load.
Planks got BW × reps instead of
time. Assisted was not a type.

**Ship:** the open Train row honors
the exercise — weight · bodyweight
reps · duration · assisted. They
type reps or time. Optional +kg
vest (extra only — never Track /
profile bodyweight). Empty / custom
leftover stays weight × reps. Guest.
First set ungated. Honesty `.971`
still scores Train. Diary stays
free. Today still one Start.
Resume `.963` kept. This-movement
`.993` / custom `.992` / Start this
again `.991` / Track trend `.989` /
EMOM `.988` / drop-set `.986` /
warmup `.985` / notes `.983` / 1RM
`.981` / supersets `.980` / Learn
`.978` / week strip `.977` / Track
`.976` stay. `/private` stays the
tight lock. No `PRIVATE_MODE` flip.

Label `2026.07-unified.994` (past
master `.993` / `75350127`). Live
www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-973-for-994.md](LOG-rotate-973-for-994.md).
