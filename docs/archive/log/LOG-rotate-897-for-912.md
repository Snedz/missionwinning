# Rotated from LOG.md for `.912`

## 2026-08-17 — History first paint is the log (`.897`)

History was four tabs, a glance card, charts, journal, and
pillar wins above the rows. The job is find a session and open it.

**Ship:** first paint is search + the list. Calendar, charts,
journal, glance, and pillar wins live in Show all. Same logs.

Label `.897` (stacked on `.896`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-882-for-897.md](docs/archive/log/LOG-rotate-882-for-897.md).
