# Rotated for .521

## 2026-08-05 — Full-launch honesty: leaderboard + free-beta dual mode (`.506`)

FL3: `/leaderboard` in zero-state + a11y sweeps; clamp class scope when America parked so empty CTA cannot 404; freeBetaDualMode pins depth unlock (usePremium / premiumServer) alongside mute pay. Comments updated for unparked board.

Mutants: class EmptyState href=/america while america parked → red; free-beta unlock without isFreeBetaPremiumUnlocked in usePremium → red.

