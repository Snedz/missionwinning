# Rotated from LOG.md for `.1030` (Copy this session onto another day)

## 2026-08-26 — Next cite is BW, not 0 kg, on assisted 0 (`.1015`)

BW cite `.1009` closed vest-0.
Assisted 0 still printed `8 × 0 kg`.

**Ship:** live Next, Last ghost, and
after-complete cite unassisted help as
`8 × BW`. Help still `8 × −20 kg`.
Missing invents nothing. Display only —
store still `weight: 0`. Reuses
`formatSetRowLine` / `formatSetRowPrev`.
Custom prefix stays weight. Guest.
First set ungated. Today still one
Start. Duration cite `.1014` stays.
`/private` stays the tight lock. No
`PRIVATE_MODE` flip.

Label `2026.07-unified.1015` (from
master `.1014` / `f4b852279`). Stamp
stays `.1015`. Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-1000-for-1015.md](docs/archive/log/LOG-rotate-1000-for-1015.md).
