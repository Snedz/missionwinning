# Rotated from LOG.md when `.810` landed

## 2026-08-14 — Astro `/about` only; Next keeps the host (`.795`)

`sites/www` had index, start, week. No about page. G3 is that page,
additive. Next keeps `www.missionwinning.com`, the ~250 SEO URLs, and
Next `/about`. No `_redirects`. No national-fitness track.

**Ship:** `about.astro` + `aboutContent.ts`. Same English the Next about
already paints. Terminal CTA. Link home. No `PRIVATE_MODE` flip.

Label `.795` (onto master `.794`).

Excellence-Override: commissioned www about page (surface; RESULT unscored)
