# Rotated from LOG.md for `.1021` (Coach chat empty load is BW, not 0)

## 2026-08-25 — Restore a deleted session (`.1006`)

`.1003` tombstones a bogus Monday.
Copy still said it cannot be recovered.
Missing: undo.

**Ship:** Restore on History. Delete
stays confirm-gated. After delete the
detail stays on the tombstone with
Restore. Overflow **Deleted sessions**
when any tombstone exists. Empty /
not-deleted / missing invents nothing.
Do not undelete a live session. Not
wipe-account. Revision wins, including
clearing `deletedAt`, so restore
survives a cloud pull. Guest. First set
ungated. Honesty `.971` still scores
Train. Today still one Start. Resume
`.963` kept. Start-from `.1005` / Hide
`.1004` / Delete `.1003` / Merge `.1002`
/ Pause `.1001` / Backfill `.1000` stay.
`/private` stays the tight lock. No
`PRIVATE_MODE` flip.

Label `2026.07-unified.1006` (from master
`.1005` / `a6856d74`). Stamp stays
`.1006`. Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-991-for-1006.md](docs/archive/log/LOG-rotate-991-for-1006.md).
