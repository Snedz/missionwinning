# Rotated from LOG.md for `.886`

## 2026-08-16 — A hop is done when a command says so (`.868`)

The agent saying "done" was the default closer. Mid-hop state lived in
`~/.grok` on one machine. A fresh spawn after compaction could not see
the ticket, the one-sentence done, or the accept command.

**Ship:** `docs/graph/HOP.md` · `PINNED.md` · `npm run graph:done`.
Does not write the queue. Empty template stays on master.

Label `.868` (onto master `.867`).

Excellence-Override: graph hop contract (tooling; RESULT unscored)

Rotated LOG oldest → [docs/archive/log/LOG-rotate-853-for-868.md](LOG-rotate-853-for-868.md).
