# Rotated from LOG.md when `.867` landed

## 2026-08-16 — H-01 is on the queue (`.852`)

A harvest that only prints is not a baton. Founder `/graph --paste` put
`IL-H-01` on GRAPH_LOOP as Now AN — not AM2, not a seventeenth letter.

The ratchet notes already said a harvest row closes the 16-run. The counter
did not see `IL-*`, so the paste would have been red. `isHarvestRow` is that
exception in the same place as the count.

**Ship:** GRAPH_LOOP AN · `src/lib/loopQueue/route.ts` · LEDGER harvest-2
(emit 1). `npm run graph` now names the build ticket. The week-4 chain is
hop 2.

**4 mutants killed** — `IL-` counted as a letter · live ticket still harvest ·
run after paste still 16 · a letter after the closer reported 17.

Label `.852` (onto master `.851`).

Excellence-Override: queue + router, no product surface (RESULT unscored)
