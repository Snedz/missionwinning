# Rotated from LOG.md for `.1019` (Never-trained is not overdue)

## 2026-08-25 — Hide this exercise from the library (`.1004`)

Custom + merge-dupes still leave a
noisy Add list. Strong leftover:
swipe-hide a movement from the
library. Hidden is not deleted.

**Ship:** hide a name from Library
or the Train overflow. It stops
appearing in Add / search / picker.
Past sets, PRs, notes stay. A hidden
movement already in a live session
stays on that session. Unhide
restores the list. Empty / missing /
already-hidden invents nothing. Do
not auto-hide lookalikes. Guest.
First set ungated. Honesty `.971`
still scores Train session count.
Today still one Start. Resume `.963`
kept. Delete `.1003` / Merge `.1002` / Pause `.1001`
/ Backfill `.1000` / in-set PR
`.999` / Reorder `.998` / History
Edit `.997` stay. `/private` stays
the tight lock. No `PRIVATE_MODE`
flip.

Label `2026.07-unified.1004` (rebased onto
master `.1003` / `4c810d57`). Stamp stays
`.1004`. Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-988-for-1004.md](docs/archive/log/LOG-rotate-988-for-1004.md).
