# Rotated from LOG.md for `.1016` (this session as a file they own)

## 2026-08-25 — Pause the live clock (`.1001`)

Resume `.963` keeps the set when
they leave Today. The session
clock still ran through a phone
call. Receipt duration has to be
true.

**Ship:** they can pause and
unpause the live session clock
(tap elapsed on the Train bar).
Pause stops accumulating.
Resume clock starts again from
the paused total. Finish writes
`durationSeconds` from
elapsed-while-running only.
Leaving Today does not auto-pause
— that is Resume, keep the set.
Rest `.995` and EMOM `.988` stay
independent. Empty invents
nothing. Guest. First set
ungated. Pause never paywalls.
Today still one Start. In-set PR
`.999` / reorder `.998` / History
Edit `.997` / note+pin `.996` /
rest `.995` / set-row type `.994`
/ history `.993` stay. `/private`
stays the tight lock. No
`PRIVATE_MODE` flip.

Label `2026.07-unified.1001` (rebased
onto master `.1000` / `1ab24514`).
Stamp stays `.1001`. Live www stays
`.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-983-for-1001.md](docs/archive/log/LOG-rotate-983-for-1001.md).
