# Rotated from LOG.md for `.927`

## 2026-08-17 — Today is Summary after the first log (`.912`)

The first workout used to swap Today onto the dashboard
house. Summary is date, pins, one sentence, one Start.

**Ship:** `/log` is Lean Summary in every phase. Dashboard
is not this route. Same Start. Same pins.

Label `.912` (stacked on `.911`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-897-for-912.md](LOG-rotate-897-for-912.md).
