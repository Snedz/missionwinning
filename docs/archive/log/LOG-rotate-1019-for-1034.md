# Rotated from LOG.md for `.1034` (Reorder lifts on this finished session)

## 2026-08-26 — Never-trained is not overdue (`.1019`)

Anatomy painted `daysSince >= 7`
as overdue. Never-trained is
`days = 99`, so Chest they never
logged read as a missed session.

**Ship:** anatomy overdue only if
days ≥ 7 and days is not the 99
never-trained sentinel. Window
session count is not the trained
flag (14-day empty still overdue
if they trained that group).
Never-trained stays idle — not
red, not “· overdue”. Score keeps
the 99 sentinel. Grid heatmap idle
stays. Guest. First set ungated.
Today still one Start. Month they
own `.1018` stays. Empty-load
`.1017` stays. `/private` stays the
tight lock. No `PRIVATE_MODE` flip.

Label `2026.07-unified.1019` (from
master `.1018` / `b376d4ecd`).
Stamp stays `.1019`. Live www
stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-1004-for-1019.md](docs/archive/log/LOG-rotate-1004-for-1019.md).
