# Rotated for .523

## 2026-08-05 — Kaizen: parked America escape hatches (`.508`)

`americaHomeOrFallback()` — join fail / empty class share / teacher back / PFT national CTA never hard-code parked `/america`. Fallback `/benchmarks` while track off. Source guard `parkedSurfaceLinks.test.ts`.

Mutants: JoinClass bare `/america` → red; FitnessTestRunner push /america ungated → red.

