# Rotated from LOG.md for `.1024` (History empty-load volume is reps, not 0 kg)

## 2026-08-25 — Next cite is BW, not 0 kg (`.1009`)

Victory already says 8 reps.
Prev already says BW. The open
lift still interpolated raw
`weight: 0` as Next: 8 × 0 kg.

**Ship:** live Next, Last ghost,
and after-complete cite print BW
(or BW + vest), not 0 kg. Loaded
stays kg. Empty next / no ghost
invents nothing. Display only.
Reuse `formatSetRowLine`. Guest.
First set ungated. Today still
one Start. Search `.1008` stays.
`/private` stays the tight lock.
No `PRIVATE_MODE` flip.

Label `2026.07-unified.1009` (from
master `.1008` / `727c6f484`). Stamp
stays `.1009`. Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-994-for-1009.md](docs/archive/log/LOG-rotate-994-for-1009.md).
