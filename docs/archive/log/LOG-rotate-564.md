# Rotated for .564

## 2026-08-06 — Kaizen: set-complete row + sheet primary consistency (`.549`)

SetLogRow: primary edge + aria + check when logged. Session check-in Save uses primary-action; Skip border-border. Add exercise sheet same CTA pattern.

Mutants: SetLogRow without data-set-complete → red; check-in without primary-action → red.

