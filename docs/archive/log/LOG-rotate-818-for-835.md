# Rotated from LOG.md when `.835` landed

## 2026-08-14 — Coach first-paint matches the pack (`.818`)

`/coach` first-painted a different subtitle, then the EN pack.
One empty title still said Ready for a new week.

**Ship:** subtitle + empty title + free-core. Not Today card. Drift 195 → 192.

Label `.818` (onto master `.817`).

Excellence-Override: Coach first-paint (surface; RESULT unscored)
