# Rotated from LOG.md for `.928`

## 2026-08-17 — Today pin Edit is Edit (`.913`)

Pins reused the header key and the dashboard lecture
kicker. Packs won, so first paint said Edit Today and
Recommended Starting Point.

**Ship:** `todayPinsEdit` is Edit. `todayPinStartKicker`
is Today's session. Header still owns Edit Today. Same
pins.

Label `.913` (stacked on `.912`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-898-for-913.md](LOG-rotate-898-for-913.md).
