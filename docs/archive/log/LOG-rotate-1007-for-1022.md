# Rotated from LOG.md for `.1022` (Heatmap empty-load volume is reps, not 0)

## 2026-08-25 — Name this finished session (`.1007`)

Untitled date rows are how they
delete the wrong Monday. Strong
names the template. We name the log.

**Ship:** private title on the History
row / receipt. Empty title is the
date. Does not rename the template.
Empty / missing / tombstone / live
invents nothing. Not a Feed. Guest.
First set ungated. Today still one
Start. Restore `.1006` stays.
`/private` stays the tight lock. No
`PRIVATE_MODE` flip.

Label `2026.07-unified.1007` (from master
`.1006` / `a913ffae`). Stamp stays
`.1007`. Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-992-for-1007.md](docs/archive/log/LOG-rotate-992-for-1007.md).
