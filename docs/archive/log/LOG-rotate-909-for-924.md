# Rotated from LOG.md for `.924`

## 2026-08-17 — Add exercise is after the table (`.909`)

A full-width Add exercise sat above the first set. The job
is log the set. Notes adds at the end of the list.

**Ship:** compact Add exercise is under the table. Header
stays name + Finish. Same sheet. Desktop inline unchanged.

Label `.909` (stacked on `.908`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-894-for-909.md](LOG-rotate-894-for-909.md).
