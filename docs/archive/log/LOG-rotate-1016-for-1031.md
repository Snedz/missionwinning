# Rotated from LOG.md for `.1031` (This month on the History calendar)

## 2026-08-26 — This session as a file they own (`.1016`)

Export this diary writes the whole
live diary. Tuesday's receipt is
still trapped in History.

**Ship:** History detail saves THAT
session as a local CSV (JSON if the
same rows). Reuses `decideExportDiary`
columns. The file re-imports via
`.1013`. Empty / missing / tomb
invents nothing — Save disabled.
Honest logged columns only. No
public URL, Feed, share, email, or
clipboard permalink. History only.
Guest. First set ungated. Today
still one Start. Assisted cite
`.1015` stays. `/private` stays the
tight lock. No `PRIVATE_MODE` flip.

Label `2026.07-unified.1016` (from
master `.1015` / `4046afd9`). Stamp
stays `.1016`. Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-1001-for-1016.md](docs/archive/log/LOG-rotate-1001-for-1016.md).
