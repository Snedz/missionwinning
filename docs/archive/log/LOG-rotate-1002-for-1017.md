# Rotated from LOG.md for `.1017` (Last cite is BW, not 0, on empty load)

## 2026-08-25 — Merge duplicate exercises (`.1002`)

Custom + history + PR split when
they logged the same movement under
two names. Strong leftover: Transfer
Exercise Data. They pick a source and
a keeper. Confirm. Cannot undo.

**Ship:** History / library door —
pick source and keeper, confirm, then
history / PRs / notes / tags / rest /
1RM / in-set PR diary ride the keeper.
Empty / same-id / missing invents
nothing. Do not auto-merge lookalikes.
Live session: source becomes the
keeper, or vanishes if the keeper is
already there — sets travel. Confirm
copy says it cannot be undone. Guest.
First set ungated. Honesty `.971`
still scores Train session count.
Today still one Start. Resume `.963`
kept. Pause `.1001` / Backfill `.1000`
/ in-set PR `.999` / Reorder `.998` /
History Edit `.997` / note+pin `.996`
/ rest `.995` / set-row type `.994` /
history `.993` / custom `.992` stay.
`/private` stays the tight lock. No
`PRIVATE_MODE` flip.

Label `2026.07-unified.1002` (rebased
onto master `.1001` / `91938fc4`).
Stamp stays `.1002`. Live www stays
`.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-985-for-1002.md](docs/archive/log/LOG-rotate-985-for-1002.md).
