## 2026-08-04 — NL pair + dab/bit + bare tbsp (`.443`)

`a pair of eggs` → qty 2; literal `dab`/`bit` join the tsp dab scale; bare `tablespoon`/`tbsp` without a digit uses the 1-tbsp (0.5×) scale. Continues Fuel estimate accuracy after couple/few.

Mutants: drop pair → 1 egg → red; omit dab from dab regex → full oil → red; bare tbsp stays full serving → red.

