# Rotated from LOG.md when `.819` landed

## 2026-08-14 — In-app copy is Alpha, not Open beta (`.804`)

The stamp is Mission Winning Alpha 0.1.0. Profile, Today, Coach,
Fuel, Move, Learn still said “open beta.” `isFreeBeta()` is mute-pay,
not the name.

**Ship:** defaultValues + EN locale floors → Alpha. Honesty test.
Flag unchanged.

Label `.804` (onto master `.803`).

Excellence-Override: Alpha copy leftover (surface; RESULT unscored)
