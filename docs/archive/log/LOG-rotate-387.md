## 2026-08-04 — Button input dialog oauth chrome solidify (`.372`)

Kaizen Loop 23 G3. Soft chrome leftovers:

1. Button / Input / Dialog / OtpInput drop `rounded-md`/`sm:rounded-lg` for `rounded-none`
2. OAuth button chrome: solid `border-border` / `bg-background` / hover borders (drop opacity tints)

Living log Loop 23 closed. Cap 16.
