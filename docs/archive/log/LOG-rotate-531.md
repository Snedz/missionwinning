# Rotated for .531

## 2026-08-05 — Kaizen: challenge Done XP hint from catalog (`.516`)

Weekly challenge Done line uses `XP_BY_ACTION.challenge_complete` (not a hard-coded 75) so catalog remains SoT. EN locale key updated.

Mutants: hard-coded +75 without XP_BY_ACTION → red.

