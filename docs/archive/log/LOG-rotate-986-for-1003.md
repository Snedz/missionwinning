# Rotated LOG — Drop-set rest-zero (`.986`)

Rotated 2026-08-25 for Delete this finished session (`.1003`).
Full record from [LOG.md](../../LOG.md).

## 2026-08-25 — Drop-set rest-zero (`.986`)

Tags already mark a set as drop. A drop
series is performed without resting.
Missing: after a drop-tagged set the rest
timer still ran — leftover 2:00 from the
work set, or a new start. That is a lie.

**Ship:** log a drop-tagged set does not
start rest; a running timer goes to zero.
Tagging the next incomplete set drop stops
rest the same way Start drop already did.
A working set still starts rest. Warmup
stays warmup (out of cites). No new tag
taxonomy. Optional. Guest. First set
ungated. Empty invents nothing. Today
still one Start. Resume `.963` kept.
Warmup `.985` / notes `.983` / 1RM `.981`
/ supersets `.980` / Learn `.978` / week
strip `.977` / Track `.976` / Move `.974`
/ cues `.973` / honesty `.971` / tags
`.970` / RPE `.967` stay. `/private`
stays the tight lock. No `PRIVATE_MODE`
flip.

Label `.986` (past master `.985` /
`8a9fe41f`). Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-961-for-986.md](LOG-rotate-961-for-986.md).
