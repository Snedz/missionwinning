# Rotated for .514

## 2026-08-05 — Exercise public CTA layout + form pack demotes (`.498`)

Desktop exercise library orange strip: `.primary-action` is `w-full` by default — forced full-measure beside copy. Fixed with `sm:w-auto` + ink on tint. Form eyes-on: demote `overhead-press` (single-arm wrong exercise) and `pull-ups` (feet cropped) from `FORM_PACK_SIDE_IDS` → SVG fallback; files kept for regen. `MOVEMENT_STANDARDS.md` — CrossFit demos as **reference-only** validation, no CF embeds.

Mutants: strip link without sm:w-auto → red; re-add OHP to SIDE_IDS without still PASS → wrong teaching returns.


