# Rotated for .518

## 2026-08-05 — Kaizen K6/K7: no soft-Coach boss + History train again (`.502`)

K6: basic-complete fallthrough no longer primaries Mission Coach — uses readiness train path (Flow-6). K7: pure `templateFromCompletedLog` + History detail **Train this again** → startWorkout / resume active if logged work; analytics `history_train_again`.

Mutants: Open Mission Coach in getNextAction basic branch → red; History without templateFromCompletedLog → red.

