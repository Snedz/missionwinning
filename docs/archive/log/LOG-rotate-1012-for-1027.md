# Rotated from LOG.md for `.1027` (Move this session to another day)

## 2026-08-25 — This-movement title is the date or the name (`.1012`)

History already says the date or
the name they typed. Tap the open
lift and the sheet still bolded
the template.

**Ship:** this-movement title is
`historySessionLabel`. Untitled is
the date. Named is the private
title. Template is subtitle only
when it differs. Empty invents
nothing. Guest. First set ungated.
Today still one Start. Export
`.1011` stays. `/private` stays the
tight lock. No `PRIVATE_MODE` flip.

Label `2026.07-unified.1012` (from
master `.1011` / `8df323949`). Stamp
stays `.1012`. Live www stays `.696`.

Rotated LOG oldest → [LOG-rotate-997-for-1012.md](LOG-rotate-997-for-1012.md).
