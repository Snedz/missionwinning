## 2026-08-13 — Exercise notes on the set log (`.748`)

Strong buries the cue. The free logger already stored a per-exercise `note` and
could read last time’s line (`lastNotesFor`) — but the field sat behind overflow
and never prefilled. The athlete retyped “belt on 3” or never saw it.

**Ship:** appearance-time seed (`startWorkout` / add / swap) from the last
persisted cue; `''` is sticky this session and is not treated as unset. Always-
visible one-line field **after** the set rows so load / reps / RPE keep first
paint. No AI notes, no feed, no Top 8. Overflow Note focuses the field.

Label `.748` (onto master `.747`). Originally reserved `.718`; landed as `.748` past master `.747`.
Excellence-Override below.

Excellence-Override: exercise notes on the log

Rotated LOG oldest → [docs/archive/log/LOG-rotate-689-for-748.md](docs/archive/log/LOG-rotate-689-for-748.md). · [`.747` for `.762`](docs/archive/log/LOG-rotate-747-for-762.md).
