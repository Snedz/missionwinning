# Rotated LOG — 1RM percent (`.981`)

Rotated 2026-08-25 for Backfill a past session (`.1000`).
Full record from [LOG.md](../../LOG.md).

## 2026-08-25 — 1RM percent (`.981`)

Notebook programs already talk
percent. The live set row only took
kg × reps. Educational Epley is a
formula estimate, not a tested max.

**Ship:** optional percent field on
the live set row / next-set cite.
Type 80% and get a load when a
known 1-rep max exists, or show
percent of that max next to the
target. Empty invents nothing. No
invented max from one set. No
autoreg-as-identity. Guest. First
set ungated. Today still one Start.
Resume `.963` kept. Supersets `.980`
/ Learn `.978` / week strip `.977`
/ Track `.976` / Move `.974` / cues
`.973` / honesty `.971` / tags `.970`
/ RPE `.967` / Fuel `.965` stay.
`/private` stays the tight lock. No
`PRIVATE_MODE` flip.

Label `.981` (past master `.980` /
`17039cb3`). Live www stays `.696`.

Rotated LOG oldest → [LOG-rotate-958-for-981.md](./LOG-rotate-958-for-981.md).
