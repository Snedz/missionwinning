## 2026-08-09 — Coach why panel English floors (`.642`)

CoachAdaptBanner "Why today's plan" used `i18n.exists(key) ? t(key) : ''` — bootstrap/hydrate blanked the panel (same class as readiness raw keys `.637`). **`coachWhyDefaults`**: English floors for every engine `whyKey`; `coachWhyLine` always supplies defaultValue. Guard: progression/selector/adjust whyKeys covered; banner no longer exists-blank.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-642.md](docs/archive/log/LOG-rotate-642.md).

