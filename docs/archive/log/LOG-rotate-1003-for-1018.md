# Rotated from LOG.md for `.1018` (Month they own)

## 2026-08-25 — Delete this finished session (`.1003`)

Backfill creates days. Edit corrects a
session that exists. Missing: undo a
bogus Monday (wrong date, test log,
imported junk). Hevy deletes one
workout and cannot recover. Strong
cannot delete one session.

**Ship:** History detail — Delete this
session, confirm, then that one
finished log is gone. Copy says it
cannot be recovered. Empty / live /
missing invents nothing. Does not wipe
the account. Does not discard a live
in-progress session. Other days stay.
History / week strip / this-movement /
PR diary skip the tombstone. Guest.
First set ungated. Honesty `.971`
still scores Train session count.
Today still one Start. Resume `.963`
kept. Merge `.1002` / Pause `.1001` /
Backfill `.1000` / in-set PR `.999` /
Reorder `.998` / History Edit `.997` /
note+pin `.996` stay. `/private`
stays the tight lock. No
`PRIVATE_MODE` flip.

Label `2026.07-unified.1003` (from
master `.1002` / `f0b10181`). Live
www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-986-for-1003.md](docs/archive/log/LOG-rotate-986-for-1003.md).
