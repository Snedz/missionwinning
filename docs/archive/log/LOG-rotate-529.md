# Rotated for .529

## 2026-08-05 — Kaizen: perfect_week spectrum rule + workout apply (`.514`)

Pure `shouldAwardPerfectWeek` — train goal met + ≥4 spectrum slots (train, fuel, non-train pillars). `applyWorkoutRewards` emits `perfect_week` → full_spectrum badge when earned.

Mutants: under train goal with fat spectrum → false; goal met train-only → false.

