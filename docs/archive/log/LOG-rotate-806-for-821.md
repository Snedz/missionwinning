# Rotated from LOG.md when `.821` landed

## 2026-08-14 — Builder empty goes to `/active` (`.806`)

A category with no templates was a centered sentence.
CareerLine already exits. This one did not.

**Ship:** EmptyState outline → `/active`. Logger never gated.

Label `.806` (onto master `.805`).

Excellence-Override: Builder empty exit (surface; RESULT unscored)
