# Rotated for .520

## 2026-08-05 — Mission Rewards + full-launch override (`.505`)

Founder override: launch with everything. Ship Mission Rewards (XP, ranks, badges) from real logs — weekly train goal is boss consistency, not toxic daily streaks. Wire finish workout / fuel day / pillar wins / commissioned. Victory XP line, Today progress card, Profile badge shelf, challenge Done state. Leaderboard on by default (secondary surface). Domain `src/lib/rewards/`.

Mutants: double-claim same workout id → 0 XP; under weekly goal then meet → awards; leaderboard parked by default again → surface test red.

