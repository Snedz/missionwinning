## 2026-08-04 — NL plenty/loads/heap/bunch/ton qty (`.451`)

Lots-band expands to `plenty`/`loads`/`heap`/`bunch`/`ton(s)` → qty 2. Plural `bananas` matches Banana. Continues Fuel estimate accuracy after lots (.449).

Mutants: drop loads from matcher → 1× chicken → red; bananas keyword only singular → rough meal → red.
