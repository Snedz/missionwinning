## 2026-08-04 — CoachPlanSessionGrid + boss id SoT (`.442`)

Week session grid peels into `CoachPlanSessionGrid`; `resolveCoachBossSessionId` is the one home for D12 boss Start (today pending else next). Page 322→292. Unit + wiring guards.

Mutants: re-inline todayPending on CoachPage → wiring red; drop isPrimaryStart boss compare → wiring red.

