## 2026-08-25 — Exercise note + pinned reminder (`.996`)

Session notes `.983` cover the workout.
Cues `.973` are our rack card. The lift
already had a one-line field, but last
History note prefills it — a one-time
"left shoulder felt off" became next
week's box.

**Ship:** on the open Train lift they
can write a note for this movement, and
pin a reminder that returns next time
they open it. The note is theirs. The
pin is theirs. Last History note is not
a pin. The pin does not appear on
History, the movement-history sheet,
the receipt, or the cloud. Empty
invents nothing. Honesty `.971` still
applies. Free. Guest. First set
ungated. Today still one Start. Resume
`.963` kept. Per-exercise rest `.995` /
set-row type `.994` / history `.993` /
custom `.992` / Start this again `.991`
/ Track trend `.989` / EMOM `.988` /
drop-set `.986` / warmup `.985` / notes
`.983` / 1RM `.981` / supersets `.980`
/ Learn `.978` stay. `/private` stays
the tight lock. No `PRIVATE_MODE` flip.

Label `2026.07-unified.996` (past
master `.995` / `8f7f19c9`). Live www
stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-976-for-996.md](docs/archive/log/LOG-rotate-976-for-996.md).
