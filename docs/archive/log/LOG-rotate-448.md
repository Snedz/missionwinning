## 2026-08-04 — NL quarter cup qty (`.433`)

`a quarter cup` / `quarter of a cup` parse as 0.25× portion scale (Fuel accuracy). Sits with word-half matchers; unit cases lock the scale.

Mutants: drop quarter matcher → full cup macros → red.
