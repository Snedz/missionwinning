# Rotated from LOG.md for `.913`

## 2026-08-17 — Library first paint is a pick list (`.898`)

Library was a poster wall: numbered cards, blurbs, tags, two
doors above search. The job is find a movement and pick it.

**Ship:** first paint is search, Filters, and a pick row. Posters,
blurbs, Today, and program templates live in Show all. Same
catalog.

Label `.898` (stacked on `.897`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-883-for-898.md](docs/archive/log/LOG-rotate-883-for-898.md).
