## 2026-08-04 — Form guide seeded a11y (`.373`)

Kaizen Loop 24 H1. Seeded axe on `/active` with form guide open (add push-ups → Form guide).

**Real axe findings fixed:**
1. `AdaptiveOverlay` body scroll region — `role=region` + `aria-label` + `tabIndex={0}` (scrollable-region-focusable)
2. `FormGuideSheet` Avoid heading — `text-red-400/90` → solid `text-destructive` (contrast)

Cap 16.
