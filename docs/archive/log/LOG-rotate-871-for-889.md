# Rotated from LOG.md for `.889`

## 2026-08-16 — `/harness` pastes the harvest (`.871`)

Empty queue plus a living `idea:next` pick used to print and wait for
`--paste`. That is why IL-H-15 sat off the list. `/harness` is now the
paste. Generate stays legal until two scout-harvests in a row survive
zero. Then C5. Not AU2.

**Ship:** `harvestGate.ts` · `idea:paste` · route `harvestAction`
paste | generate. Next `/harness` pastes IL-H-15.

Label `.871` (onto master `.870`).

Excellence-Override: harness harvest paste (tooling; RESULT unscored)

Rotated LOG oldest → [docs/archive/log/LOG-rotate-856-for-871.md](LOG-rotate-856-for-871.md).
