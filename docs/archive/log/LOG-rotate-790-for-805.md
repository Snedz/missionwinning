# Rotated from LOG.md when `.805` landed

## 2026-08-14 — Preview gate is one door, and it opens (`.790`)

The teaser printed Get notified and the no-spam line twice (parent +
form). Done 401'd when Preview lacked the codes list; a 200 still
assigned `/`, which remounts the same teaser.

**Ship:** title/foot once. Ungated walk-open skips the session probe
and sends Done / any typed code to `/welcome`. Gated www still
validates. Logger never gated. No `PRIVATE_MODE` flip.

Label `.790` (onto master `.789`).

Excellence-Override: Preview gate copy + walk-open unlock
