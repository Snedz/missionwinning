# Rotated from LOG.md for `.1025` (Completed set-table empty load is BW, not 0)

## 2026-08-25 — Library skips deleted sessions (`.1010`)

Delete a bogus Monday. History
skips the tomb. Library still
counted it in Logged in N
sessions and the volume spark.

**Ship:** Library count and spark
skip `deletedAt`. Tomb-only is
empty. Empty invents nothing.
Guest. First set ungated. Today
still one Start. BW cite `.1009`
stays. `/private` stays the tight
lock. No `PRIVATE_MODE` flip.

Label `2026.07-unified.1010` (from
master `.1009` / `20faec3ab`). Stamp
stays `.1010`. Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-995-for-1010.md](docs/archive/log/LOG-rotate-995-for-1010.md).
