# Rotated from LOG.md for `.910`

## 2026-08-17 — Search is a field over the rooms (`.895`)

Search was a renamed More sheet: tiers, What’s New, quiet
foot. The job is type the room, open it, pin it if you want.

**Ship:** `filterSearchCatalog` / `summaryPinIdForHref`. Field on
the existing sheet. Fuel, Coach, Train pin to Summary from the
row. No `/search` route.

Label `.895` (stacked on `.894`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-880-for-895.md](docs/archive/log/LOG-rotate-880-for-895.md).
