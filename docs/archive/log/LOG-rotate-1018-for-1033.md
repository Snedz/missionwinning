# Rotated from LOG.md for `.1033` (This month shows how many live sessions)

## 2026-08-26 — Month they own (`.1018`)

The History month grid already marks
trained days. A tap could not open
that day's live rows. Session count
is a fact already on the log. A fire
count is not.

**Ship:** History Show-all month —
tap a live day, see that day's live
rows, open the row they already have.
Tombs stay unmarked and out of the
day list. Start-from fold never
erases the month. Search stays a
query. Charts stay the full live
diary. Empty month / empty day
invents nothing. History only. Guest.
First set ungated. Today still one
Start. Empty-load cite `.1017`
stays. `/private` stays the tight
lock. No `PRIVATE_MODE` flip.

Label `2026.07-unified.1018` (from
master `.1017` / `8a606a12`). Stamp
stays `.1018`. Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-1003-for-1018.md](docs/archive/log/LOG-rotate-1003-for-1018.md).
