# Rotated from LOG.md for `.1015` (assisted 0 cite is BW, not 0 kg)

## 2026-08-25 — Backfill a past session (`.1000`)

Edit-finished `.997` corrects a session
that already exists. Missing: Monday
they trained and never opened the app.

**Ship:** they can log a session they
already did — pick a date (and optional
start/end time), type the sets they
remember, Save. Duration is honest from
the times they set, or omitted if they
turn timing off. Empty invents nothing.
Not Resume. Not Edit. Guest. First set
ungated. Honesty `.971` still scores
Train session count. Today still one
Start. Resume `.963` kept. In-set PR
`.999` / Reorder `.998` / History Edit
`.997` / note+pin `.996` / rest `.995`
/ set-row type `.994` / history `.993`
stay. `/private` stays the tight lock.
No `PRIVATE_MODE` flip.

Label `2026.07-unified.1000` (past
master `.999` / `fb7f9d34`). Live
www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-981-for-1000.md](docs/archive/log/LOG-rotate-981-for-1000.md).
