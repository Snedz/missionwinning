# Rotated from LOG.md when `.820` landed

## 2026-08-14 — CareerLine empty goes to `/active` (`.805`)

Your record on Profile was invitation copy with no path.
Other empties already ScreenDock or EmptyState.

**Ship:** EmptyState outline CTA → `/active`. Logger never gated.

Label `.805` (onto master `.804`).

Excellence-Override: CareerLine empty exit (surface; RESULT unscored)
