# Rotated from LOG.md when `.807` landed

## 2026-08-14 — www gate, Alpha 0.1.0 (`.792`)

Localhost painted Log a set as the red and sent `/welcome`, which
skips the door. Live www is waitlist-first. Invite-only / beta
language is the old stamp.

**Ship:** drop the `/welcome` poster. Notify me is the one red. Eyebrow
and waitlist copy are Alpha 0.1.0, not invite-only. After Done still
the `.696` landing. Logger `/active` stays public. No `PRIVATE_MODE`
flip.

Label `.792` (onto master `.791`).

Excellence-Override: www gate, Alpha 0.1.0
