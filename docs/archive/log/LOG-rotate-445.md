## 2026-08-04 — Today header focus line builder (`.430`)

`buildTodayHeaderFocusLine` is the one home for show-gate + bodyweight tag on Today's header. Dashboard wires it; unit + wiring guards.

Mutants: restore inline `userEquip === 'bodyweight'` in dashboard → wiring red.
