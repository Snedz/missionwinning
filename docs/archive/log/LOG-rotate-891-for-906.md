# Rotated from LOG.md for `.906`

## 2026-08-17 — Today Summary is one pin (`.891`)

After First Steps left, Today was still a date, a journey strip, and a
paragraph. Summary is a pin grid. Cold default is one pin. Four is the
ceiling. Start stays the docked red field.

**Ship:** `defaultSummaryPins` / `clampSummaryPins`. Lean mounts
`TodaySummaryPins`. Encourage and coach-invite off the lean shell.

Label `.891` (stacked on `.890`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-873-for-891.md](docs/archive/log/LOG-rotate-873-for-891.md).
