## 2026-08-04 — NL few qty + dab words (`.441`)

`a few eggs` / `few almonds` → qty 3; `a dash|splash|pinch of …` uses tsp-scale dab (not a full serving). Continues Fuel estimate accuracy after couple (.438).

Mutants: drop few matcher → 1 egg protein → red; map dab to qty 1 → full oil cals → red.

