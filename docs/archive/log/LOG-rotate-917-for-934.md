# Rotated from LOG.md for `.934`

## 2026-08-18 — Excellence path map names Cue me and builder (`.917`)

The gate discovers top-level folders and failed on two
that had already shipped: Cue me (`src/components/speech`)
and builder (`src/lib/builder`). Every surface PR went red
on a map that could not see them.

**Ship:** both folders are on `KNOWN_TOP_LEVELS` as surface.
Same gate. Same RESULT.

Label `.917` (stacked on `.916`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [LOG-rotate-902-for-917.md](LOG-rotate-902-for-917.md).
