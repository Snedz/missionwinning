# LOG rotation — `.750` (rotated for `.766`)

Rotated out of [LOG.md](../../../LOG.md) when `.766` shipped, to keep that file
at ≤15 entries. Nothing is deleted; this is the full entry.

## 2026-08-13 — Session history list (`.750`)

Free session history excellence: deepen the one true list on `/history`
Sessions. Not a feed, not the Athlete Page, not Victory vs-last.

**Ship:** `sessionHistoryList` projects date · title/muscles · set count from
device `workoutHistory` (tombstones omitted). Empty waits on persist hydrate
and stays an invitation (Open Today). Tap opens the completed log read-only
(existing details dialog). **Again** stays a separate control. Calendar and
`/history/[date]` unchanged. Cloud merge never gates the list. No Top 8,
comments, or streak-as-shame.

Label `.750` (onto master `.749`). Originally reserved `.720`; landed as `.750` past master `.749`.
Excellence-Override below.

Excellence-Override: session history list

Rotated LOG oldest → [docs/archive/log/LOG-rotate-691-for-750.md](docs/archive/log/LOG-rotate-691-for-750.md). · [`.749` for `.764`](docs/archive/log/LOG-rotate-749-for-764.md).
