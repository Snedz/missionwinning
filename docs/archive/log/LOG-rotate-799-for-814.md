# Rotated from LOG.md when `.814` landed

## 2026-08-14 — Astro `/compare` index; Next keeps the stories (`.799`)

www needed a compare index. The three vs-pages stay on Next
(`/guide/mission-winning-vs-*`). This page only points at them.

**Ship:** `compare.astro` + `compareContent.ts`. No fourth competitor.
No `_redirects`. No host steal.

Label `.799` (onto master `.798`).

Excellence-Override: commissioned www compare index (surface; RESULT unscored)
