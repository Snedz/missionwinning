# Rotated LOG — Quiet Move (`.974`)

Rotated 2026-08-25 for Per-exercise rest (`.995`).
Full record from [LOG.md](../../LOG.md).

## 2026-08-25 — Quiet Move (`.974`)

Rest days on the quiet week strip are empty on
purpose. Empty is not a fail. Rest still had
nowhere quiet to go.

**Ship:** `/move` logs an optional easy walk or
easy session — minutes or distance if they have
them, or just the kind. Own diary. Empty
week-strip days stay empty. Not a second Today
Start. Not a ring. Not a Health permission
before Train. Not a feed of walks. Guest. First
set ungated. Today still one Start. `/private`
stays the tight lock. No `PRIVATE_MODE` flip.
Past master `.973`. Keeps cues `.973`, honesty
`.971`, set tags `.970`, RPE `.967`, Fuel
`.965`, resume `.963`. Concern `.969`.

Label `.974` (Quiet Move on master that now
has `.973` / `.971` / `.970` / `.967` / `.965`).
Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-953-for-974.md](docs/archive/log/LOG-rotate-953-for-974.md).
