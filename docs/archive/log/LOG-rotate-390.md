## 2026-08-04 — HoldToConfirm and button hover chrome solidify (`.375`)

Kaizen Loop 24 H3. Soft chrome leftovers:

1. Button destructive/secondary hover drops opacity tints for solid fills
2. HoldToConfirm armed ring + fill/label drop opacity tints
3. OtpInput error border `red-500/60` → solid `border-destructive`

Living log Loop 24 closed. Cap 16.
