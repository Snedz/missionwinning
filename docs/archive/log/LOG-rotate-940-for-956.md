# Rotated from LOG.md for `.956`

## 2026-08-24 — Strong CSV import: preview + confirm (`.940`)

The `.180` parser already turned an English Strong-app
export into local history. Drop wrote immediately. A
bad row was counted in the parser and then hidden.

**Ship:** pick shows a dry-run preview (workouts, sets,
skipped rows). Confirm writes. Cancel does not. Empty
file and header-only stay errors. A second file still
adds. No account. Offline.

Label `.940` (past master `.939` after #777 + Dependabot;
do not steal `.935` #778 or `.938` #780).

Rotated LOG oldest → [LOG-rotate-920-for-940.md](./LOG-rotate-920-for-940.md).
