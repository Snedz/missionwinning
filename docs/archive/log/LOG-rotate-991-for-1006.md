## 2026-08-25 — This session becomes a Start (`.991`)

Honor notebook is program-in. Repeat last
copies the newest log. Resume keeps the
open session. Missing: Thursday starts
Monday's log without rebuilding.

**Ship:** from the close receipt or
History they can **Start this again** —
same movements, last loads as targets.
Empty invents nothing. Not a shop. Not
a 3-template cap. Honor notebook stays
program-in. Guest. First set ungated.
Today still one Start. Resume `.963`
kept. Track trend `.989` / EMOM `.988` /
drop-set `.986` / warmup `.985` / notes
`.983` / 1RM `.981` / supersets `.980` /
Learn `.978` / week strip `.977` / Track
`.976` / Move `.974` / cues `.973` /
honesty `.971` / tags `.970` stay.
`/private` stays the tight lock. No
`PRIVATE_MODE` flip.

Label `.991` (past master `.989` /
`adad3e58`). Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-967-for-991.md](docs/archive/log/LOG-rotate-967-for-991.md).
