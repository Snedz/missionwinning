## 2026-08-25 — This-movement history (`.993`)

Vs-last and the next-set cite already
show last. The open lift name was a
span. History is whole sessions.
Library is a count plus a spark.
Missing: tap the movement, see prior
sessions of that lift.

**Ship:** tap the open lift name to see
prior sessions of that movement (their
diary). Empty / short list invents
nothing. Honesty `.971` still applies.
Not a projected-max chart. Not another
human's number. Not a Feed permalink.
Not a shame slope. Optional. Guest.
First set ungated. Today still one
Start. Resume `.963` kept. Custom
`.992` / Start this again `.991` /
Track trend `.989` / EMOM `.988` /
drop-set `.986` / warmup `.985` /
notes `.983` / 1RM `.981` / supersets
`.980` / Learn `.978` / week strip
`.977` / Track `.976` / Move `.974`
stay. `/private` stays the tight lock.
No `PRIVATE_MODE` flip.

Label `2026.07-unified.993` (past
master `.992` / `7bb7c464`). Live www
stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-971-for-993.md](LOG-rotate-971-for-993.md).
