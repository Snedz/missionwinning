# Rotated from LOG.md for `.950`

## 2026-08-24 — Guards match the product (`.929`)

The cheap merge bar was red on master: LearnVs
`never`, a Journal grep from before `.926`, a mic the
Talk path does not have, and harness reading SMOOTHER
out of a `ready-for-founder` sentence.

**Ship:** `otherLearnVsPages` takes a string. Journal
empty greps the shipped debrief. Composer has no mic.
`ready-for-founder` first means role is null.

Label `.929` (stacked on `.928`).

Rotated LOG oldest → [docs/archive/log/LOG-rotate-914-for-929.md](docs/archive/log/LOG-rotate-914-for-929.md).
