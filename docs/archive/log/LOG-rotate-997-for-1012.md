## 2026-08-25 — Edit a finished session (`.997`)

History cites the diary read-only.
A typo (225 instead of 135) stayed
on the completed row, and Coach /
Wednesday / 1RM / volume all read
it. Resume `.963` is the live set —
leave Today and come back. That is
not this ship.

**Ship:** open a finished session
from History, edit the sets they
actually logged, then Save. Confirm
before a destructive change. Empty
invents nothing — does not wipe,
does not tombstone. Same session
id. Guest. First set ungated.
Honesty `.971` still applies.
Today still one Start. Resume
`.963` kept. Note+pin `.996` /
rest `.995` / set-row type `.994`
/ history `.993` / custom `.992`
/ Start this again `.991` / Track
trend `.989` / EMOM `.988` /
drop-set `.986` / warmup `.985` /
notes `.983` stay. `/private`
stays the tight lock. No
`PRIVATE_MODE` flip.

Label `2026.07-unified.997` (past
master `.996` / `2be854e6`). Live
www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-977-for-997.md](docs/archive/log/LOG-rotate-977-for-997.md).
