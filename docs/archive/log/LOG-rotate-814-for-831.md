# Rotated from LOG.md when `.831` landed

## 2026-08-14 — Locale footer waits to guess country (`.814`)

SSR painted “could not confirm a region.” The client painted Canada.
Timezone is not available on the server.

**Ship:** detect after mount. First paint is language + US select.

Label `.814` (onto master `.813`).

Excellence-Override: Locale footer hydrate (surface; RESULT unscored)
