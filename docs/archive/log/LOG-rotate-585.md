# Rotated for .585

## 2026-08-07 — Fix: assessment win bumps streak; assess CTAs plain (`.570`)

Profile assessment win calls bumpTrainingStreak (was toast-only lie). Assessments recommendation chips 44px plain. PFT result buttons 44px.

Mutants: ProfileAssessmentCard without bumpTrainingStreak → red.

