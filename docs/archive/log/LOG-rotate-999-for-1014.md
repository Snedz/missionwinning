## 2026-08-25 — In-set PR they hit (`.999`)

A named-app leftover: when they log a
working set that is actually a personal
record vs their own diary, a small
felt-win on the live set. Best logged 5,
heaviest, most reps — numbers they
already wrote. Honesty `.971` when there
is no prior: invent nothing.

**Ship:** after a working set that beats
a number they already logged, a quiet PR
on that set (checkbox / felt-win).
First session invents nothing. No
projected 1RM banner. No medals-as-Feed.
Guest. First set ungated. Honesty `.971`
still scores Train session count, not a
PR score. Today still one Start. Resume
`.963` kept. Reorder `.998` / History
Edit `.997` / note+pin `.996` / rest
`.995` / set-row type `.994` / history
`.993` / custom `.992` / Start this
again `.991` stay. vs-last stays.
e1RM hide stays. `/private` stays the
tight lock. No `PRIVATE_MODE` flip.

Label `2026.07-unified.999` (past
master `.998` / `8d5d1c1d`). Live
www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-980-for-999.md](docs/archive/log/LOG-rotate-980-for-999.md).
