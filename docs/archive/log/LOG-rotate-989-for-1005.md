## 2026-08-25 — Quiet Track trend (`.989`)

Track already logs scale / tape on `/track`.
The week strip already lets one optional
Fuel / Walk / Scale row sit on an empty
rest day. Missing: they logged twice and
cannot see last-vs-this without leaving
Train-as-home for the diary.

**Ship:** if they have two Track weight or
tape logs, that week-strip rest day can
show muted last → this. Empty invents
nothing. Diary-only `/track` logs paint
Scale on an empty rest day. Train Done
stays Train. Fuel / Walk that day swallows
the trend. Not a Today widget. Not rings.
Not a shame slope. Not body photos.
`/track` stays the diary. Guest. First set
ungated. Today still one Start. Resume
`.963` kept. Honesty `.971` still scores
Train only. EMOM `.988` / drop-set `.986`
/ warmup `.985` / notes `.983` / 1RM
`.981` / supersets `.980` / Learn `.978` /
week strip `.977` / Track `.976` / Move
`.974` / cues `.973` / tags `.970` stay.
`/private` stays the tight lock. No
`PRIVATE_MODE` flip.

Label `.989` (past master `.988` /
`277b55d3`). Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-965-for-989.md](docs/archive/log/LOG-rotate-965-for-989.md).
