# Rotated LOG — Quiet Learn (`.978`)

Rotated 2026-08-25 for Live session reorder (`.998`).
Full record from [LOG.md](../../LOG.md).

## 2026-08-25 — Quiet Learn (`.978`)

Learn first paint was a ten-path
catalog. First success already lived
in Strength Basics `sb-0` — log a
set, then Coach from those logs.

**Ship:** `/learn` first paint is
that existing intro. Other paths,
guidebook, and sample fold under
Show more. Cue list can open Learn
when they want more than a rack
card. Empty invents nothing. Not on
Today. Guest. First set ungated.
Today still one Start. Resume `.963`
kept. Quiet Track `.976` / Move
`.974` / cues `.973` / honesty
`.971` / tags `.970` / RPE `.967` /
Fuel `.965` stay. Week strip `.977`
stays. `/private` stays the tight
lock. No `PRIVATE_MODE` flip.

Label `.978` (past master `.977` /
`79791d8b`). Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-956-for-978.md](docs/archive/log/LOG-rotate-956-for-978.md).
