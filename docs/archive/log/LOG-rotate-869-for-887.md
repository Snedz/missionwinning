# Rotated from LOG.md for `.887`

## 2026-08-16 — Process is `/harness` (`.869`)

`/graph` named the outer loop after the wrong thing. The field word is
agent harness. Graph already means Neo4j ingest and the idea graph.

**Ship:** command `/harness` · `npm run harness` / `harness:done` · hop
files in `docs/harness/`. `/graph` and `npm run graph` stay as aliases.
`GRAPH_LOOP.md`, `ideaGraph`, and `graph:ingest` are unchanged.

Label `.869` (onto master `.868`).

Excellence-Override: process rename (tooling; RESULT unscored)

Rotated LOG oldest → [docs/archive/log/LOG-rotate-854-for-869.md](LOG-rotate-854-for-869.md).
