# Rotated from LOG.md for `.876`

## 2026-08-16 — Coach week arrives as a visible diff (`.861`)

H-02: the adapt banner asserted that the week changed. The proposal is the
session-count change `adaptPlan` already made, plus the stored set
`logCitation` already quotes. Same Today slot. TAP_BUDGET holds.

**Ship:** `weekDiff.ts` · `CoachAdaptBanner` · `todayBlockBudget` instrument.

Label `.861` (onto master `.860`).

Excellence-Override: Coach week visible diff (Today/Coach banner; RESULT unscored)

Rotated LOG oldest → [docs/archive/log/LOG-rotate-846-for-861.md](LOG-rotate-846-for-861.md).
