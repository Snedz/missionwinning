# Rotated for .582

## 2026-08-07 — Fix: empty states need exit CTAs (`.567`)

Fuel recipes empty → Log food #fuel-log. Benchmarks empty → Open Today. Leaderboard squad empty focuses code field. Habit log uses toast not alert.

Mutants: recipes empty without fuel-log → red; benchmarks empty without /log → red; squad empty without focus → red.

