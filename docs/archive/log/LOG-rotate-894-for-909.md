# Rotated from LOG.md for `.909`

## 2026-08-17 — Fuel first paint is a notepad (`.894`)

Fuel was a house: recipes, week, wizard, a floating Log food, then
the type field. The job is type → + → remaining moves.

**Ship:** remaining first, notepad type-in, today’s lines. Show all
holds recipes, barcode, week, weight, photo, sign-in. FAB deleted.

Label `.894` (stacked on `.893`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-876-for-894.md](docs/archive/log/LOG-rotate-876-for-894.md).
