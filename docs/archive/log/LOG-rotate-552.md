# Rotated for .552

## 2026-08-06 — Coach session why + Fuel empty/error honesty (`.537`)

Wave 8 steal: pure buildSessionWhyLine on CoachTodayCard (focus + load zone, non-predictive). FuelRecipesPanel EmptyState/ErrorState + premium retry.

Mutants: CoachToday without session why mount → red; Fuel panel without ErrorState → red.

