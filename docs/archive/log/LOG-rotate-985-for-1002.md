# Rotated LOG — Warmup batch (`.985`)

Rotated 2026-08-25 for Merge duplicate exercises (`.1002`).
Full record from [LOG.md](../../LOG.md).

## 2026-08-25 — Warmup batch (`.985`)

Tags already mark warmup. Strong
paywalls the calculator that batches
them. After supersets, leftover in-set
grammar: add three warmups from this
work set without a formula paywall.

**Ship:** from a working set, Add
warmups inserts ½ / ⅔ / ¾ of that
weight (5 / 3 / 1). Simple fractions.
Athlete can edit or delete any. Empty
invents nothing. Warmups stay excluded
from cites. No calculator paywall. No
injury warmup. Guest. First set
ungated. Today still one Start. Resume
`.963` kept. Notes `.983` / 1RM `.981`
/ supersets `.980` / Learn `.978` /
week strip `.977` / Track `.976` /
Move `.974` / cues `.973` / honesty
`.971` / tags `.970` / RPE `.967`
stay. `/private` stays the tight lock.
No `PRIVATE_MODE` flip.

Label `.985` (past master `.983` /
`046fe67e`; concern `.984`). Live www
stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-960-for-985.md](LOG-rotate-960-for-985.md).
