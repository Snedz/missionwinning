# Rotated for .512

## 2026-08-05 — Guide + Paths single content handoff (`.496`)

Flow-3 dual-pad bridge: public `/paths/[id]` primary CTA **Open in Learn** → `/learn?path=<id>` (Learn expands that free path, strips query). Guide: public `/guide` is magazine canonical; in-app `/learn/guide` links Magazine (web) + per-chapter magazine view; public magazine links Track progress in app. Pure `seoLearnBridge` + guards.

Mutants: path detail only `/welcome` primary → red; guide index without public magazine link → red.


