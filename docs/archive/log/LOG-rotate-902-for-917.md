# Rotated from LOG.md for `.917`

## 2026-08-17 — Live session is name, table, rest (`.902`)

The logger had the table and then talked: coach tip, Plates,
Cue me, jot, heart rate above the sets. The job is log the set.

**Ship:** header is the session name, elapsed/sets, Finish.
Plates and the coach tip live in overflow. HR, jot, readiness
in Show all. Speech does not own Train. Same table, same rest.

Label `.902` (stacked on `.901`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-887-for-902.md](LOG-rotate-887-for-902.md).
