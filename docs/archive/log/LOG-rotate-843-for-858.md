# Rotated from LOG.md when `.858` landed

## 2026-08-15 — Graph memory of the dev history (`.843`)

Agent tooling: LOG + archive + CONTEXT as a temporal graph. Secrets redacted.
Label `.843` (onto master `.842`).
