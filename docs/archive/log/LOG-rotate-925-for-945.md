# Rotated from LOG.md for `.945`

## 2026-08-18 — Train empty Start is Start Workout (`.925`)

The empty dock defaulted Start workout. Packs say Start
Workout. First paint listed it after the Today Coach pin.

**Ship:** defaultValue matches the pack. Same dock.
Logger stays free.

Label `.925` (stacked on `.924`).

Rotated LOG oldest → [docs/archive/log/LOG-rotate-910-for-925.md](docs/archive/log/LOG-rotate-910-for-925.md).
