# Rotated from LOG.md when `.812` landed

## 2026-08-14 — Today Coach week shows dose + adapt (`.797`)

Coach already painted this week's dose and the adapt banner. Today's
week strip was days only — so the week "felt earned from logs" only
after opening `/coach`. G6 is that evidence on Today too.

**Ship:** `TodayCoachWeekStrip` mounts `summarizeWeekDose` + compact
`CoachAdaptBanner`. No second red. Lean first-paint stays off Coach.

Label `.797` (onto master `.796`).

Excellence-Override: Coach week dose+adapt on Today (surface; RESULT unscored)
