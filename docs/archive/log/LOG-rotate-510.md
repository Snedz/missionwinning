# Rotated for .510

## 2026-08-05 — Flow architecture + Product footer pinout (`.494`)

Chip-level site IA: `docs/FLOW_ARCHITECTURE.md` (dies A/B, buses, critical path, dual pads) + INDEX/docs hub links. Product footer: **Start free** first → Compare → **How Coach adapts** (`/#coach`, not bare Coach / not `/coaching` / not `/coach`). Guard `footerFlow.test.ts`.

Mutants: Product first link `/#coach` → red; defaultValue `Coach` on adapt pad → red.


