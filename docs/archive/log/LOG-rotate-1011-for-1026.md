# Rotated from LOG.md for `.1026` (Repeat this session)

## 2026-08-26 — Export this diary (`.1011`)

Import + backfill dumped years
into the diary. Search finds a
row. Missing was a file they can
take. Account already has Strong /
Hevy / MW interchange — wrong door,
wrong columns, no `sessionTitle`.

**Ship:** History overflow saves the
live diary as CSV. JSON is the same
rows. Honest logged fields only.
Empty invents nothing — Save stays
disabled. Tombs stay out unless
restored. Start-from does not shrink
the file. Guest. First set ungated.
Today still one Start. Library skip
tombs `.1010` stays. BW cite `.1009`
stays. Search `.1008` stays.
`/private` stays the tight lock. No
`PRIVATE_MODE` flip.

Label `2026.07-unified.1011` (from
master `.1010` / `4d128292`). Stamp
stays `.1011`. Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-996-for-1011.md](docs/archive/log/LOG-rotate-996-for-1011.md).
