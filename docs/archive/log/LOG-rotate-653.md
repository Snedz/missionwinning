## 2026-08-09 — Empty Active docks one Start (`.638`)

CX residual from `.637`: empty `/active` had only EmptyState outline + Today/Builder links — no red primary under the thumb. **EmptyState stays invite-only** (no inline CTA). **ScreenDock** carries a `poster-field` Start (same invert pattern as Today’s hero); compact docks it; desktop keeps the field in flow. zero-state `/active` cap stays **0** (nested button is paper; red is the field). Live check: dock “Start workout”, poster `rgb(173,23,0)`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-638.md](docs/archive/log/LOG-rotate-638.md).
