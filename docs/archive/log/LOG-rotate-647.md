## 2026-08-09 — Feel Wave 8: dogfood residual (`.632`)

Fixes from local Field manual dogfood — not more page subtitles:

- **Beta:** step strings in `BOOTSTRAP_EN` + `defaultValue` from `BETA_EN` so first paint never shows bare keys; guard test.
- **Private gate:** 2px inputs; 44px access summary; access stays secondary to Notify me.
- **Today First Steps:** eyebrow type; dismiss demoted to quiet underline (progress/next action stay primary).

Rotated LOG oldest → [docs/archive/log/LOG-rotate-632.md](docs/archive/log/LOG-rotate-632.md). · [rotated for `.633`](docs/archive/log/LOG-rotate-633.md).
