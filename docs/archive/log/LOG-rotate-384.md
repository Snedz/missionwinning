## 2026-08-04 — Select and tooltip radius solidify (`.369`)

Kaizen Loop 22 F3. Soft chrome leftovers:

1. `Select` trigger + content drop `rounded-md` for `rounded-none`
2. `Tooltip` content drops `rounded-md` for `rounded-none`

Living log Loop 22 closed. Cap 16.
