# Rotated from LOG.md for `.953`

## 2026-08-24 — Coach why-this-session (`.934`)

Reserved `.699` concern (F-012 / F-002 session why-line).
This is PR #774 (minted as `.932` on `.930`). Rebased
onto `.933` brand so the door stays Free / Log a set.
Offline.

#478's engine was already on master via the merge-all that
closed it as “superseded by #544 / `.766`”. `.766` is
`logCitation`. The gap was silent empty: whyKey stories
could say “your logs” with zero workouts.

**Ship:** `session-empty` on a known empty history. Boss
`PlanSessionCard` cites inputs · rule · effect when logs
exist, or one quiet honest line when they do not. Coach
stays skippable. Why stays free. No E-Adjacency.

Label `.934` (stacked on `.933`). This is the `.699` concern.
Door copy stays `.933`.

Rotated LOG oldest → [LOG-rotate-917-for-934.md](LOG-rotate-917-for-934.md).
