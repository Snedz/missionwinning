# Rotated from LOG.md when `.826` landed

## 2026-08-14 — Builder first-paint matches the pack (`.809`)

The badge said “Free programs” then hydrated to “N programs.”
Empty saved said “routines” then “workouts.”

**Ship:** interpolate `{{count}}`. Align empty title. Drift 216 → 214.

Label `.809` (onto master `.808`).

Excellence-Override: Builder first-paint (surface; RESULT unscored)
