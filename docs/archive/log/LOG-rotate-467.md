## 2026-08-04 — Victory share decision helpers (`.452`)

`buildVictorySharePayload` + `nextVictoryShareAfterFile` / `AfterText` own the Share ladder. Cancel after file share stays `done` (no clipboard). Sheet wires helpers; unit + wiring guards.

Mutants: cancelled file → try_text → red; inline utm URL on the sheet → wiring red.
