# Rotated from LOG.md when `.857` landed

## 2026-08-15 — CI ran for the first time and found two more (`.842`)

PR CI ran `build-and-test` and `www` after Actions billing returned. Three
`privateGateRedirect` assertions were `.839` fallout. www `/about` `/compare`
`/vision` had no budget.

Label `.842` (onto `.841`).
