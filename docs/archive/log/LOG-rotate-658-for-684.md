## 2026-08-10 — Landing FAQ JSON-LD free-beta mute (`.658`)

CX walk: Landing FAQ UI already dropped Super Bundle under free beta, but `faqPageJsonLd()` still emitted “What is the Super Bundle?” in schema.org FAQPage (and the page HTML). Shared `landingFaqKeysForJsonLd()` for UI + JSON-LD; free beta never merchandises Super Bundle in landing FAQ schema.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-658.md](docs/archive/log/LOG-rotate-658.md).
