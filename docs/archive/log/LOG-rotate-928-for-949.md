# Rotated from LOG.md for `.949`

## 2026-08-24 — Public Alpha snapshot exporter (`.928`)

The org repo is a progress report, not a cutover.
Daily origin stays Snedz. The exporter was untracked
when the first orphan copy was taken.

**Ship:** `npm run snapshot:public` writes a sibling
orphan tree. `--force` keeps the org remote. Deny
tests: `npm run snapshot:check`. No Public flip.

Label `.928` (stacked on `.927`).

Rotated LOG oldest → [docs/archive/log/LOG-rotate-913-for-928.md](docs/archive/log/LOG-rotate-913-for-928.md).
