# Rotated from LOG.md for `.965`

## 2026-08-24 — Hevy English CSV import (`.947`)

The guess was that the English set-table export
was already Strong-shaped. The headers differ
(`title` / `exercise_title` / `weight_kg`, not
Title Case Strong). The parser already had that
layout as `set-table-a`.

**Ship:** first-class Hevy fixtures on the same
Account path as Strong. Preview, then confirm.
Empty / one / malformed. Skipped rows counted.
More than one file. No account. Strong unread.

Label `.947` (past master `.946`). No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-927-for-947.md](./LOG-rotate-927-for-947.md).
