# Rotated from LOG.md for `.958` (2026-08-24)

## 2026-08-24 — Gated www is the four-scene field (`.942`)

`.933` locked the words. Cold visitors still got a
signup sheet. `CinematicWww` already ported N1 and
was dead to the live door.

**Ship:** `/private` mounts SET → ANYWHERE → WEEK →
DOOR. Door slot is Get notified + Enter with code.
Brand pack stays master’s: Log a set. Offline.
`/today` and `/train` 308 before the gate. `/notify`
is the Super Bundle form. Cookie `/` stays `.696`.
No `PRIVATE_MODE` flip.

Label `.942` (past master `.941` after #780; do not
keep `.935`).

Rotated LOG oldest → [LOG-rotate-922-for-942.md](./LOG-rotate-922-for-942.md).
