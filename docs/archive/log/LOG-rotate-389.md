## 2026-08-04 — Active exerciseHasCompletedSet extract (`.374`)

Kaizen Loop 24 H2. `exerciseHasCompletedSet` owns the completed-set predicate so ActiveExerciseCard does not inline `sets.some`. Wiring guard on the card. Cap 16.
