# Rotated from LOG.md for `.1020` (Library spark is reps, not 0, on empty load)

## 2026-08-25 — Start history from this date (`.1005`)

Import + backfill dump years into
week 1. The athlete wants a date
week strip / Coach / streak start
from. Older logs stay in the diary.

**Ship:** History overflow — Start
history from this date. Pick a day.
Older sessions remain if they open
them. Week strip / Coach / streak
start there. Data stays. Fold, don't
erase. Empty / missing / future
invents nothing. Confirm-gated if it
hides a lot of days. Clearing the
fold restores the full diary. Not
delete-session. Not hide-exercise.
Not backfill. Not edit. Guest. First
set ungated. Honesty `.971` still
scores Train session count. Today
still one Start. Resume `.963`
kept. Hide `.1004` / Delete `.1003` /
Merge `.1002` / Pause `.1001` /
Backfill `.1000` stay. `/private`
stays the tight lock. No
`PRIVATE_MODE` flip.

Label `2026.07-unified.1005` (rebased onto
master `.1004` / `b48e54b0`). Stamp stays
`.1005`. Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-989-for-1005.md](docs/archive/log/LOG-rotate-989-for-1005.md).
