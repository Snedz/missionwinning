# Rotated for .611

## 2026-08-07 — Ratchets: i18n 141→0, coverage 415→390, master push lane (`.596`)

Three gate steps were red on `master` and nothing reported it, because **no workflow runs on a push to master** — every one triggers on `pull_request`, `workflow_dispatch` or a cron, and prod ships via a Vercel Deploy Hook that bypasses Actions. New `ratchets.yml` runs the no-build half of the gate on that path. Deliberately not a `push:` on `ci.yml`: that exclusion is a documented metered-minutes decision, and a trigger there would run a full build plus 53 hero e2e per merge.

**i18n 141 → 0 (cap 16 → 0).** Measured on a clean `origin/master` worktree: **141 keys used in the UI existed in no EN pack**, nearly 9× the cap, every one rendering its English `defaultValue` in all 15 languages. `src/i18n/rewardsLocales.ts` never existed at all, so the whole rewards surface from `.505`–`.543` was untranslated; the rest arrived across `.562`–`.594` (account export/delete, owner tools, readiness prep, sign-in, form guides, founder wins). Six keys **could not** be catalogued as they stood — `moveSubtitleDepth`, `mindSubtitleDepth`, `fuelSubtitleDepth`, `fuelPremiumRecipesTitleCount`, `activeRestSkipAria`, `todayFounderStreakCloud` were single keys whose `defaultValue` was a runtime ternary. One catalog value cannot hold two sentences, so each became two keys with two **literal** call sites — a computed key (`t(cond ? 'a' : 'b')`) would have gone *invisible* to the counter rather than covered by it.

**Coverage 415 → 390 (floor 407 → 390).** Breached on `master` since `.544` across four ships. Ten logic modules gained colocated tests here; `master` independently added `challengeI18n`, `exerciseUsage` and `privacyConsent` while this branch was open, and those took master's copy.

**Two ratchets that could not fail.** `i18nCoverage.test.ts` held `HIGH_WATER = 710` against a live cap of 16 — a **44× loosening stayed green**, on the ratchet already breached three times this month. Now lockstep, as `coverageBudget.test.ts` has always been. The dependency ratchet had the same shape and moved further: cap **and** high-water both 13 → **9**. Four advisories cleared upstream; two more arrived mid-branch (js-yaml quadratic `!!omap`, nanoid zero-size loop) and **both were fixed rather than admitted** — `npm audit fix --package-lock-only` moved js-yaml 4.3.0 → 4.3.1 and nanoid 3.3.16 → 3.3.18. The allowlist shrank instead of growing, which is the branch the guard's own error message asks for first ("Fix it, **or** add it to ACCEPTED"). Worth recording because the reflex is the second branch: an earlier revision of this wave had already written a reasoned acceptance for js-yaml before checking `fixAvailable`, which was `true`.

**The repo's own map was wrong.** `CLAUDE.md` documented a **16**-step gate; it runs **18**, and the list omitted **Coverage floors** — the step that had degraded most — and the port guard. New `gateDocParity.test.ts` derives the count from `gate.mjs`'s step increments and checks the ordinals.

**Third rebase, third label collision.** `.545`→`.548`, `.562`→`.595`, `.595`→`.596`: master ships every 15–20 minutes, so any branch open longer than one ship interval collides on both the build label *and* the `LOG-rotate-<label>.md` filename, because both derive from the **incoming** label — the one value another branch may claim first. This time master rotated the *same* `.580` entry into the *same* filename, so the archive conflict was content-identical. Naming the archive after the entry it contains (permanent, rotated exactly once) instead of the label it was rotated for (contested) would make that class of conflict trivial. Proposed, not taken — it changes a documented convention in `LOG.md`.

**Also merged here:** `joinClassLoading` existed twice after the merge — this branch put it in `fitnessTestLocales`, master's `.595` put the whole `joinClass*` family in `infoLocales`. Both merge into one namespace, so one would have silently won. Master's placement kept; the duplicate removed.

**Not closed, founder-owned:** Actions is not assigning runners (`runner_id: 0`, ~3s death), so neither PR CI nor `ratchets.yml` can fire until the spending limit clears. Branch protection likewise. The **bundle budget stays red** — its own optimisation change, with chunk-level analysis now in hand.

**Two hero `@gate` specs restored — the kaizen programme had been breaking its own guards.** Three were red on `master`; the causes are three different mechanisms with one shape, *a deliberate UX change silently invalidating a hero test that no CI lane was running*. `.550` gave the rest-Skip button an `aria-label`, which **replaces** the accessible name, so an anchored `/^skip$/i` stopped matching — in the same file, a few lines below a comment that read *"Label stays exactly 'Skip' — logger-depth / a11y match /^skip$/i"*. `.548` changed empty-Finish behaviour and copy ("nothing logged" → "Log a set first", session now **kept**), and the spec still asserted the pre-`.548` product on both counts; note the i18n key kept its old name `activeNothingLogged` while its value changed, so grepping the key agreed with the stale assertion. Both now assert the current contract — `logger-depth` keys off `data-testid`, since the visible word and the announced label are both surface a kaizen pass is expected to change.

**The third is left red on purpose.** `workout complete updates Mission Score` fails because the score band now sits inside a collapsed disclosure: the span resolves — a collapsed container keeps content in the DOM — and reports `hidden` forever. A first repair probed with `if (await details.count())`, which is a point-in-time question with no waiting against a `dynamic(..., { ssr: false })` mount, so it skipped silently and failed one line later — `CLAUDE.md` §6's *assert preconditions, never skip past them*, reproduced inside a repair for a test nobody was running. Waiting on `getByRole('group', { name: /today details/i })` then found nothing, so the disclosure is not the shape the a11y snapshot implied. Cause known, repair not landed; reverted rather than shipped half-working.

**`npm run relabel` — the treadmill reconciliation as a command.** Renumbering past `origin/master` moves seven sites, not one: the label, the LOG heading, the LOG archive link, the `## Now` header, the CONTEXT bullet, the archive file and its `# Rotated for` heading, and the archive INDEX row. Done by hand three times in one day, each time under the pressure of a branch that would not merge — which is precisely when bookkeeping gets skipped and hard rule 5 quietly stops holding.

Its own falsification found three defects in it, all of the shape this repo keeps meeting. It **interleaved validation with writing**, so refusing an archive collision left four sites already rewritten — a half-relabelled tree, the state the command exists to prevent; now it plans, then applies, or does neither. Two edits against the same file each resolved from the on-disk original, so the second write **silently discarded the first** and the run still printed `✓` for both: `## Now` never moved while the output claimed it had. And a global replace of the archive link rewrote **master's** `.594` link as well as the branch's — in a collision the two are textually identical, so it now takes the last occurrence only (the branch appends) and refuses outright when an INDEX row is ambiguous. The INDEX row also moves only when the branch owns the archive file it names: a lone match looks unambiguous exactly when it belongs to somebody else.

Verified fail-closed: no-op when already past master, refuses on archive-name collision, refuses on ambiguous row, and writes nothing in either refusal. Green path moves all seven and leaves `check-build-label` passing with master's own links untouched.

Mutants: 12 killed — the 9 above, plus toast copy → red, `data-testid="rest-skip"` renamed → red, disclosure summary label changed → red. Tests 2049 → 2152.
