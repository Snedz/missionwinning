# Archived from LOG.md for `.1052` (Revert Patreon costume, restore wireframe)

`.1051` landed on master as squash
`49dfe6de` (PR #878) and was
reverted in `.1052`. Heading kept
so rotation moves, never deletes.

## 2026-08-27 — Patreon costume on our skeleton (`.1051`)

Founder said GO. The modernist
paper/ink/Archivo system is the
**wireframe costume**. The live
site clones unsigned / creator-studio
Patreon visually and hangs **our**
routes and copy on it.

**Ship:** `docs/DESIGN_PATREON.md` +
shared `src/styles/patreonTokens.css`
(`.ptn`). ABC Oracle Plus is the
reference face, not licensable —
Archivo Variable stays. Modernist
`DESIGN_SYSTEM.md` is not overwritten.
`sites/www` snapshotted at
`docs/archive/www-wireframe-1050/`.
Public www: sticky Train / Coach /
History / About, center wordmark,
Log in + Get notified pills, photo
hero, editorial shelves. Copy from
`homeContent.ts`. No fake traction.
Signed-in dual rail (Today / Train /
Coach / History / Library / Account).
Today stays **one Start**. `/account`
is two-column settings. `/private`
diff vs `.1050` is empty. Cookie `/`
`LandingPage` stays `.696`. Guest.
First set ungated. No `PRIVATE_MODE`
flip. No production promote.
`[skip vercel]`.

Label `2026.07-unified.1051` (from
master `.1050` / `c2b54d585c80ab1f5a2de2070f147ee549782c78`).
Stamp stays `.1051`. Live www stays `.696`.

`.1052` reverted this ship. The
`.1035` rotate that `.1051` wrote
(`LOG-rotate-1035-for-1051.md`)
was undone with the squash; `.1035`
is now [LOG-rotate-1035-for-1052.md](LOG-rotate-1035-for-1052.md).
