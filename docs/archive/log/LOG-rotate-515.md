# Rotated for .515

## 2026-08-05 — Flow-6/7/8 readiness train-first + coach invite (`.499`)

Chip residual after Flows 0–4: after first log, phase becomes readiness and Today boss could be **PAR-Q** or **guidebook** while Victory/First Steps boss **session 2**. Pure `pickReadinessPrimaryAction` — while `!streakMet`, primary is train (`/active`, week-1 session-2 copy at exactly one log); PAR-Q only after commitment for commissioning; guidebook never primary. Flow-7: `todayCoachInviteMayMount` for sessions 1–3 on basic **or** readiness (was basic-only dead after first log). Flow-8: Library “Today Hub” → “Today”.

Mutants: readiness primary `/learn/guide` → red; coach-invite basic-only phase gate → red.

