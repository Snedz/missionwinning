## 2026-08-04 — Coach chat transcript + composer peel (`.448`)

Premium open UI peels into `CoachChatTranscript` + `CoachChatComposer`. Panel keeps fetch/SSE + free peels; open body mounts the two. Panel 349→287. Wiring guards.

Mutants: re-inline role="log" on the panel → wiring red; leave coach-chat-input on the panel → wiring red.
