# Rotated from LOG.md for `.883`

## 2026-08-16 — Returning dose follows missed sessions (`.865`)

H-07: the same fourteen-day gap is not the same return. Stored miss
markers set the dose. No plan keeps the calendar short-session scale.
The quiet line still names no gap.

**Ship:** `doseScaleForMissedSessions` · `computeReentry` reads the plan.

Label `.865` (onto master `.864`).

Excellence-Override: re-entry dose (Today/Coach start; RESULT unscored)

Rotated LOG oldest → [docs/archive/log/LOG-rotate-850-for-865.md](LOG-rotate-850-for-865.md).
