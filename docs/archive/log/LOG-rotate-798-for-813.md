# Rotated from LOG.md when `.813` landed

## 2026-08-14 — Astro `/vision` only; Next keeps the host (`.798`)

`sites/www` had index, start, week, about. No vision page. C1 is that
page, additive. Next keeps `www.missionwinning.com` and Next `/vision`.

**Ship:** `vision.astro` + `visionContent.ts`. Same English the Next
vision page already paints. No `_redirects`. No national-fitness track.

Label `.798` (onto master `.797`).

Excellence-Override: commissioned www vision page (surface; RESULT unscored)
