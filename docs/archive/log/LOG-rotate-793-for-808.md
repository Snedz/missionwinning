# Rotated from LOG.md when `.808` landed

## 2026-08-14 — Landing Reveal paints; photo slots are real (`.793`)

After Done, `/` is the `.696` landing. Below-fold bands (When you miss /
Where you train / The free core) lived in the DOM at opacity 0 until
IntersectionObserver fired. Full-page capture never intersects, so the
founder saw blank paper. The three stills were already in `public/photo/`
and already passed as `GrayscalePhoto` `base` — the empty captions were
the hidden band, not missing files.

**Ship:** `.reveal` paints at opacity 1. No restyle. No third red on `/`.
No `PRIVATE_MODE` flip.

Label `.793` (onto master `.792`).

Excellence-Override: landing Reveal was blank paper (surface; RESULT unscored)
