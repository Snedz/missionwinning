# Rotated from LOG.md for `.916`

## 2026-08-17 — Lean dock is Start, not I-Day (`.901`)

`.890` killed the tour card. The dock still said Begin I-Day
and sent the tap to `/welcome`. A notes app still won.

**Ship:** lean dock is Start / Resume / Just Go. Cold tap
opens Train. Welcome stays in Search. Same logger.

Label `.901` (stacked on `.900`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-886-for-901.md](LOG-rotate-886-for-901.md).
