## 2026-08-25 — EMOM/AMRAP timer (`.988`)

Train already had rest. Missing: a clock
that is not rest — an interval or countdown
on the set they are logging so they do not
fuss with a watch for EMOM or AMRAP.

**Ship:** optional EMOM minute (60s
interval, restarts) or AMRAP window (5 / 10
/ 12 / 20 min countdown) on the live set
row. Empty invents nothing. Rest still runs
for ordinary rest. Drop-set rest-zero `.986`
stays. One athlete clock at a time. Guest.
First set ungated. Today still one Start.
Resume `.963` kept. Warmup `.985` / notes
`.983` / 1RM `.981` / supersets `.980` /
Learn `.978` / week strip `.977` / Track
`.976` / Move `.974` / cues `.973` /
honesty `.971` / tags `.970` / RPE `.967`
stay. `/private` stays the tight lock. No
`PRIVATE_MODE` flip. Not Watch-as-pitch.

Label `.988` (title stays `.987`; past
master `.986` / `9c3b2ce6`). Live www
stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-963-for-987.md](docs/archive/log/LOG-rotate-963-for-987.md).
