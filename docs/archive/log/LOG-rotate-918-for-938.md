# Rotated from LOG.md for `.938`

## 2026-08-18 — Fuel house door is Search, barcode & recipes (`.918`)

Fuel reused defaultValue Show all on `fuelShowMore`. Packs
win, so first paint said Show all then the Fuel lecture.

**Ship:** defaultValue matches the pack. Same notepad. Same
disclosure.

Label `.918` (stacked on `.917`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-903-for-918.md](LOG-rotate-903-for-918.md).
