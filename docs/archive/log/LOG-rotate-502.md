## 2026-08-05 — Free-beta mute + launch-env honesty (`.487`)

Kaizen night A: durable free-beta mute guard (`freeBetaMute.test.ts`) — hard mute surfaces + discover merchandising `/bundle` links must call `isFreeBeta`. Re-entry `doseScale` wiring locked on Today shells. `check-env --launch` warns when `MAIL_POSTAL_ADDRESS` unset and notes FREE_BETA default; `launch-verify` footer lists postal/migrations/agent hard law.

Mutants: remove bundle redirect → discover/hard mute red; strip doseScale from HomeTodayLean → dose test red.
