## 2026-08-09 — Today health chips + pillar labels honesty (`.644`)

Cross-pillar coach chips used `defaultValue: actionLabel` for insight text (hydrate showed "Open Move pillar" as the tip). Chips now use `translateCoachInsightLine` / action label floors; visible label is the short action, full insight on `title`. PillarScoreBreakdown never defaults labels to raw keys (`train` → `Train`).

Rotated LOG oldest → [docs/archive/log/LOG-rotate-644.md](docs/archive/log/LOG-rotate-644.md).

