# Rotated LOG — Supersets (`.980`)

Rotated 2026-08-25 for In-set PR they hit (`.999`).
Full record from [LOG.md](../../LOG.md).

## 2026-08-25 — Supersets (`.980`)

A named-app set log treats a superset as a
group, not a tag. Pair-of-two already lived
on the logger. After tags, RPE, and cues,
leftover in-set grammar: pair movements so
rest + vs-last do not treat A2 as a new
exercise. Circuits they already run stay
on the notebook.

**Ship:** two or more exercises group as
one superset in the live log. Rest after
the last peer in the round, on the first
peer's rest. vs-last stays per movement.
After-complete cite is quiet mid-round.
Save / Start / Repeat last / Finish-partial
keep the group when they had one. Empty
invents nothing. Optional. Guest. First
set ungated. Today still one Start. Resume
`.963` kept. Week strip `.977` / Track
`.976` / Move `.974` / cues `.973` /
honesty `.971` / tags `.970` / RPE `.967`
/ Fuel `.965` stay. Learn `.978` stays.
`/private` stays the tight lock. No
`PRIVATE_MODE` flip.

Label `.980` (past master `.978` / `1a06fbd7`;
concern `.979`). Live www stays `.696`.

Rotated LOG oldest → [LOG-rotate-957-for-980.md](./LOG-rotate-957-for-980.md).
