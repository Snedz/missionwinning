# Rotated from LOG.md for `.1023` (Coach citation empty load is BW, not 0kg)

## 2026-08-25 — Find a past session (`.1008`)

Week strip + movement history +
start-from still leave a long
scroll. The History box only
matched the template name.
Untitled rows are dates. Named
rows are a private title.

**Ship:** search the History list
by title, template, date, lift,
or session note. Empty query
invents nothing — same live rows.
No matches stays empty. Tombs
stay under Deleted sessions.
Guest. First set ungated. Today
still one Start. Name `.1007`
stays. `/private` stays the tight
lock. No `PRIVATE_MODE` flip.

Label `2026.07-unified.1008` (from
master `.1007` / `cd066471`). Stamp
stays `.1008`. Live www stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-993-for-1008.md](docs/archive/log/LOG-rotate-993-for-1008.md).
