## 2026-08-04 — ActiveReadinessDeltaStrip peel (`.432`)

Readiness delta + volume-trim offer peels into `ActiveReadinessDeltaStrip`. Page mounts it; toast helpers + show gates live in the strip. Wiring updated (.406/.408 → .432).

Mutants: leave volumeTrimToastKind on the page → wiring red.
