## 2026-08-04 — ActiveWorkoutSheets overlay cluster (`.450`)

Check-in · form · add · plates · victory peels into `ActiveWorkoutSheets`. Page mounts the cluster; handlers stay on the page. Page ~660→632. Wiring guards.

Mutants: re-inline FormGuideSheet/AddExerciseSheet on the page → wiring red; drop Victory from sheets → wiring red.
