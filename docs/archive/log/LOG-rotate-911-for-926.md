# Rotated from LOG.md for `.926`

## 2026-08-17 — Cue me is in the overflow (`.911`)

The table is the logger. Spoken next-set lines are optional.
Cue me is a labeled tap in More, not a second red on the header.

**Ship:** `ActiveTrainCues` in the overflow. Label stays at 390.
Rest-end speak needs Cue me on and a skip/end tap. `/active`
files still do not import `@/lib/speech`.

Label `.911` (stacked on `.910`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-896-for-911.md](LOG-rotate-896-for-911.md).
