# Rotated from LOG.md for `.907`

## 2026-08-17 — Cold chrome is Summary + Search (`.892`)

Tiny hops after the set table left the five-tab house on first open.
Research already named the floor: Home is pins + Start. Coach and Fuel
are Search. Train joins the dock only while a session is live.

**Ship:** `resolveMobileTabHrefs`. Cold bar is Summary + Search. Lean
Today is date, 0–4 pins, one highlights sentence, one docked Start.
Journey strip, rewards, continuity, and day-review deleted from lean
(not hidden). Same logger, same outbox.

Label `.892` (stacked on `.891`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-874-for-892.md](docs/archive/log/LOG-rotate-874-for-892.md).
