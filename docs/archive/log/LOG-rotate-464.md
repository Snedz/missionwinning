## 2026-08-04 — NL several/lots/double-portion/bowl-of (`.449`)

`several` → 3; `a lot`/`lots` → 2; `double portion` → 2 (GLOBAL `double` no longer scales portion phrases); `servings`/`bowls` join portion words; `bowl of rice` skips Bowl-base food match so it does not double-count.

Mutants: drop several → 1 egg → red; leave bowl-of as Bowl+Rice → red; map double portion only via 1.35 adj → not 2× → red.
