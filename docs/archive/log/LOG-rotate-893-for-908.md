# Rotated from LOG.md for `.908`

## 2026-08-17 — Pins tap; Edit persists (`.893`)

A pin you cannot tap is wallpaper. Highlights that ignore the log are
a tour. Summary is a grid you Edit, and a sentence that only speaks
when a set exists.

**Ship:** `parseSummaryPinIds` / `resolveSummaryPins`. Session pin runs
the same Start as the dock. Fuel and Coach are optional paper doors.
Highlights count today’s sets from the log. Default is still one pin.
0 pins is allowed.

Label `.893` (stacked on `.892`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-875-for-893.md](docs/archive/log/LOG-rotate-875-for-893.md).
