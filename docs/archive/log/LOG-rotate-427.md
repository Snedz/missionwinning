## 2026-08-04 — Victory session-2 next after first log (`.412`)

`pickVictoryNextAction` uses `week1SecondSessionCue` when completedWorkouts
is 1 so Victory and Today share one boss habit (T1.3). Early Coach CTA
starts at session 2+.
