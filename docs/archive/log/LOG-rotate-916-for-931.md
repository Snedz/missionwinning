# Rotated from LOG.md for `.931`

## 2026-08-18 — Show all is Show all (`.916`)

House doors reused the Fuel key. Packs won, so Train,
Coach, Mind, Victory, History, Library, and Builder said
Search, barcode & recipes.

**Ship:** those surfaces use `todayShowAll`. Fuel keeps
`fuelShowMore`. Same disclosures.

Label `.916` (stacked on `.915`).

No `PRIVATE_MODE` flip.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-901-for-916.md](LOG-rotate-901-for-916.md).
