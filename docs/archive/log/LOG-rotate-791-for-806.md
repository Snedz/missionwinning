# Rotated from LOG.md when `.806` landed

## 2026-08-14 — Homepage after Done is the `.696` landing (`.791`)

Preview sent Done to I-Day. The page the founder wants is production
from 2026-08-12 (`8398ae3b`, dpl `4fi6bsdkh`): MarketingNav, Log a set
/ Your week rewrites itself, squat demo. Not cinematic www.

**Ship:** restore that LandingPage. `/` unlocks from the signed cookie
only — `hasServerPrivateAccess` is true whenever the gate is off, so
Preview would have skipped the teaser. Done on Preview mints the
cookie. Production still checks codes. No `PRIVATE_MODE` flip.

Label `.791` (onto master `.790`).

Excellence-Override: restore .696 homepage after Done
