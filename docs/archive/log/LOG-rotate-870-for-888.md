# Rotated from LOG.md for `.888`

## 2026-08-16 — Harness follows the Horizon W path (`.870`)

Empty GRAPH_LOOP plus an empty harvest used to stop. That is the right
rule for letters and the wrong rule for the product: Horizon W is still
NOW and still unscored.

**Ship:** `route` `path` (recipe 15) · `criticalPath.ts` names the first
unproven W-instrument. Current master prints `C5` founder phone, not AU2.

Label `.870` (onto master `.869`).

Excellence-Override: harness critical path (tooling; RESULT unscored)

Rotated LOG oldest → [docs/archive/log/LOG-rotate-855-for-870.md](LOG-rotate-855-for-870.md).
