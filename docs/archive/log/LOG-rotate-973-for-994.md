# Rotated LOG — Cues on the movement (`.973`)

Rotated 2026-08-25 for Set-row type (`.994`).
Full record from [LOG.md](../../LOG.md).

## 2026-08-25 — Cues on the movement (`.973`)

The live set already had a Form guide
sheet behind Info. Setup lived one tap
away. Week-4 dies when they do not know
the setup and abandon the set.

**Ship:** opening the live exercise
shows a short written cue list (setup
first, then execute, cap 3). Optional
still only from media we already have.
Hide never blocks Log set. Full guide
stays behind Info. Not a clip feed.
Not on Today. Guest. First set ungated.
Today still one Start. Resume `.963`
kept. Honesty `.971` / tags `.970` /
RPE `.967` / Fuel `.965` stay.
`/private` stays the tight lock. No
`PRIVATE_MODE` flip. Skips `.972`.

Label `.973` (past master `.971`). Live
www stays `.696`.

Rotated LOG oldest → [LOG-rotate-952-for-973.md](./LOG-rotate-952-for-973.md).
