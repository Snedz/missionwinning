# Rotated from LOG.md for `.951`

## 2026-08-24 — One set-count for a completed log (`.930`)

Today highlights and Victory receipt each had a private
`setCount`. Same domain type, two bodies.

**Ship:** `countCompletedLogSets` is the one helper.
Both readers import it.

Label `.930` (stacked on `.929`).

Rotated LOG oldest → [docs/archive/log/LOG-rotate-915-for-930.md](docs/archive/log/LOG-rotate-915-for-930.md).
