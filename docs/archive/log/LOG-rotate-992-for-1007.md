## 2026-08-25 — Custom exercise (`.992`)

The live set picker already searched
the catalog. A library miss killed the
add — "No matches" — and a session row
whose id was not in the catalog was
unmounted. They could not name a
movement during a live workout and
keep logging.

**Ship:** type a name the library does
not have on the live Train add/swap
picker. It persists locally and in the
session. Log sets without leaving
Train. Unlimited. Free. Guest. First
set ungated. Empty invents nothing.
Stays theirs on Repeat last / Start
this again / the local diary. Not a
public catalog. No video. No paywall.
No invented library-size traction.
Today still one Start. Resume `.963`
kept. Start this again `.991` still
holds. Track trend `.989` last-vs-this
stays on the strip cell. EMOM `.988` /
drop-set `.986` / warmup `.985` /
notes `.983` / 1RM `.981` / supersets
`.980` / Learn `.978` / week strip
`.977` / Track `.976` / Move `.974` /
cues `.973` stay. `/private` stays the
tight lock. No `PRIVATE_MODE` flip.

Title stays **Custom exercise (.990)**.
Label `2026.07-unified.992` (past
master `.991` / `d561b8a5`). Live www
stays `.696`.

Rotated LOG oldest → [docs/archive/log/LOG-rotate-970-for-992.md](docs/archive/log/LOG-rotate-970-for-992.md).
