# www wireframe snapshot (`.1050`)

Snapshot of `sites/www` at the modernist paper/ink/Archivo costume, taken before the Patreon live costume (`.1051`). `node_modules`, `dist`, and `.astro` are omitted.

**Restore:** copy `www/` back over `sites/www` (then `npm --prefix sites/www install` if needed).

**Wireframe token sheet (do not overwrite):** [DESIGN_SYSTEM.md](../../DESIGN_SYSTEM.md)

**Live costume:** [DESIGN_PATREON.md](../../DESIGN_PATREON.md)

Taken from master `c2b54d585c80ab1f5a2de2070f147ee549782c78` (`.1050`).
