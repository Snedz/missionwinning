# CONTEXT.md `## Now` — `.1098` rotated for `.1114`

Rotated out of `CONTEXT.md` `## Now` on 2026-09-24 so the block stays within 25 bullets after `.1114` filled the budget. Full record remains in the LOG archive.

- **`.1098`:** (`2026.07-unified.1098`) **CapResult deny-code set freeze** — Closed host-lifecycle denies are exactly `scope_denied` / `unknown_method` / `unknown_capability` / `not_mounted` / `already_mounted` / `unknown_mini` / `bad_deeplink` / `storage_cap`. No silent ninth code without PLAN. Scoped stubs `stub` / `photos_stub` stay. Known envelopes stay `.1079`–`.1097`. No UI / Stripe / camera. Paper only. `[skip vercel]`. No tip-promote. Live www stays `.697`. PRIVATE_MODE stays.
