# VISION evidence

Own-app stills only. Name: `U<n>-R<r>-<beat>.png`. Competitor pixels stay out of git.

Viewport **390×844**. Local: `npm run dev` (localhost, not 127.0.0.1). Do not `next start` unless `PRIVATE_MODE=false`.

SMOOTHER R1 (2026-08-16, 390×844, process on :3000):

| File | Beat |
|------|------|
| `SMOOTHER-today.png` | Returning `/log` — First Steps 2/7 + dock `Just Go — Chest` |
| `SMOOTHER-train.png` | `/active` empty — EmptyState + Today / Builder + `Repeat last session` |
| `SMOOTHER-victory.png` | Session locked · Volume **8 reps** · Next = See Mission Coach |
| `SMOOTHER-coach.png` | `/coach` 307 → `/private` on this host (`PRIVATE_MODE` explicit) |
