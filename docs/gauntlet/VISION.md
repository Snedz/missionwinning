# Vision gauntlet — standing workbench

**Status:** `ready-for-founder` · not a GRAPH_LOOP letter · hard cap **≤10 build PRs** (2 spent: `.886` `.887` — unused cap is success)
**Goal (pointers, not a second copy):** [vision.md](../../vision.md) (free logger + Mission Coach) · [docs/THESIS.md](../THESIS.md) year-1 metric (week-4 retained weekly loggers) · [ORCHESTRATION.md](../../ORCHESTRATION.md) mermaid (Alpha flip prep → public → Stripe → week-4).
**Now:** [CONTEXT.md](../../CONTEXT.md) `## Now`. Alpha 0.1.0. Mute-pay. `PRIVATE_MODE` on. Postal later.
**Protocol:** [GAUNTLET_LOOP.md](../GAUNTLET_LOOP.md). Builder never grades itself. Bar from §3 only.
**Terminal agent state:** `ready-for-founder`. Never write excellence `status: pass`. Never flip `PRIVATE_MODE`.

**Next spawn:** `ready-for-founder` — **report written.** U1–U2 critic PASS. SMOOTHER R1 walked Today / Train / Victory; Coach 307→`/private` on this host (`PRIVATE_MODE` explicit, not a product FAIL). No open builder brief. Next action is the **founder walk** against the report below. Do not write `status: pass`. Do not invent a GRAPH_LOOP letter. Do not open a unit for the below-fold thumb residual.

LEAD 2026-08-16: `#727` / `#728` still unproven on this tree. Victory cell + share card print `fmt.num(totalVolume)` + kg — a Just Go push-up session is **Volume 0 kg**. Helper already shipped (`volumeDisplay.ts` + test). `#727` duplicates it. `#728` (`week_one.svg` is still a closed triangle) is U2, not this hop.

Forbidden on Next spawn: postal, EIN, live Stripe, `PRIVATE_MODE=false`, America, iOS, new pillars, inventing traction.

## Units / bars

| Unit | Bar | Instrument (exact command) | Budget |
|------|-----|----------------------------|--------|
| **U1** | First-session bodyweight receipt prints working reps, not `0 kg`. Loaded volume still prints kg/lb. Warmups excluded. Victory cell and share card share `formatWorkoutVolumeDisplay`. | `npx tsx --test src/lib/workout/volumeDisplay.test.ts src/lib/workout/workoutVictory.test.ts src/lib/share/shareCard.test.ts` | 2 rounds |
| **U2** | Week One medallion is three session bars, not a closed triangle. | `npx tsx --test src/lib/rewards/badgeIcons.test.ts` | 2 rounds |

## Round log

| unit | round | builder ref | critic verdict | biggest gap |
|------|-------|-------------|----------------|-------------|
| U1 | 1 | #735 `.886` — wired `formatWorkoutVolumeDisplay` / `sumWorkingReps` into Victory + share card. No `victoryVolumeStat`. | **PASS** (trio). Live 390×844 Volume **8 reps**, not 0 kg. Instrument 37/37. Stills `docs/gauntlet/VISION/evidence/U1-R1-*.png`. FEEL (founder-only, not a builder brief): The receipt finally reads as work done — eight push-ups look like eight push-ups. Biggest remaining gap: The share card never painted on this live walk (Web Share: “Couldn’t share from this browser”), so share-card pixels are instrument-only. | Share-card PNG is instrument-only (not an instrument FAIL) |
| U2 | 1 | #736 `.887` — `week_one.svg` three bars; guard fails if `L32 20 L44 38 Z` returns. | **PASS** (trio). Live `/rewards/badges/week_one.svg` 200: three bars x=16/29/42, last `#ec3013`; no `L32 20 L44 38 Z`. Instrument 3/3. Stills `U2-R1-week-one.svg.png` · `U2-R1-week-one-mark.png`. FEEL (founder-only, not a builder brief): Three ink-and-red session bars read as a week started, not as a broken-image warning triangle. Biggest remaining gap: The 32px Victory line and 40px Profile shelf were not driven — this device has one workout (`first_blood` only) and `/profile` stills the `/private` teaser. | Week One 32px on Victory unearned on this device |
| Smoother | 1 | no builder — walk only. DESIGN_REVIEW `Gauntlet VISION.Smoother R1`. | Walk 390×844 on :3000. Today red `main` 0 / dock button-fills 0 (poster invert). Train empty invite + live Log set red in dock. Victory Volume **8 reps**. Coach `/coach` 307 → `/private` (`PRIVATE_MODE` explicit). Stills `SMOOTHER-today.png` · `SMOOTHER-train.png` · `SMOOTHER-victory.png` · `SMOOTHER-coach.png`. | Today below-fold thumbs <44; `/coach` gated on this host. Week One still unearned at 2 workouts. |

## Report

**Opened** 2026-08-16 as the standing workbench after Horizon W pass + Horizon 0 path tickets. Goal already in the repo. Not a letter.

**Spent 2 of ≤10** (`.886` U1 · `.887` U2). Unused cap is success.

| Unit | Result |
|------|--------|
| U1 Victory BW receipt | Critic **PASS**. Live 390×844 Volume **8 reps**, not 0 kg. Wired existing `formatWorkoutVolumeDisplay`. `#727` left unmerged (second helper). |
| U2 Week One mark | Critic **PASS**. Three session bars, not a closed triangle. `#728` left unmerged. 32px Victory line unearned on a 1–2 workout device. |
| SMOOTHER R1 | Walked. U1 holds on Victory. Coach 307 is the private gate. Below-fold Today thumbs <44 is residual, not a new unit. |

**Founder walk:** I-Day (bodyweight) → Chest Just Go → log 8 × BW → Finish. Receipt must say **8 reps**. Week One mark, once earned (≥3 sessions), must be three bars.

**Not this campaign:** `PRIVATE_MODE` flip · postal · EIN · Stripe · inventing a letter · opening a unit for below-fold thumbs or the logger next-line `8 × 0 kg` (Prev already says BW; that is a later unit if the founder wants it).

Only the founder writes [EXCELLENCE_RESULT.md](../EXCELLENCE_RESULT.md) `status`. It already is `pass`. This campaign does not touch it.
