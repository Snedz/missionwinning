# 06 — BUILD_LATER

Gate: founder override **or** a real payment, **and** at least one before/after Clean Up pair dumped on hardware.

## Phase A

1. macOS/iOS tool: drop a file → parse EXIF + IPTC (ImageIO / `CGImageSource` properties). No PhotoKit keyword query (no API).
2. Report: present / absent keys; copy-paste plain text for a claim file.
3. Explicit UI line: **Spatial Reframe pipeline API locked — this app does not reframe.**

## Phase B

- Batch folder of exports
- Optional C2PA read **if** a dumped file actually contains a manifest (UNKNOWN until seen)

## Never

- Calling or wrapping Spatial Reframe
- A pixel-space “AI detector” as a substitute when metadata is missing
- Claiming PhotoKit can filter the library by Photos keywords

## Proofs allowed now

A markdown table of IPTC DigitalSourceType URIs (see SOURCES) and this locked-API sentence. No “scanner” binary.
