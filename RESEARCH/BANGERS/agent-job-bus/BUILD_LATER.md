# agent-job-bus — build later

**Gate:** 5 cold pays / 14 days.

Forbidden now: hosted queue, MW cron replacement, Temporal clone.

If PASS: one binary, SQLite, HTTP or Unix socket `enqueue` / `ack` / `fail`. At-least-once. Document poison-message behavior. No dashboard in v1.
