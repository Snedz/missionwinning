# tutor-repair-dualcam — build later

**Gate:** 5 cold pays / 14 days.

Forbidden now: camera app, MW Train “form check” video.

If PASS: ship as **presets inside** dual-capture-plus **or** a config file for DoubleTake users (cheaper). Do not start a second Xcode target unless the parent slug also passed.
