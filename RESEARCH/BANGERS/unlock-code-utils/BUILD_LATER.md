# unlock-code-utils — build later

**Gate:** [SELL_FIRST.md](SELL_FIRST.md) result = **PASS** (5 cold pays / 14 days) **and** a founder/counsel skim of App Store 5.1.1 — or a written founder override.

Until then: no binary, no keychain vault, nothing that reads other apps’ screens.

---

## Forbidden now

- iOS/Android binary or share extension  
- Keychain / password vault  
- Reading other apps’ screens (no ReplayKit abuse, no “screen grabber”)  
- Copy that implies jailbreak, bypass, stolen accounts, 2FA intercept  
- A second brand if ClearShot is already asking — fold into a mode instead  
- Invented checkout URL  
- Cloud upload of codes

---

## If the bar passes — smallest after-PASS

1. Share extension + on-device text (Vision).  
2. Heuristics for code-shaped strings (Wi-Fi PSK, gift-code groups, backup-code lists).  
3. Clipboard by default. Optional local note. **No cloud. No account.**  
4. Privacy Nutrition: photos only; no tracking. Purpose string names the job.  
5. One IAP matching the lander ($4.99 HYPOTHESIS).  
6. Prefer shipping as a **ClearShot mode** if [clearshot-ios](../clearshot-ios/BUILD_LATER.md) also passed.

Out of v1: vault, iCloud sync, Android.

---

## Later (not v1)

Wi-Fi QR write to Settings (system APIs only, documented). Never a “unlock tool.”
