# ai-edit-auditor — build later

**Gate:** 5 cold pays / 14 days.

Forbidden now: hosted rewriter, MW coach chat changes.

If PASS: CLI or tiny web page, local files, unified diff + checklist JSON. Optional LLM for “did this sentence satisfy bullet 3” — only if the lander sold that. Default: deterministic diffs.
