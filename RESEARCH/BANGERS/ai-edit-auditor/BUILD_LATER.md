# ai-edit-auditor — build later

**Gate:** [SELL_FIRST.md](SELL_FIRST.md) result = **PASS** (5 cold pays / 14 days) or a written founder override.

Until then: no hosted rewriter, no MW coach chat changes.

---

## Forbidden now

- Hosted “AI editor” or chat UI  
- Changes to `src/lib/coach/` or `/api/coach`  
- Legal-admissible / court-ready copy  
- Inventing a category leader in the lander  
- Invented checkout URL  
- Cloud upload of client manuscripts as the default

---

## If the bar passes — smallest after-PASS

1. CLI or tiny local web page.  
2. Inputs: brief.txt + before + after (local files).  
3. Outputs: unified diff + checklist JSON. Human still clicks pass/fail.  
4. Default: **deterministic** diffs (names, leftover TODO / “as an AI”, length).  
5. Optional LLM for “did this sentence satisfy bullet 3” — **only** if the lander sold that.  
6. No account. Price matches the lander ($19 HYPOTHESIS).

Out of v1: team SSO, PDF redline clone of Draftable, MW integration.

---

## Later (not v1)

Batch folders. Word/PDF via an existing compare lib — only after repeat demand is written down.
