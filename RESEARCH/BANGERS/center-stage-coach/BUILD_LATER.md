# center-stage-coach — build later

**Gate:** 5 cold pays / 14 days.

Forbidden now: AVFoundation app, anything under `src/lib/coach/`.

If PASS: tiny camera preview + overlay. Fail closed on unsupported devices. No workout logging.
