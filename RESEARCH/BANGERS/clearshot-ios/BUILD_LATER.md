# clearshot-ios — build later

**Gate:** 5 cold pays / 14 days.

---

## Forbidden now

- iOS app, share-sheet extension, or TestFlight  
- Mounting `utility.clearshot` or opening `billing.read`  
- Cloud Vision / uploaded OCR  
- Using **CleanShot** as the filed App Store name without counsel  
- Android in v1

---

## If the bar passes — smallest binary

1. iOS share extension + tiny host app.  
2. On-device text rectangles (Vision). Tap to redact; brush to blur.  
3. Export PNG back to share sheet / Photos.  
4. No account. No analytics SDK.  
5. One IAP (lifetime) matching the lander.  
6. Privacy Nutrition: no tracking; photos only.

Out of v1: templates, video, team vaults, MW host.

---

## MW later (not this experiment)

`packages/mw-core` already reserves `UTILITY_CLEARSHOT_MANIFEST` (`utility.clearshot`, Android surface, `freeCore: true`, no billing). A future mini is a **different** commission. Do not “just hook the IAP into the host.”
