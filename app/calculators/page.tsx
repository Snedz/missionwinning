'use client';

import { CalculatorsPage } from '@/page-components/CalculatorsPage';

export default function Calculators() {
  // Public / marketing version of calculators (also accessible inside the app shell)
  return <CalculatorsPage />;
}
