# Landing documentary photos

Grayscale photography for marketing slots (`GrayscalePhoto` on `/`).

| File | Landing slot |
|------|----------------|
| `phone-bench` | Offline first |
| `home-rack` | Your gear, not a gym |
| `bare-wrist` | No wearable |

Each name has `.webp` + `.avif`. Pass `base="/photo/<name>"` — the component desaturates again for consistency.
