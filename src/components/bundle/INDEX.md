# src/components/bundle/

> One concern: Super Bundle shop cards on `/bundle`.

| File | Role |
|------|------|
| `BundleShopStack.tsx` | Free vs Super Bundle stacked cards — inventory from `bundleShop.ts` |

Page chrome + checkout success live in `src/page-components/BundlePage.tsx`.
Frozen plan: [docs/SUPER_BUNDLE_SHOP_PLAN.md](../../../docs/SUPER_BUNDLE_SHOP_PLAN.md).
