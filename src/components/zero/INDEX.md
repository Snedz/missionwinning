# src/components/zero/

> Signed-in chrome from zero — PathShell. Commands are Start / Log / Generate.
> Not House. Not AppLayout.

| File | Purpose |
|------|---------|
| `PathShell.tsx` | Root signed-in chrome. Journey sync + outbox drain stay mounted. Compact floor words; desktop side words. Train compact is immersive with a Today link. |
| `path.css` | Word-nav chrome. Rooms still use `house.css` until the look hop remints them. |

Do not import `HouseIconRail`, `HouseMore`, `HouseSecondRail`, `HouseGuide`, or `CommissioningCeremony`.
