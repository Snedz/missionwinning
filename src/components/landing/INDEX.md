# src/components/landing/

Marketing / gated-www only. Not Today, Train, or Coach.

| File | What |
|------|------|
| `CinematicWww.tsx` | Four 100svh scenes (SET · ANYWHERE · WEEK · DOOR) plus a quiet later line after `</main>` (not a fifth scene). SET is a full-viewport logger field with TARGET / set table and one quiet Coach line under the h1 (L1 on fold 1). Anywhere: type on the still (live port: honest ink slot). Week: authored miss / travel / band. Door: poster-red field, form on a paper strip. HUD nav, no bar. `mode: gate \| open`. L2 never the first viewport. |
| `CinematicLogger.tsx` | Viewport-scale log-a-set still. TARGET chrome. Same `suggestNextSetTarget` engine as `/active`. |
| `cinematic.css` | Tokens only. Radius 0. One inverted field. Poster door. Ghost CTAs. |
| `LogToPlanHero.tsx` | Earlier boxed specimen — kept; cinematic logger is the www hero now. |
| `CoachAdaptDemo.tsx` | Adapt demo used on older landing bands. |

Direction: [docs/design/WWW_NIGHT.md](../../../docs/design/WWW_NIGHT.md). Comp: [docs/design/concepts/05-exquisite.html](../../../docs/design/concepts/05-exquisite.html).
