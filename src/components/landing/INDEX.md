# src/components/landing/

Marketing / gated-www only. Not Today, Train, or Coach.

| File | What |
|------|------|
| `CinematicWww.tsx` | Live gated door (`.935`): `GateTeaser` mounts this with `mode="gate"`. Four 100svh scenes (SET · ANYWHERE · WEEK · DOOR) plus a quiet later line after `</main>` (not a fifth scene): History you own. Today is not a Feed. Dated Never list lives on the door paper strip, not after main. SET is one display line + Set eyebrow; HUD mark only. Fold 1 public line is Log a set. Offline.; support is No account. No wearable. Anywhere: type on the still (live port: honest ink slot). Week: authored miss / travel / band (Mission Coach). Door: poster-red field, form on a paper strip. HUD nav, no bar. Cookie / gate-off `/` stays `.696` `LandingPage` — do not restore this as `/`. L2 never the first viewport. |
| `CinematicLogger.tsx` | Viewport-scale log-a-set still. Last / Next / Why. TARGET chrome. Same `suggestNextSetTarget` engine as `/active`. |
| `cinematic.css` | Tokens only. Radius 0. One inverted field. Poster door. Ghost CTAs. |
| `LogToPlanHero.tsx` | Earlier boxed specimen — kept; cinematic logger is the www hero now. |
| `CoachAdaptDemo.tsx` | Adapt demo used on older landing bands. |

Direction: [docs/design/WWW_NIGHT.md](../../../docs/design/WWW_NIGHT.md). Comp: [docs/design/concepts/05-exquisite.html](../../../docs/design/concepts/05-exquisite.html).
