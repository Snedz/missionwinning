'use client';
/**
 * Page: /log — Today. Summary first paint in every phase.
 * Date, pins, one sentence, one Start. Dashboard is not this route.
 * See: docs/JOURNEY.md
 */

import { HomeTodayLean } from '@/page-components/HomeTodayLean';

export function HomePage() {
  return <HomeTodayLean />;
}
