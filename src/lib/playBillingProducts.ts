/** Shared Play product IDs (client docs + server allowlist). */
export const PLAY_PRODUCT_ALLOWLIST = new Set([
  'super_bundle_monthly',
  'super_bundle_yearly',
  'super_bundle',
]);

export const PLAY_DEFAULT_SUBSCRIPTION_ID = 'super_bundle_monthly';
