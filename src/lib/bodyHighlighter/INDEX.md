# src/lib/bodyHighlighter/

> Display-only map from recent completed sessions to `react-body-highlighter` rows. Not a planner input.

| File | Role |
|------|------|
| `muscleBalance.ts` | `{ name, muscles, frequency }[]` from `buildMuscleHeatmap` session counts |
| `muscleBalance.test.ts` | Window, tombs, one session once, closed slug projection |
| `NOTICE.md` | MIT copyright for `react-body-highlighter` (GV79, 2020) |

UI: `src/components/coach/MuscleBalanceMap.tsx`, mounted once on `/coach`.
