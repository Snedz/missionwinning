/** Shown on Profile so testers can confirm the deployed build. Bump when shipping UI to master. `.752` pregnancy safety v1 — hard-session stop line only (counsel-hold). */
export const APP_BUILD_LABEL = "2026.07-unified.752";
